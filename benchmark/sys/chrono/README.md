# Бенчмарки модуля работы с датой и временем

Отчёт по набору сценариев `benchmark/sys/chrono`. Измеряется модуль
`src/sys/chrono` — разбор и формирование записей дат в произвольном формате,
календарная арифметика, работа с временными зонами и обозначениями
продолжительности.

## Как запускать

```sh
# все сценарии подмодуля с проверкой порогов
./benchmarks/sys --filter=chrono

# измерение без проверки порогов (калибровка на новой машине)
./benchmarks/sys --filter=chrono --relaxed

# одна группа сценариев
./benchmarks/sys --filter=chrono/parse
./benchmarks/sys --filter=chrono/units/offset
```

Отклонение любого показателя от порога завершает прогон с ненулевым кодом,
поэтому набор пригоден как шаг сборки: регрессия останавливает конвейер.
Полный прогон занимает около двух секунд в неоптимизированной сборке.

## Что измеряется и почему именно это

У каждой измеряемой операции два показателя: количество операций в секунду и
количество выделений памяти на операцию. Второй важнее первого — от машины и
режима сборки он почти не зависит, и порог по нему задан вплотную к измеренному.

| Сценарий | Что нагружает |
|---|---|
| `parse/iso8601` | Разбор самого распространённого формата обмена датами: восемь переменных формата, включая миллисекунды и обозначение зоны |
| `parse/clf` | Формат журнала веб-сервера: месяц записан названием и определяется поиском по таблице, смещение зоны — числом |
| `parse/compact` | Сплошная запись без разделителей: границы полей задаёт один только формат. Ловит регрессию, при которой разбор начинает опираться на разделители вместо разрядности поля |
| `format/iso8601` | Формирование полной записи со всеми составляющими даты |
| `format/clf` | То же с выводом месяца названием |
| `format/strip` | Перевод записи из формата в формат: разбор исходной и формирование конечной за один вызов |
| `format/zone` | Обозначение временной зоны из смещения. Смещение некратно часу: кратное формируется коротким путём без вывода минут |
| `units/timestamp` | Получение текущего штампа времени — самая частая операция модуля |
| `units/get` | Извлечение составляющей даты. Требует полного разложения штампа на календарные поля, поэтому измеряет разложение целиком, а не выборку одного поля |
| `units/begin` | Граница месяца: определение года, начала года, признака високосности и перебор длительностей месяцев. Границы суток, часа и минуты получаются остатком от деления и меряли бы одну операцию |
| `units/actual` | Остаток дней в году: граница наибольшей единицы измерения и разница до неё |
| `units/offset` | Смещение даты на месяцы — единственную единицу непостоянной длительности, требующую разложения даты и обратной сборки |
| `units/abbreviation` | Подбор наибольшей единицы измерения, в которой продолжительность выражается значащим числом |
| `zones/match` | Сопоставление обозначения зоны с известной. Выполняется на каждой разобранной записи, размеченной названием зоны |
| `zones/offset` | Перевод обозначения со смещением (`GMT-0328`) в секунды: разбор обеих составляющих |
| `duration/parse` | Разбор обозначения размерности времени (`90m`). Им задаются сроки жизни соединений, таймауты и сроки хранения |
| `duration/print` | Обратное преобразование: продолжительность в секундах в текстовое обозначение |

### Про эталонную дату

Все сценарии работают от одной даты — `1743943021520`, то есть
`Sun, 06 Apr 2025 15:37:01.520 UTC+3`, той же, на которой построены юнит-тесты
модуля. Все её составляющие значащие: ненулевые миллисекунды, середина года,
середина месяца, вторая половина суток. Дата вида «полночь первого января»
сокращала бы работу календарной арифметики и давала бы завышенные показатели.

### Про устройство замера

Два приёма в оснастке замера обязательны, и без них набор измерял бы пустой
цикл — причём только в оптимизированной сборке, где показатель и важен.

**Довод операции меняется от круга к кругу.** Методы модуля не имеют побочных
действий, и вызов с одними и теми же доводами компилятор вправе вынести из
цикла целиком. Дата поэтому смещается на номер круга: довод меняется, а
миллисекунды календарного положения даты не меняют, и объём работы остаётся
прежним.

**Результат каждого круга накапливается внутри самого круга.** Присваивание
результата переменной, читаемой только после цикла, компилятор устраняет как
запись, которую следующий круг всё равно перезапишет, — и от всего прогона
остаётся последний круг. Именно так и вышло при первом прогоне
`units/abbreviation`: в `Release` он показал 1.3 млрд операций в секунду.
Накопление связывает круги зависимостью по данным, устранить которую нельзя.

Накопленное значение уходит в контрольную сумму набора, объявленную
`volatile`: устранить запись в неё компилятор не вправе, а вместе с нею не
устранит и всю цепочку, её породившую. Прежде роль этой проверки играл
показатель выделений памяти — ненулевое его значение доказывало, что операции
выполнялись все до одной, — но разбор записи выделять память перестал, и
доказательством осталась одна лишь контрольная сумма.

## Результаты

Apple Silicon. Приведены обе сборки: режим по умолчанию репозитория
(`CMAKE_BUILD_TYPE` не задан, флагов оптимизации нет) и `Release`.

| Сценарий | Без оптимизации | Release | Порог |
|---|---:|---:|---:|
| `parse/iso8601` | 718 751 разборов/с | 6 211 983 | 340 000 |
| `parse/iso8601/allocations` | **0** | **0** | 0.01 |
| `parse/clf` | 716 103 | 6 238 562 | 350 000 |
| `parse/clf/allocations` | **0** | **0** | 0.01 |
| `parse/compact` | 917 605 | 7 542 023 | 440 000 |
| `parse/compact/allocations` | **0** | **0** | 0.01 |
| `format/iso8601` | 1 481 976 записей/с | 3 774 849 | 750 000 |
| `format/iso8601/allocations` | **1** | **1** | 1 |
| `format/clf` | 1 670 212 | 4 263 445 | 800 000 |
| `format/clf/allocations` | **1** | **1** | 1 |
| `format/strip` | 427 771 переводов/с | 2 499 781 | 200 000 |
| `format/strip/allocations` | **1** | **1** | 1 |
| `format/zone` | 21 303 415 обозначений/с | 54 628 629 | 10 000 000 |
| `format/zone/allocations` | **0** | **0** | 0.01 |
| `units/timestamp` | 39 413 915 штампов/с | 70 535 959 | 13 000 000 |
| `units/get` | 9 709 979 извлечений/с | 55 166 576 | 4 500 000 |
| `units/begin` | 23 932 557 границ/с | 114 333 616 | 11 000 000 |
| `units/actual` | 37 734 425 расчётов/с | 124 789 417 | 14 500 000 |
| `units/offset` | 7 637 999 смещений/с | 34 161 268 | 3 700 000 |
| `units/abbreviation` | 99 956 678 аббревиатур/с | 1 342 883 923 | 47 000 000 |
| `units/*/allocations` | **0** | **0** | 0.01 |
| `zones/match` | 4 763 946 сопоставлений/с | 85 818 493 | 1 850 000 |
| `zones/match/allocations` | **0** | **0** | 0.01 |
| `zones/offset` | 1 903 805 переводов/с | 35 070 799 | 780 000 |
| `zones/offset/allocations` | **0** | **0** | 0.01 |
| `duration/parse` | 6 563 187 разборов/с | 85 576 727 | 2 650 000 |
| `duration/parse/allocations` | **0** | **0** | 0.01 |
| `duration/print` | 1 113 324 обозначений/с | 2 805 321 | 590 000 |
| `duration/print/allocations` | **0** | **0** | 0.01 |

Пороги пропускной способности откалиброваны по неоптимизированной сборке с
двукратным запасом: они ловят регрессию в разы, а не колебания планировщика
операционной системы. При калибровке на новой машине запускать с `--relaxed` и
брать половину измеренного значения.

Пороги выделений памяти заданы вплотную к измеренному и одинаковы для обеих
сборок: выделяет память лишь формирование записи, и ровно один раз. Порог 0.01
означает, что не допускается ни одного выделения на операцию — доля выше нуля
поймает даже одно выделение на сотню повторов.

## Что из этого следует

**Разбор записи даты не выделяет память вовсе.** Ни одно из полей, ни
обозначение временной зоны, ни доля секунды. Разбор поля даты — это чтение
нескольких цифр из уже готовой строки, и промежуточных объектов ему не нужно:
список групп совпадения размещается на стеке, а значения читаются прямо из
исходной записи по границам групп. Показатель не зависит от окружения и ловит
появление любого выделения в горячем пути.

**Формирование записи выделяет память ровно один раз** — на возвращаемый объект
строки, что неустранимо по подписи метода: запись даты длиннее порога
размещения строки внутри самого объекта. Промежуточных строк в пути
формирования нет, и порог 1 ловит их появление.

**Календарная арифметика не выделяет память вовсе** — ни разложение даты на
поля, ни границы календарных отрезков, ни смещение на месяцы.

**Разбор записи вдвое дешевле её формирования по стоимости памяти и вдвое
дороже по времени** (719 тыс. операций в секунду против 1.48 млн). Разница
теперь целиком во времени: разбор проходит запись посимвольно и опознаёт
границы полей, а формирование лишь выкладывает готовые числа. В `Release`
разбор оказывается уже быстрее формирования (6.21 млн против 3.77 млн):
формирование упирается в работу с объектом строки, от режима сборки не
зависящую, а разбор без выделений памяти встраивается компилятором целиком.

**Перевод записи из формата в формат немногим дороже разбора** (428 тыс. против
716 тыс. операций в секунду на том же конечном формате): в его стоимости
доминирует разбор исходной записи, а формирование конечной добавляет к нему
меньше половины.

**Перевод обозначения зоны со смещением вдвое дороже простого сопоставления**
(1.9 млн против 4.76 млн операций в секунду): сопоставление — это поиск по
ассоциативному массиву, а перевод дополнительно разбирает числовое смещение.

**Получение штампа времени практически бесплатно** (39 млн операций в секунду,
25 наносекунд): на macOS и BSD обращение ко времени обслуживается через
страницу общего доступа ядра и системным вызовом не является. Показатель ловит
регрессию, при которой оно станет им — переход в ядро дороже на порядок.

**Оптимизация даёт от 2.5 до 18 раз.** Меньше всего выигрывают операции над
объектом строки: формирование записи даты — в 2.5 раза, обозначение временной
зоны — в 2.6. Больше всего выигрывают операции, целиком встраиваемые
компилятором: сопоставление обозначения зоны в 18 раз, разбор обозначения
размерности в 13, аббревиатура размерности в 13. Разбор записи даты, прежде
упиравшийся в распределитель памяти, теперь выигрывает 8.6 раза.

## Что дал отказ от динамической памяти в разборе

Разбор переменной формата размещал список групп совпадения в динамической
памяти — по выделению на каждую переменную записи. Список этот по устройству
короток, не больше восьми групп, и размещается на стеке целиком. Вместе с ним
ушёл промежуточный список строк из пути сопоставления обозначения зоны.

| Сценарий | Было | Стало | Прирост |
|---|---:|---:|---:|
| `parse/iso8601` | 383 449 | 718 751 | +87% |
| `parse/clf` | 475 075 | 716 103 | +51% |
| `parse/compact` | 544 836 | 917 605 | +68% |
| `format/strip` | 318 530 | 427 771 | +34% |
| `zones/match` | 2 096 410 | 4 763 946 | ×2.3 |
| `zones/offset` | 1 071 808 | 1 903 805 | +78% |
| `duration/parse` | 4 053 123 | 6 563 187 | +62% |

В `Release` прирост того же порядка: разбор ISO 8601 с 3.15 до 6.21 млн
разборов в секунду, сопоставление обозначения зоны с 43.4 до 85.8 млн.

## Что осталось неоптимизированным

**Поиск временной зоны копирует обозначение под ключ.** Реестр зон — это
`unordered_map <string, int32_t>`, и поиск в нём по представлению строки
требует построения ключа. Выделения памяти это сейчас не стоит — обозначения
зон коротки и размещаются внутри самого объекта строки, — но копирование
остаётся, и длинное название зоны выделение вернёт. Гетерогенный поиск
(`std::unordered_map` с прозрачным компаратором либо собственная таблица по
представлению) убирает и копирование, и эту зависимость от длины обозначения.
