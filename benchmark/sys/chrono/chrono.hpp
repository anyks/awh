/**
 * @file chrono.hpp
 * @date 2026-07-29
 *
 * @license{LicenseRef-AWH-1.0}
 *
 * @author Yuriy Lobarev
 *
 * @telegram{forman}
 * @phone{+7 (910) 983-95-90}
 *
 * @email forman@anyks.com
 * @site https://anyks.com
 *
 * @brief Заголовочный файл бенчмарков модуля работы с датой и временем — общее окружение
 *        сценариев, эталонная дата и средства проведения замера
 *
 * @copyright Copyright © 2026
 *
 */

#ifndef __AWH_BENCHMARK_SYS_CHRONO__
#define __AWH_BENCHMARK_SYS_CHRONO__

/**
 * Стандартные заголовочные файлы
 */
#include <chrono>
#include <string>
#include <cstddef>
#include <cstdint>

/**
 * Подключаем заголовочные файлы проекта
 */
#include "../../main.hpp"
#include "../../../include/sys/log.hpp"
#include "../../../include/sys/chrono.hpp"

/**
 * @brief основное пространство имён
 *
 */
namespace awh {
	/**
	 * @brief Пространство имён бенчмарков
	 *
	 */
	namespace benchmark {
		/**
		 * @brief Пространство имён бенчмарков модуля работы с датой и временем
		 *
		 * @details Модуль разбирает и формирует записи дат в произвольном формате,
		 *          выполняет календарную арифметику и переводит обозначения временных
		 *          зон в смещения. Обращения к нему идут из журналирования, из разбора
		 *          заголовков протоколов и из всякого места, где дата приходит извне
		 *          текстом, поэтому его стоимость входит в стоимость этих операций,
		 *          но отдельно там не видна.
		 *
		 *          Показатель у сценариев двойной: операций в секунду и выделений
		 *          памяти на операцию. Второй важнее первого - календарная арифметика
		 *          работает с целыми числами и выделять память не обязана вовсе, а
		 *          разбор записи даты обязан ровно настолько, насколько этого требует
		 *          подпись метода
		 *
		 */
		namespace chrono {
			/**
			 * @brief Эталонная дата сценариев в миллисекундах с эпохи
			 *
			 * @details Соответствует Sun, 06 Apr 2025 15:37:01.520 UTC+3 - та же дата,
			 *          на которой построены юнит-тесты модуля. Все её составляющие
			 *          значащие: ненулевые миллисекунды, середина года, середина
			 *          месяца и вторая половина суток. Дата вида «полночь первого
			 *          января» сокращала бы работу календарной арифметики и давала бы
			 *          завышенные показатели
			 *
			 */
			static constexpr uint64_t CHRONO_DATE = 1743943021520;
			/**
			 * @brief Количество повторений сценариев разбора и формирования записи даты
			 *
			 * @details Одна операция обходится в сотни наносекунд, и на меньшем
			 *          количестве повторений замер измерял бы разрешение часов
			 *
			 */
			static constexpr size_t CHRONO_ROUNDS = 100000;
			/**
			 * @brief Количество повторений сценариев календарной арифметики
			 *
			 * @details Арифметика на порядок дешевле работы с записью даты, поэтому
			 *          повторений ей нужно больше: на общем количестве показатель
			 *          определялся бы накладными расходами цикла замера
			 *
			 */
			static constexpr size_t CALENDAR_ROUNDS = 500000;
			/**
			 * @brief Количество повторений прогрева сценариев
			 *
			 * @details Первые обращения к модулю выходят на установившийся режим:
			 *          распределитель памяти выходит на рабочий объём, а внутренние
			 *          накопители объекта заполняются
			 *
			 */
			static constexpr size_t CHRONO_WARMUP = 5000;

			/**
			 * @brief Структура итогов прогона сценария
			 *
			 */
			typedef struct Outcome {
				// Количество выполненных операций
				size_t operations;
				// Затраченное время в секундах
				double seconds;
				// Количество выполненных выделений памяти
				size_t allocations;
				// Суммарный объём выделенной памяти в октетах
				size_t allocated;
				/**
				 * @brief Конструктор
				 *
				 */
				explicit Outcome() noexcept :
				 operations(0), seconds(0.0), allocations(0), allocated(0) {}
			} outcome_t;
			/**
			 * @brief Функция формирования сведений о прогоне сценария
			 *
			 * @param output итоги прогона сценария
			 * @return       сведения о прогоне для вывода
			 *
			 */
			std::string details(const outcome_t & output) noexcept;
			/**
			 * @brief Функция извлечения количества операций в секунду
			 *
			 * @param output итоги прогона сценария
			 * @return       количество операций в секунду
			 *
			 */
			double perSecond(const outcome_t & output) noexcept;
			/**
			 * @brief Функция извлечения количества выделений памяти на одну операцию
			 *
			 * @param output итоги прогона сценария
			 * @return       количество выделений памяти на одну операцию
			 *
			 */
			double perOperation(const outcome_t & output) noexcept;
			/**
			 * @brief Функция получения контрольной суммы прогонов
			 *
			 * @note Накопление результатов операций в контрольную сумму обязательно:
			 *       без потребителя результата компилятор вправе устранить измеряемое
			 *       вычисление целиком, и замерялась бы стоимость пустого цикла
			 *
			 * @return ссылка на контрольную сумму прогонов
			 *
			 */
			volatile uint64_t & checksum() noexcept;
			/**
			 * @brief Функция получения объекта фреймворка сценариев
			 *
			 * @return объект фреймворка
			 *
			 */
			const awh::fmk_t * framework() noexcept;
			/**
			 * @brief Функция получения объекта логирования сценариев
			 *
			 * @note Логирование отключено: вывод в поток стоит дороже самой измеряемой
			 *       операции, а разбор заведомо неполной записи даты модуль сопровождает
			 *       сообщением
			 *
			 * @return объект логирования
			 *
			 */
			const awh::log_t * logger() noexcept;
			/**
			 * @brief Шаблон типа измеряемого сценария
			 *
			 * @tparam CALLBACK тип функции выполнения одной операции
			 *
			 */
			template <typename CALLBACK>
			/**
			 * @brief Функция проведения замера сценария
			 *
			 * @details Замеру предшествует прогрев: первые операции выводят внутренние
			 *          буферы модуля и распределитель памяти на рабочий объём, и их
			 *          выделения памяти к установившемуся режиму отношения не имеют
			 *
			 * @note Функции выполнения операции передаётся номер круга, и операции над
			 *       датой обязаны его учитывать: методы модуля не имеют побочных
			 *       действий, и вызов с одними и теми же доводами оптимизирующий
			 *       компилятор вправе вынести из цикла целиком. Измерялась бы тогда
			 *       стоимость пустого цикла, причём только в оптимизированной сборке,
			 *       где показатель и важен. Смещение даты на номер круга задаёт
			 *       довод, меняющийся от круга к кругу, а миллисекунды календарного
			 *       положения даты не меняют, поэтому объём работы остаётся прежним
			 *
			 * @note Одного лишь меняющегося довода мало: результат каждого круга
			 *       обязан накапливаться внутри самого круга. Присваивание результата
			 *       переменной, читаемой только после цикла, компилятор устраняет как
			 *       запись, которую следующий круг всё равно перезапишет, и от всего
			 *       прогона остаётся последний круг. Накопление же связывает круги
			 *       зависимостью по данным, устранить которую нельзя
			 *
			 * @param rounds   количество выполняемых операций
			 * @param callback функция выполнения одной операции
			 * @return         итоги прогона сценария
			 *
			 */
			inline outcome_t measure(const size_t rounds, CALLBACK callback) noexcept {
				/**
				 * Выполняем прогрев измеряемой операции
				 */
				for(size_t i = 0; i < CHRONO_WARMUP; i++)
					// Выполняем очередную операцию прогрева
					callback(i);
				// Включаем учёт выделений памяти
				awh::benchmark::counting(true);
				// Запоминаем момент начала измерения
				const auto start = std::chrono::steady_clock::now();
				/**
				 * Выполняем измеряемую операцию требуемое количество раз
				 */
				for(size_t i = 0; i < rounds; i++)
					// Выполняем очередную измеряемую операцию
					callback(i);
				// Запоминаем момент окончания измерения
				const auto finish = std::chrono::steady_clock::now();
				// Отключаем учёт выделений памяти
				awh::benchmark::counting(false);
				// Итоги прогона сценария
				outcome_t result;
				// Устанавливаем количество выполненных операций
				result.operations = rounds;
				// Устанавливаем затраченное время
				result.seconds = std::chrono::duration <double> (finish - start).count();
				// Получаем статистику выделений памяти
				awh::benchmark::allocations(result.allocations, result.allocated);
				// Выводим итоги прогона сценария
				return result;
			}
			/**
			 * @brief Макрос объявления пары сценариев скорости и выделений памяти
			 *
			 * @details Каждая измеряемая операция даёт два показателя: сколько операций
			 *          в секунду и сколько выделений памяти на операцию. Оба снимаются
			 *          с одного и того же прогона, поэтому итоги прогона кэшируются, а
			 *          объявления сценариев выписываются макросом: иначе один и тот же
			 *          текст повторялся бы для каждой измеряемой операции набора
			 *
			 * @param SUFFIX  окончание имён объявляемых функций
			 * @param OUTCOME функция получения итогов прогона сценария
			 *
			 */
			#define AWH_CHRONO_SCENARIO(SUFFIX, OUTCOME)                            \
				static awh::benchmark::result_t speed ## SUFFIX() noexcept {        \
					awh::benchmark::result_t result;                                \
					const outcome_t & outcome = OUTCOME();                          \
					result.value = perSecond(outcome);                              \
					result.details = details(outcome);                              \
					return result;                                                  \
				}                                                                   \
				static awh::benchmark::result_t memory ## SUFFIX() noexcept {       \
					awh::benchmark::result_t result;                                \
					const outcome_t & outcome = OUTCOME();                          \
					result.value = perOperation(outcome);                           \
					result.details = details(outcome);                              \
					return result;                                                  \
				}
		};
	};
};

#endif // __AWH_BENCHMARK_SYS_CHRONO__
