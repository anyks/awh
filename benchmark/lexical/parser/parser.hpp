/**
 * @file: parser.hpp
 * @date: 2026-07-26
 * @license: LicenseRef-AWH-1.0
 *
 * @telegram: @forman
 * @author: Yuriy Lobarev
 * @phone: +7 (910) 983-95-90
 * @email: forman@anyks.com
 * @site: https://anyks.com
 *
 * @brief Заголовочный файл бенчмарков модуля разбора чисел — общее окружение сценариев,
 *        эталонные числовые записи всех путей разбора и средства проведения замера
 *
 * @copyright: Copyright © 2026
 *
 */

#ifndef __AWH_BENCHMARK_LEXICAL__
#define __AWH_BENCHMARK_LEXICAL__

/**
 * Стандартные заголовочные файлы
 */
#include <atomic>
#include <chrono>
#include <string>
#include <cstddef>
#include <cstdint>

/**
 * Подключаем заголовочные файлы проекта
 */
#include "../../main.hpp"
#include "../../../include/lexical/lexical.hpp"

/**
 * @brief основное пространство имён
 *
 */
namespace awh {
	/**
	 * @brief Пространство имён бенчмарков
	 *
	 */
	namespace benchmark {
		/**
		 * @brief Пространство имён бенчмарков модуля разбора чисел
		 *
		 * @note Пространство имён названо по подмодулю разбора, а не по модулю:
		 *       имя lexical внутри пространства имён бенчмарков перекрыло бы
		 *       пространство имён самого модуля awh::lexical
		 *
		 */
		namespace parser {
			/**
			 * @brief Структура итогов прогона сценария
			 *
			 */
			typedef struct Outcome {
				// Количество выполненных операций
				size_t operations;
				// Затраченное время в секундах
				double seconds;
				// Количество выполненных выделений памяти
				size_t allocations;
				// Суммарный объём выделенной памяти в октетах
				size_t allocated;
				/**
				 * @brief Конструктор
				 *
				 */
				explicit Outcome() noexcept :
				 operations(0), seconds(0.0), allocations(0), allocated(0) {}
			} outcome_t;
			/**
			 * @brief Функция формирования сведений о прогоне сценария
			 *
			 * @param output итоги прогона сценария
			 * @return       сведения о прогоне для вывода
			 *
			 */
			std::string details(const outcome_t & output) noexcept;
			/**
			 * @brief Функция формирования сведений о прогоне сценария сравнения
			 *
			 * @param module итоги прогона средствами модуля
			 * @param native итоги прогона средствами стандартной библиотеки
			 * @return       сведения о прогоне для вывода
			 *
			 */
			std::string comparison(const outcome_t & module, const outcome_t & native) noexcept;
			/**
			 * @brief Функция извлечения количества операций в секунду
			 *
			 * @param output итоги прогона сценария
			 * @return       количество операций в секунду
			 *
			 */
			double perSecond(const outcome_t & output) noexcept;
			/**
			 * @brief Функция извлечения количества выделений памяти на одну операцию
			 *
			 * @param output итоги прогона сценария
			 * @return       количество выделений памяти на одну операцию
			 *
			 */
			double perOperation(const outcome_t & output) noexcept;
			/**
			 * @brief Функция извлечения кратности превосходства над стандартной библиотекой
			 *
			 * @param module итоги прогона средствами модуля
			 * @param native итоги прогона средствами стандартной библиотеки
			 * @return       кратность превосходства
			 *
			 */
			double speedup(const outcome_t & module, const outcome_t & native) noexcept;
			/**
			 * @brief Функция получения контрольной суммы прогонов
			 *
			 * @note Накопление результатов операций в контрольную сумму обязательно:
			 *       без потребителя результата компилятор вправе устранить измеряемый
			 *       разбор целиком, и замерялась бы стоимость пустого цикла
			 *
			 * @return ссылка на контрольную сумму прогонов
			 *
			 */
			volatile uint64_t & checksum() noexcept;
			/**
			 * @brief Функция запрета выноса измеряемого разбора за пределы цикла
			 *
			 * @details Все сценарии разбирают одну и ту же запись, а разбор является
			 *          чистым вычислением и встраивается компилятором целиком, поэтому
			 *          оптимизирующая сборка вправе выполнить его однократно и вынести
			 *          за пределы цикла - замерялась бы стоимость пустого цикла.
			 *          Пустая ассемблерная вставка с пометкой обращения к памяти
			 *          лишает компилятор такого права, не добавляя в код ни одной
			 *          инструкции: содержимое разбираемой записи после неё считается
			 *          возможно изменившимся, и разбор обязан выполняться заново
			 *
			 * @note Без этой меры оптимизирующая сборка показывала на сборке числа из
			 *       мантиссы и показателя степени 1.75 млрд операций в секунду, то есть
			 *       0.57 нс на операцию - меньше, чем стоит одно умножение с обращением
			 *       к таблице степеней
			 *
			 * @param value указатель на результат измеряемой операции
			 *
			 */
			inline void barrier(void * value) noexcept {
				/**
				 * Для компиляторов с поддержкой ассемблерных вставок используем их
				 */
				#if defined(__clang__) || defined(__GNUC__)
					// Выполняем пустую вставку с пометкой обращения к памяти
					__asm__ __volatile__("" : "+r,m" (value) : : "memory");
				/**
				 * Для остальных компиляторов используем барьер упорядочивания обращений
				 */
				#else
					// Выполняем сохранение результата измеряемой операции
					volatile void * sink = value;
					// Выполняем барьер упорядочивания обращений к памяти
					std::atomic_signal_fence(std::memory_order_seq_cst);
					// Подавляем предупреждение о неиспользуемом значении
					(void) sink;
				#endif
			}
			/**
			 * @brief Шаблон типа измеряемого сценария
			 *
			 * @tparam CALLBACK тип функции выполнения одной операции
			 *
			 */
			template <typename CALLBACK>
			/**
			 * @brief Функция проведения замера сценария
			 *
			 * @details Замеру предшествует прогрев: первые операции выводят кэш
			 *          процессора на рабочий режим по таблицам степеней модуля, и
			 *          их стоимость к установившемуся режиму отношения не имеет
			 *
			 * @param rounds   количество выполняемых операций
			 * @param callback функция выполнения одной операции
			 * @return         итоги прогона сценария
			 *
			 */
			inline outcome_t measure(const size_t rounds, CALLBACK callback) noexcept {
				/**
				 * Выполняем прогрев измеряемой операции
				 */
				for(size_t i = 0; i < 64; i++)
					// Выполняем очередную операцию прогрева
					callback();
				// Включаем учёт выделений памяти
				awh::benchmark::counting(true);
				// Запоминаем момент начала измерения
				const auto start = std::chrono::steady_clock::now();
				/**
				 * Выполняем измеряемую операцию требуемое количество раз
				 */
				for(size_t i = 0; i < rounds; i++)
					// Выполняем очередную измеряемую операцию
					callback();
				// Запоминаем момент окончания измерения
				const auto finish = std::chrono::steady_clock::now();
				// Отключаем учёт выделений памяти
				awh::benchmark::counting(false);
				// Итоги прогона сценария
				outcome_t result;
				// Устанавливаем количество выполненных операций
				result.operations = rounds;
				// Устанавливаем затраченное время
				result.seconds = std::chrono::duration <double> (finish - start).count();
				// Получаем статистику выделений памяти
				awh::benchmark::allocations(result.allocations, result.allocated);
				// Выводим итоги прогона сценария
				return result;
			}
			/**
			 * @brief Функция получения эталонной десятичной записи 32-битного целого числа
			 *
			 * @details Все эталонные записи возвращаются объектом строки и разбираются
			 *          по указателям на её данные: конструирование записи внутри
			 *          измеряемого цикла выделяло бы память и вносило бы в замер
			 *          стоимость работы со строкой вместо стоимости разбора
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & integer32() noexcept;
			/**
			 * @brief Функция получения эталонной десятичной записи 64-битного целого числа
			 *
			 * @details Запись содержит девятнадцать цифр - на одну меньше предельного
			 *          для десятичной записи количества, поэтому разбор выполняется
			 *          в один проход: переполнение при таком количестве цифр невозможно
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & integer64() noexcept;
			/**
			 * @brief Функция получения эталонной десятичной записи с контролем переполнения
			 *
			 * @details Запись содержит предельные для десятичной записи двадцать цифр:
			 *          при таком количестве цифр накопленное значение могло переполниться,
			 *          и разбор выполняется повторно с точной проверкой на каждой цифре
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & integerChecked() noexcept;
			/**
			 * @brief Функция получения эталонной шестнадцатеричной записи целого числа
			 *
			 * @details Запись содержит предельные для основания шестнадцать цифр,
			 *          поэтому разбор выполняется повторно с контролем переполнения
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & integerHex() noexcept;
			/**
			 * @brief Функция получения эталонной записи целого числа по основанию 36
			 *
			 * @details Запись содержит предельные для основания тринадцать цифр,
			 *          поэтому разбор выполняется повторно с контролем переполнения
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & integerBase36() noexcept;
			/**
			 * @brief Функция получения эталонной записи целого числа с переполнением разрядности
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & integerOverflow() noexcept;
			/**
			 * @brief Функция получения эталонной записи числа быстрого пути разбора
			 *
			 * @details Мантисса укладывается в точное представление, а показатель
			 *          степени не выходит за пределы точных степеней десяти, поэтому
			 *          разбор завершается быстрым путём Клингера одним умножением
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & realFast() noexcept;
			/**
			 * @brief Функция получения эталонной записи числа научной записи
			 *
			 * @details Показатель степени выходит за пределы точных степеней десяти,
			 *          поэтому быстрый путь неприменим и разбор идёт приближением
			 *          Эйзеля-Лемира по таблице степеней пятёрки
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & realScientific() noexcept;
			/**
			 * @brief Функция получения эталонной записи числа точного пути разбора
			 *
			 * @details Приближения по таблице степеней пятёрки для этой записи
			 *          недостаточно, чтобы округлить результат однозначно, поэтому
			 *          разбор уходит на точное сравнение длинной арифметикой. Запись
			 *          подобрана перебором: такие значения среди случайных встречаются
			 *          примерно в одном случае из тысячи, и наугад точный путь не берётся
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & realExact() noexcept;
			/**
			 * @brief Функция получения эталонной записи числа с усечённой мантиссой
			 *
			 * @details Количество значащих цифр превышает разрядность мантиссы, поэтому
			 *          разбор набирает первые девятнадцать значащих цифр, а остаток
			 *          записи учитывает только показателем степени. Стоимость сценария -
			 *          это стоимость просмотра длинной записи, а не длинной арифметики
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & realLong() noexcept;
			/**
			 * @brief Функция получения эталонной записи числа формата RFC 8259
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & realJson() noexcept;
			/**
			 * @brief Функция получения эталонной записи бесконечности
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::string & realInfinity() noexcept;
			/**
			 * @brief Функция получения эталонной записи числа в двухбайтовой кодировке
			 *
			 * @details Запись повторяет содержимое научной записи: сравнение однобайтового
			 *          и двухбайтового разбора имеет смысл только на одинаковой работе
			 *
			 * @return эталонная запись числа
			 *
			 */
			const std::u16string & realUtf16() noexcept;
		};
	};
};

#endif // __AWH_BENCHMARK_LEXICAL__
