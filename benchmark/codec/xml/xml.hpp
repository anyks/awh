/**
 * @file xml.hpp
 * @date 2026-08-01
 *
 * @license{LicenseRef-AWH-1.0}
 *
 * @author Yuriy Lobarev
 *
 * @telegram{forman}
 * @phone{+7 (910) 983-95-90}
 *
 * @email forman@anyks.com
 * @site https://anyks.com
 *
 * @brief Заголовочный файл бенчмарков контейнера XML — общее окружение сценариев,
 *        эталонные тексты разметки всех путей разбора и средства проведения замера
 *
 * @copyright Copyright © 2026
 *
 */

#ifndef __AWH_BENCHMARK_CODEC_XML__
#define __AWH_BENCHMARK_CODEC_XML__

/**
 * Стандартные заголовочные файлы
 */
#include <atomic>
#include <chrono>
#include <string>
#include <cstddef>
#include <cstdint>

/**
 * Подключаем заголовочные файлы проекта
 */
#include "../../main.hpp"
#include "../../../include/codec/xml/xml.hpp"

/**
 * @brief основное пространство имён
 *
 */
namespace awh {
	/**
	 * @brief Пространство имён бенчмарков
	 *
	 */
	namespace benchmark {
		/**
		 * @brief Пространство имён бенчмарков контейнера XML
		 *
		 * @note Пространство имён названо по контейнеру, а не по модулю: имя codec
		 *       внутри пространства имён бенчмарков перекрыло бы пространство имён
		 *       самого модуля awh::codec
		 *
		 */
		namespace markup {
			/**
			 * @brief Структура итогов прогона сценария
			 *
			 */
			typedef struct Outcome {
				// Количество разобранных октетов исходного текста
				size_t bytes;
				// Количество выполненных операций
				size_t operations;
				// Затраченное время в секундах
				double seconds;
				// Количество выполненных выделений памяти
				size_t allocations;
				// Суммарный объём выделенной памяти в октетах
				size_t allocated;
				/**
				 * Накопленный итог измеряемой работы по всем кругам
				 *
				 * @details Величина эта - ЕДИНСТВЕННОЕ свидетельство того, что работа
				 * состоялась. Прежде она уходила в контрольную сумму и наружу не
				 * выдавалась вовсе, и замер, где разбор ОТКАЗАЛ на первом же знаке,
				 * отчитывался мерой тем большей, чем раньше наступил отказ: числитель
				 * берётся у длины эталона, помноженной на круги, а знаменатель падает
				 * до нуля. Сценарий вознаграждал поломку и молчал о ней
				 *
				 * @note Найдено по правилу Генри 04.09.2026: замер, берущий числитель у
				 *       эталона, ломается не при написании, а тогда, когда поведение
				 *       модуля разойдётся с эталоном, - и о расхождении не доносит
				 */
				uint64_t accumulator;
				/**
				 * @brief Конструктор
				 *
				 */
				explicit Outcome() noexcept :
				 bytes(0), operations(0), seconds(0.0), allocations(0), allocated(0), accumulator(0) {}
			} outcome_t;
			/**
			 * @brief Функция формирования сведений о прогоне сценария
			 *
			 * @param output итоги прогона сценария
			 * @return       сведения о прогоне для вывода
			 *
			 */
			std::string details(const outcome_t & output) noexcept;
			/**
			 * @brief Функция извлечения пропускной способности разбора
			 *
			 * @param output итоги прогона сценария
			 * @return       пропускная способность в мегабайтах в секунду
			 *
			 */
			double perSecond(const outcome_t & output) noexcept;
			/**
			 * @brief Функция извлечения количества выделений памяти на один документ
			 *
			 * @param output итоги прогона сценария
			 * @return       количество выделений памяти на один документ
			 *
			 */
			double perDocument(const outcome_t & output) noexcept;
			/**
			 * @brief Функция проверки работоспособности учёта выделений памяти
			 *
			 * @details Показатель «не более» при молчащем счётчике отчитывается «уложился»
			 *          всегда: нулевой расход меньше всякого порога. Проверка эта отделяет
			 *          исправный модуль от неработающего учёта
			 *
			 * @warning Сторожит поломку, уже случавшуюся: у MinGW стандартная библиотека
			 *          подключалась отдельной библиотекой, и выделения, выполненные внутри
			 *          неё, замене оператора в самом бинарном приложении не были видны.
			 *          Учёт недосчитывал втрое, а на записи отчитывался нулём - и все
			 *          сценарии выделений отчитывались «уложился»
			 *
			 * @param output итоги прогона сценария
			 * @param result заполняемый результат измерения
			 * @return       признак работоспособности учёта
			 *
			 */
			bool counted(const outcome_t & output, awh::benchmark::result_t & result) noexcept;
			/**
			 * @brief Функция извлечения задержки обработки одного документа
			 *
			 * @details Показатель этот назначен мелким документам, где решает не
			 *          пропускная способность, а постоянные издержки на запуск: в
			 *          обиходе договора UPnP документы идут по два-десять килобайт,
			 *          и заметная их доля уходит на заведение хранилищ разбора, а не
			 *          на сам разбор. Мегабайты в секунду такую надбавку скрывают -
			 *          на мелком документе она попросту растворяется в его размере
			 *
			 * @param output итоги прогона сценария
			 * @return       задержка обработки одного документа в микросекундах
			 *
			 */
			double perLatency(const outcome_t & output) noexcept;
			/**
			 * @brief Функция получения контрольной суммы прогонов
			 *
			 * @note Накопление итогов разбора в контрольную сумму обязательно: без
			 *       потребителя результата компилятор вправе устранить измеряемый
			 *       разбор целиком, и замерялась бы стоимость пустого цикла
			 *
			 * @return ссылка на контрольную сумму прогонов
			 *
			 */
			volatile uint64_t & checksum() noexcept;
			/**
			 * @brief Шаблон типа измеряемого сценария
			 *
			 * @tparam CALLBACK тип функции разбора одного документа
			 *
			 */
			template <typename CALLBACK>
			/**
			 * @brief Функция проведения замера сценария
			 *
			 * @details Замеру предшествует прогрев: первый разбор выводит на рабочий
			 *          режим кэш процессора и распределитель памяти, и его стоимость
			 *          к установившемуся режиму отношения не имеет
			 *
			 * @param bytes    размер разбираемого текста разметки в октетах
			 * @param rounds   количество разбираемых документов
			 * @param callback функция разбора одного документа
			 * @return         итоги прогона сценария
			 *
			 */
			inline outcome_t measure(const size_t bytes, const size_t rounds, CALLBACK callback) noexcept {
				// Выполняем прогрев измеряемого разбора
				checksum() += static_cast <uint64_t> (callback());
				// Включаем учёт выделений памяти
				awh::benchmark::counting(true);
				// Запоминаем момент начала измерения
				const auto start = std::chrono::steady_clock::now();
				// Накопитель итогов разбора
				uint64_t accumulator = 0;
				/**
				 * Выполняем разбор требуемого количества документов
				 */
				for(size_t i = 0; i < rounds; i++)
					// Выполняем разбор очередного документа
					accumulator += static_cast <uint64_t> (callback());
				// Запоминаем момент окончания измерения
				const auto finish = std::chrono::steady_clock::now();
				// Отключаем учёт выделений памяти
				awh::benchmark::counting(false);
				// Накапливаем контрольную сумму прогона
				checksum() += accumulator;
				// Итоги прогона сценария
				outcome_t result;
				// Устанавливаем количество разобранных октетов
				result.bytes = (bytes * rounds);
				// Устанавливаем количество выполненных операций
				result.operations = rounds;
				// Устанавливаем затраченное время
				result.seconds = std::chrono::duration <double> (finish - start).count();
				// Устанавливаем накопленный итог измеряемой работы
				result.accumulator = accumulator;
				// Получаем статистику выделений памяти
				awh::benchmark::allocations(result.allocations, result.allocated);
				// Выводим итоги прогона сценария
				return result;
			}
			/**
			 * @brief Функция проверки того, что измеряемая работа состоялась
			 *
			 * @details Застава эта нужна ВСЯКОМУ сценарию, чей числитель берётся у длины
			 *          эталона: работа, отказавшая мгновенно, даёт нуль итога и время,
			 *          близкое к нулю, а мера от того лишь растёт. Отличить быструю
			 *          работу от быстрого отказа больше нечем
			 *
			 * @note Порог берётся ЧИСЛОМ КРУГОВ, а не нулём: здоровый круг даёт хотя бы
			 *       единицу итога, и `accumulator < rounds` означает, что отказал хотя
			 *       бы один круг. Нуль поймал бы лишь полный отказ всех кругов
			 *
			 * @param output итоги прогона сценария
			 * @return       признак того, что работа НЕ состоялась
			 *
			 */
			inline bool barren(const outcome_t & output) noexcept {
				// Выводим признак того, что итог работы меньше числа кругов
				return (output.accumulator < static_cast <uint64_t> (output.operations));
			}
			/**
			 * @brief Функция получения эталонного ответа по договору SOAP
			 *
			 * @details Все эталонные тексты возвращаются объектом строки и разбираются
			 *          по указателям на её данные: сборка текста внутри измеряемого
			 *          цикла выделяла бы память и вносила бы в замер стоимость работы
			 *          со строкой вместо стоимости разбора
			 *
			 * @note Ответ по договору SOAP - это тот самый случай, ради которого
			 *       контейнер и написан: мелкий документ с пространствами имён,
			 *       разбираемый по одному на каждое обращение к устройству
			 *
			 * @return эталонный текст разметки
			 *
			 */
			const std::string & soap() noexcept;
			/**
			 * @brief Функция получения эталонного описания устройства по договору UPnP
			 *
			 * @return эталонный текст разметки
			 *
			 */
			const std::string & device() noexcept;
			/**
			 * @brief Функция получения эталонного крупного документа
			 *
			 * @details Документ содержит узлы, атрибуты и содержимое в соотношении,
			 *          близком к настоящим выгрузкам, и показывает установившуюся
			 *          пропускную способность за пределами кэша процессора
			 *
			 * @return эталонный текст разметки
			 *
			 */
			const std::string & large() noexcept;
			/**
			 * @brief Функция получения эталонного документа с преобладанием атрибутов
			 *
			 * @return эталонный текст разметки
			 *
			 */
			const std::string & attributes() noexcept;
			/**
			 * @brief Функция получения эталонного документа с описанием типа
			 *
			 * @details Документ несёт внутреннее подмножество, объявляющее виды атрибутов и
			 * значения их по умолчанию. Путь этот в прочих сценариях не берётся ВОВСЕ: без
			 * описания типа отображение объявлений пусто, и ни приведение значений по
			 * объявленному виду, ни подстановка умолчаний не выполняются
			 *
			 * @return эталонный текст разметки
			 *
			 */
			const std::string & declared() noexcept;
			/**
			 * @brief Функция получения эталонного документа с преобладанием содержимого
			 *
			 * @return эталонный текст разметки
			 *
			 */
			const std::string & content() noexcept;
			/**
			 * @brief Функция получения эталонного глубоко вложенного документа
			 *
			 * @details Глубокая вложенность нагружает стек открытых узлов и хранилище
			 *          имён: усечение хранилища при завершении узла ведётся по
			 *          положению имени, и потеря этого порядка обращает расход памяти
			 *          на разбор в возрастающий
			 *
			 * @return эталонный текст разметки
			 *
			 */
			const std::string & nested() noexcept;
		};
	};
};

#endif // __AWH_BENCHMARK_CODEC_XML__
