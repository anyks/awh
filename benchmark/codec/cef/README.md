# Бенчмарки контейнера CEF

Отчёт по набору сценариев `benchmark/codec/cef`. Измеряется модуль
`src/codec/cef` — потоковое чтение записей Common Event Format, укладка разбора
в дерево контейнера ABC и обратная сборка записи из дерева.

## Как запускать

```sh
# все сценарии подмодуля с проверкой порогов
./benchmarks/codec --filter=cef

# измерение без проверки порогов (калибровка на новой машине)
./benchmarks/codec --filter=cef --relaxed

# один сценарий, отбор ведётся по вхождению в название
./benchmarks/codec --filter="выделения"
```

Отклонение любого показателя от порога завершает прогон с ненулевым кодом,
поэтому набор пригоден как шаг сборки: регрессия останавливает конвейер.

**Собирать только сборкой `Release`.** Каталог `build` дерева отладочный, и
замер в нём даёт равномерное девятикратное замедление, какое при беглом чтении
выглядит просадкой отдельного пути.

## Что измеряется и почему именно это

| Сценарий | Что нагружает |
|---|---|
| `чтение записи IDS` | Запись живого журнала системы обнаружения вторжений: приставка syslog, метки имён, пустые значения, значение с пробелами и адреса сети — всё, чем запись CEF нагружает разбор, в одной строке около 600 октетов |
| `чтение записи заслона` | Запись заслона Check Point: двойная отмена знака равенства в именах различительных и значения с пробелами во множестве. Путь снятия отмены знаков нагружается сильнее прочих |
| `чтение записи auditd` | Самая тяжёлая из живых записей: свыше полутора сотен пар расширения, повторяющиеся ключи десятками, управляющая вставка внутри значения. Взята из образца прежнего модуля CEF без правок |
| `разбор потока записей` | Число разбираемых записей в секунду — тот самый EPS, каким мерилась работа прежнего модуля. Сборщик журналов принимает поток от многих устройств, и решает у него не пропускная способность в мегабайтах, а число событий, какое он успевает разобрать. Замер ведётся потоком из 128 записей подряд |
| `просадка от подачи кусками` | Отношение времени подачи записи кусками по одному октету ко времени подачи её целиком. Случай худший из мыслимых: обрыв приходится и на середину имени ключа, и на середину отменяющей последовательности |
| `выделения на чтение` | Количество выделений памяти на чтение одной записи. Показатель воспроизводим и от машины зависит слабо |
| `разбор записи IDS в дерево` | Укладка разбора в дерево контейнера ABC поверх чтения: к разбору добавляются заведение узлов дерева и розыск ключей |
| `разбор записи auditd в дерево` | То же на самой тяжёлой записи: видна стоимость роста отображения и обращения повторяющегося ключа в перечень |
| `задержка разбора короткой записи` | Время обработки записи без расширения вовсе — одна приставка и заголовок. На такой записи решает не пропускная способность, а постоянные издержки на заведение хранилищ разбора |
| `удорожание сличением со словарём` | Отношение времени разбора со сличением ключей со словарём ко времени разбора без него. Сличение разыскивает всякий ключ двоичным поиском и приводит значение к виду, словарём заданному |
| `оборот разбора со сборкой` | Полный оборот: разбор записи в дерево и сборка её обратно. Показатель назначен потребителю, правящему событие на лету |
| `выделения на разбор в дерево` | Количество выделений памяти на укладку одной записи в дерево |
| `сборка записи IDS` | Обход дерева и сборка записи с постановкой отмены знаков порознь по областям |
| `сборка записи заслона` | То же на записи заслона сети: видна стоимость обхода отображения и постановки отмены в значениях с чертой. Записью auditd сценарий этот мериться НЕ может: эталон её несёт в заголовке одиннадцать полей вместо семи, остаток уезжает в расширение ключом с чертами и пробелами, а такой ключ записи не поддаётся, и писатель отвечает отказом законно |
| `выделения на сборку` | Количество выделений памяти на сборку одной записи |

### Про потребление результата

Чтение содержимое **само не копирует**: события выдают представления во
внутреннее хранилище разбора. В сценариях чтения потребитель обязан
просуммировать итог, иначе компилятор вправе устранить измеряемый разбор
целиком и замерялась бы стоимость пустого цикла. Итоги всех прогонов
накапливаются в контрольную сумму `checksum()`.

### Про эталонные записи

Все эталонные записи собираются однократно до замера и возвращаются ссылкой на
объект строки: сборка их внутри измеряемого цикла выделяла бы память и вносила
бы в замер стоимость работы со строкой вместо стоимости разбора.

Записи взяты из живых журналов — образцов владельца и примеров описания
ArcSight, — а не придуманы: придуманная запись обходит стороной ровно те пути,
на которых разбор и спотыкается.

## Показатели

Сняты на **девяти машинах**, сборка `Release`, 04-06.09.2026. Всякий столбец — своя
машина, а не повторение одной: разница между ними и есть здесь самое ценное.

| Машина | Собиратель | Стандартная библиотека |
|---|---|---|
| **macOS** | Apple Silicon, clang | libc++ |
| **FreeBSD 14.1** | amd64, clang 18 | libc++ |
| **OpenBSD 7.9** | amd64, clang 19 | libc++ |
| **Debian 12** | amd64, **g++ 12.2**, glibc | **libstdc++** |
| **Alpine 3.24** | amd64, **g++ 15.2**, **musl** | **libstdc++** |
| **NetBSD 10** | amd64, g++ 10.5 | libstdc++ |
| **Solaris 11.4** | amd64, g++ 15.2 | libstdc++ |
| **OpenIndiana** | amd64, g++ 14.3 | libstdc++ |
| **DragonFly 6.4** | amd64, g++ 14 | libstdc++ |

На машинах о двух видах ядер взято лучшее из нескольких прогонов отдельными
процессами: середина ряда внутри одного прогона смещена там целиком.

| Сценарий | macOS | FreeBSD | OpenBSD | Debian | Порог |
|---|---|---|---|---|---|
| чтение записи IDS, МБ/с | 137 | 34.0 | 28.5 | 60.1 | 10.0 |
| чтение записи заслона, МБ/с | 132 | 33.1 | 27.8 | 58.7 | 10.0 |
| чтение записи auditd, МБ/с | 121 | 30.4 | 26.7 | 51.4 | 10.0 |
| разбор потока, записей/с | 45 031 | 21 372 | 24 839 | **12 530** | 4 000 |
| просадка от подачи кусками, раз | 2.8-3.2 | 2.74 | **8.71** | 2.56 | 20.0 |
| сборка записи IDS, МБ/с | 140-167 | 40.4 | 26.4 | 70.7 | 10.0 |
| сборка записи заслона, МБ/с | 139-159 | 40.4 | 27.4 | 72.2 | 10.0 |
| разбор записи IDS в дерево, МБ/с | 41.0 | 12.0 | 10.8 | 17.1 | 3.0 |
| разбор записи auditd в дерево, МБ/с | 36.0 | 9.7 | 9.2 | 13.7 | 2.5 |
| задержка короткой записи, мкс | 1.61 | 6.21 | 7.95 | 4.22 | 40.0 |
| оборот разбора со сборкой, мкс | 16.5 | 56.9 | 67.4 | 37.4 | 200.0 |
| удорожание сличением, раз | 1.01 | 1.01 | 1.01 | 1.02 | 3.0 |
| **выделения на чтение** | 15 | 15 | 15 | **22** | 400 |
| **выделения на сборку** | 3 | 3 | 3 | 3 | 200 |
| **выделения на разбор в дерево** | 70 | 70 | 70 | **80** | 800 |

Пять машин, добавленных 06.09.2026, все на `libstdc++` и все на amd64 одного
сервера, оттого и сведены отдельною таблицей: между собой они разнятся мало, а
вместе показывают то, чего первые четыре показать не могли, — что расход выделений
определяется стандартной библиотекой C++, а не библиотекой C и не системой.

| Сценарий | Alpine | NetBSD | Solaris | OpenIndiana | DragonFly | Порог |
|---|---|---|---|---|---|---|
| чтение записи IDS, МБ/с | 59.2 | 51.4 | 54.5 | 56.1 | 51.4 | 10.0 |
| чтение записи заслона, МБ/с | 59.0 | 51.9 | 46.8 | 55.6 | 51.2 | 10.0 |
| чтение записи auditd, МБ/с | 54.7 | 47.5 | 49.0 | 51.2 | 47.8 | 10.0 |
| разбор потока, записей/с | 12 923 | 12 596 | 12 693 | 12 834 | 12 509 | 4 000 |
| просадка от подачи кусками, раз | 4.62 | 3.88 | 6.14 | 6.49 | 7.53 | 20.0 |
| сборка записи IDS, МБ/с | 60.2 | **36.0** | 57.7 | 63.3 | 65.3 | 10.0 |
| сборка записи заслона, МБ/с | 61.5 | **39.2** | 68.7 | 64.3 | 65.9 | 10.0 |
| разбор записи IDS в дерево, МБ/с | 16.0 | 15.4 | 15.4 | 15.8 | 16.0 | 3.0 |
| разбор записи auditd в дерево, МБ/с | 12.5 | 12.7 | 12.9 | 12.9 | 12.7 | 2.5 |
| задержка короткой записи, мкс | 4.74 | 4.99 | 4.79 | 4.82 | 4.56 | 40.0 |
| оборот разбора со сборкой, мкс | 46.8 | 48.1 | 41.7 | 41.0 | 40.7 | 200.0 |
| удорожание сличением, раз | 1.03 | 1.03 | 1.03 | 1.04 | 1.03 | 3.0 |
| **выделения на чтение** | 22 | 22 | 22 | 22 | 22 | 400 |
| **выделения на сборку** | 3 | **119** | 3 | 3 | 3 | 200 |
| **выделения на разбор в дерево** | 80 | 80 | 80 | 80 | 80 | 800 |

На всех пяти машинах прошли все пятнадцать сценариев.

### Чему учат девять машин

**«Самой медленной машины» не существует.** OpenBSD медленнее прочих почти всюду —
чтение 28.5 против 60.1 у Debian, оборот 67 против 37 мкс, — но поток записей
разбирает ВДВОЕ быстрее Debian. Дно у пятнадцати сценариев легло на три машины из
четырёх, и ни одна не дала дна больше чем в половине. Оттого порог берётся по дну
КАЖДОГО сценария, а не по «самому медленному стенду»: экономия на одной машине
дала бы неверный порог у десятка сценариев.

**Расход выделений определяется стандартной библиотекой C++ — не машиной, не
системой и не библиотекой C.** Три машины на libc++ дали 15 / 3 / 70 число в число;
шесть машин на libstdc++ — 22 / 3 / 80, и тоже число в число, при том что среди них
и glibc, и **musl** (Alpine), и две системы Sun, и две BSD. Двух точек на это не
хватало: пока стенд с libstdc++ был один и стоял на glibc, признаки «библиотека C»
и «библиотека C++» были неразличимы, и вывод держался на догадке. Alpine на musl
их развёл: расход не сдвинулся ни на единицу. Сужение порога по libc++ давало бы
ложную тревогу на всяком стенде с libstdc++.

**Исключение из этого — NetBSD, и оно чужое.** Сборка записи стоит там **119**
выделений против 3 у остальных восьми. Считаются они не деревом и не писателем: их
делает сама `libc` NetBSD внутри обращения чисел в текст. Порог 200 выдержал с
запасом всего в 1.7 раза, и порог, выставленный по «дешёвым» машинам, дал бы там
ложную тревогу на исправном модуле. Оттого расход на сборку и мерится порогом
широким, а не втрое от наблюдаемого.

**Мера отношением от машины ЗАВИСИТ.** Просадка от подачи кусками легла по девяти
машинам в размах от 2.56 (Debian) до 8.71 (OpenBSD) — расхождение в три с половиной
раза при одном и том же коде.
Отношение сокращает машину лишь настолько, насколько обеим его долям машина
одинаково дорога: числитель тут заводит перекладывание недочитанного, знаменатель
почти не заводит, а раздатчик памяти OpenBSD дорог. Проверять такие меры надо
машиной с ИНЫМ раздатчиком, а не тремя похожими.

**Порог, выставленный ожиданием, отличить от замеренного по числу нельзя.** Порог
разбора потока стоял 20 000 и выглядел просторным, покуда FreeBSD не дала 21 480 —
запас в семь процентов при исправном модуле. Опущен до 7 000, а после Debian с её
12 530 — до 4 000. Правился он трижды за день, и всякий раз новой машиной. Пять
машин, добавленных после, дали 12 509…12 923 и порога не сдвинули: правило это
самим своим повторением и проверилось.

### Замер, вознаграждающий поломку

Числитель у большинства сценариев берётся длиной ЭТАЛОННОЙ записи, помноженной на
число кругов, а не объёмом сделанной работы. Откажи чтение или сборка - и сценарий
отчитается о работе, какой не было, тем БЫСТРЕЕ, чем раньше наступил отказ: знак
меры переворачивается, и поломка вознаграждается.

Случай этот не выдуман. Сценарий сборки записи auditd показывал 166.64 МБ/с, покуда
писатель ту запись собирал; после того как писатель начал отвергать ключи с пробелами
внутри (находка ворошителя, решение намеренное), сценарий стал отчитываться 3248 МБ/с,
собирая 45 октетов вместо 387 и отказывая всякий круг. Ни одна застава того не
заметила: сторож смотрел лишь на то, что кругов сделано не ноль.

Оттого всякий сценарий, меру у эталона берущий, поверяет пригодность ОДНИМ кругом до
замера: разбор состоялся, а где требуется - и обратная сборка не пуста. Негодность
объявляется исходом наравне с числом, с причиной в отчёте, а не отсутствием числа.

### Что показатели говорят

**Сличение со словарём ничего не стоит.** Отношение 1.02 — это разброс замера,
а не удорожание: розыск ключа ведётся двоичным поиском по неизменной таблице,
уложенной в постоянную память двоичного файла, и рядом со стоимостью самого
разбора он не виден вовсе.

**Укладка в дерево втрое дороже чтения** — 41 МБ/с против 137 МБ/с, и стоит она
55 выделений памяти сверх пятнадцати. Плата эта за узлы дерева контейнера ABC,
а не за разбор: потребителю, которому дерево не нужно, надлежит брать потоковое
чтение, а не событие, удерживаемое целиком.

**Сборка быстрее разбора** — 167 МБ/с против 137 МБ/с при трёх выделениях против
пятнадцати. Обход дерева проще разбора: границ отыскивать не надо, они уже
известны.

**Просадка от подачи кусками по одному октету — от 2.6 до 8.7 раза.** Это худший
из мыслимых случаев: обрыв приходится и на середину имени ключа, и на середину
отменяющей последовательности. Настоящая подача идёт кусками по полторы тысячи
октетов, где просадки нет вовсе. Разброс между машинами тут не шум: три машины
дали 2.56-2.93, а OpenBSD 8.71 — подача кусками заводит перекладывание
недочитанного, и дорогой раздатчик памяти входит в числитель сильнее, чем в
знаменатель.

### Про сличение с прежним модулем

Сличение проведено и вынесено отдельно — [COMPARISON.md](COMPARISON.md): одна и та
же запись, одна машина, один собиратель, с поверкой равенства работы до всяких
чисел. Кратко: укладка в дерево вдвое быстрее прежнего разбора (39.8 против 19.1
МБ/с), поток событий — в шесть с половиной раз (124 против 19.1), а строгое
сличение стоит около четырёх процентов там, где у прежнего модуля разница между
степенями сличения не измеряется вовсе.

Числа ЭТОГО набора сличать с прежним модулем нельзя: машины разные, сборки разные,
а прежний замер вёлся с очисткой контейнера на всякой записи. Доказательство
требует одинакового устройства у обеих сторон, и место ему только в отчёте
сличения.

## Про пороги

Пороги-отношения (`просадка от подачи кусками`, `удорожание сличением со
словарём`) от машины ЗАВИСЯТ, и первый из них — сильно: 2.56 у Debian против 8.71
у OpenBSD. Отношение сокращает машину лишь настолько, насколько обеим его долям
машина одинаково дорога, а этого никто не проверяет, ибо мера выглядит
самодостаточной.

Пороги пропускной способности взяты по дну КАЖДОГО сценария на четырёх машинах, а
не по «самой медленной машине»: таковой не существует — у всякого сценария она
своя. При каждом пороге записано, чем и когда он снят: число без меры, какою оно
получено, поломку меры не выдаёт, а поломка эта случается позже написания
сценария — тогда, когда поведение модуля разойдётся с эталоном.

## Чего в наборе нет

**Замера сличения с чужими разборщиками CEF.** Сличение требует одинакового
устройства у обеих сторон — одинаковых записей, одинакового числа проходов и
одинаковой сборки, — и место ему в отдельном отчёте `COMPARISON.md`, а не среди
сценариев собственного набора.

**Замера расхода памяти деревом.** Дерево держит содержимое записи целиком, и
расход его памяти кратен размеру записи — величина эта видна из сведений о
прогоне и отдельного сценария не требует.
