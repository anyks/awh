# Бенчмарки контейнера INI

Отчёт по набору сценариев `benchmark/codec/ini`. Измеряется модуль
`src/codec/ini` — потоковое чтение текста настроек, сборка дерева, поиск по
дереву и запись текста настроек.

## Как запускать

```sh
# все сценарии подмодуля с проверкой порогов
./benchmarks/codec --filter=ini

# измерение без проверки порогов (калибровка на новой машине)
./benchmarks/codec --filter=ini --relaxed

# один сценарий, отбор ведётся по вхождению в название
./benchmarks/codec --filter="выделения"
```

Отклонение любого показателя от порога завершает прогон с ненулевым кодом,
поэтому набор пригоден как шаг сборки: регрессия останавливает конвейер.

## Что измеряется и почему именно это

| Сценарий | Что нагружает |
|---|---|
| `чтение настроек службы` | Постоянные накладные расходы на файл: заведение хранилищ разбора, приведение кодировки, сброс состояния. Файл малый, около 500 октетов, — тот самый случай, ради которого контейнер и написан: файл настроек читается однажды при запуске приложения |
| `чтение настроек Git` | Наречие, нагружающее разбор сильнее прочих: подраздел в кавычках, примечание в конце строки, управляющие последовательности и свойство без значения — всё сразу |
| `чтение крупного файла` | Текст на 16 МиБ: разбор в установившемся режиме, где накладные расходы на файл пренебрежимы, а видна собственная скорость разбора |
| `чтение примечаний` | Текст, наполовину состоящий из примечаний. Файл настроек, писанный человеком, устроен именно так, и путь разбора примечаний нагружается наравне с разбором свойств |
| `выделения на чтение` | Количество выделений памяти на разбор одного файла. Показатель воспроизводим до единиц и от размера файла не зависит: чтение ведётся на переиспользуемых хранилищах |
| `просадка от подачи кусками` | Отношение времени подачи текста кусками по 1460 октетов ко времени подачи того же текста целиком |
| `задержка чтения настроек службы` | Время обработки одного малого файла в микросекундах. На таком файле решает не пропускная способность, а стоимость запуска разбора |
| `запись настроек службы` | Сборка исходящего текста: объявления разделов, свойства, примечания, запись чисел |
| `запись ограждаемых значений` | Значения, требующие кавычек либо управляющих последовательностей: пробельная обвязка, знак примечания, кавычка, знак конца строки |
| `сборка дерева настроек службы` | Сборка дерева поверх чтения: к разбору добавляются хранилище знаков и указатели поиска |
| `сборка крупного дерева` | То же на тексте в 16 МиБ: видна стоимость роста хранилищ |
| `сборка дерева со множеством разделов` | Около сотни тысяч разделов: стережёт устройство поиска разделов. Раскладка по свёртке имени держит стоимость заведения раздела постоянной, попарное сличение имён обратило бы сборку в квадратичную |
| `обратная запись дерева` | Запись собранного дерева обратно в текст с сохранением примечаний и порядка записей |
| `обход дерева со множеством разделов` | Обход собранного дерева по разделам и свойствам каждого из них. Стережёт выдачу перечня имён свойств: перечень собран указателями при перестроении, и обход раздела стоит числа его свойств. Перебор всех записей дерева ради каждого раздела обращал бы обход в квадратичный |
| `сборка дерева вызовами` | Заведение двадцати тысяч свойств вызовами установки значения — сборка дерева из кода, а не разбором текста. Стережёт приращение указателей поиска: запись встаёт в конец перечня, порядковые номера прочих записей не сдвигаются, и перестраивать указатели целиком незачем |
| `поиск значений по имени` | Обращения к значениям по имени раздела и имени свойства. Стережёт раскладку по свёртке имён: перебор записей дерева при каждом обращении уронил бы показатель на порядки |

### Про потребление результата

Чтение содержимое **само не копирует**: события выдают представления во
внутреннее хранилище разбора. В сценариях чтения потребитель обязан
просуммировать итог, иначе компилятор вправе устранить измеряемый разбор
целиком и замерялась бы стоимость пустого цикла. Итоги всех прогонов
накапливаются в контрольную сумму `checksum()`.

### Про эталонные тексты

Все эталонные тексты собираются однократно до замера и возвращаются ссылкой на
объект строки: сборка их внутри измеряемого цикла выделяла бы память и вносила
бы в замер стоимость работы со строкой вместо стоимости разбора.

## Показатели

Сняты на рабочей машине — Apple Silicon, сборка `Release`, 10.08.2026.

Разброс между прогонами на этой машине доходит до трети. Показатели ниже — из
одного прогона, и читать их следует как порядок величины, а не как точное число.

Столбец порогов здесь **устарел**: пороги переразмечены 21.08.2026 по двенадцати
стендам, действующие их значения — в разделе «Дно по стендам».

| Сценарий | Измерено | Порог |
|---|---|---|
| чтение настроек службы | 175.88 МБ/с | 25.00 |
| чтение настроек Git | 209.86 МБ/с | 25.00 |
| чтение крупного файла | 217.65 МБ/с | 16.00 |
| чтение примечаний | 207.19 МБ/с | 16.00 |
| выделения на чтение | 2.00 выд./файл | 6.00 |
| просадка от подачи кусками | 0.95 раз | 2.00 |
| задержка чтения настроек службы | 1.55 мкс/файл | 15.00 |
| запись настроек службы | 394.28 МБ/с | 30.00 |
| запись ограждаемых значений | 368.78 МБ/с | 50.00 |
| сборка дерева настроек службы | 119.85 МБ/с | 6.00 |
| сборка крупного дерева | 145.00 МБ/с | 8.00 |
| сборка дерева со множеством разделов | 65.94 МБ/с | 4.00 |
| обратная запись дерева | 484.46 МБ/с | 40.00 |
| обход дерева со множеством разделов | 141.01 МБ/с | 15.00 |
| поиск значений по имени | 3 928 951 обр./с | 500 000 |
| сборка дерева вызовами | 5 684 274 уст./с | 400 000 |

### Дно по стендам, 21.08.2026

Прежняя разметка держалась на пяти стендах; ныне пересмотрена по **двенадцати**, тремя
прогонами отдельными процессами, дно берётся по каждому сценарию в отдельности с
двукратным запасом.

| сценарий | дно | машина | порог |
|---|---|---|---|
| чтение настроек службы | 54.62 МБ/с | OpenBSD | 27.00 |
| чтение настроек Git | 48.08 МБ/с | OpenBSD | 24.00 |
| чтение крупного файла | 34.73 МБ/с | OpenBSD | 17.00 |
| чтение примечаний | 36.55 МБ/с | OpenBSD | 18.00 |
| выделения на чтение | 2.00 выд./файл | Alpine | 6.00 |
| просадка от подачи кусками | 0.99 раза | OpenIndiana | 1.90 |
| задержка чтения настроек службы | 7.33 мкс/файл | OpenBSD | 14.00 |
| запись настроек службы | 53.29 МБ/с | OpenBSD | 26.00 |
| запись ограждаемых значений | 81.86 МБ/с | OpenBSD | 40.00 |
| сборка дерева настроек службы | 12.66 МБ/с | OpenBSD | 6.30 |
| сборка крупного дерева | 17.30 МБ/с | OpenBSD | 8.60 |
| сборка дерева со множеством разделов | 8.44 МБ/с | OpenBSD | 4.20 |
| обратная запись дерева | 70.60 МБ/с | OpenBSD | 35.00 |
| обход дерева со множеством разделов | 20.66 МБ/с | OpenIndiana | 10.00 |
| поиск значений по имени | 879 523 обр./с | OpenBSD | 440 000 |
| сборка дерева вызовами | 803 219 уст./с | OpenBSD | 400 000 |
| снятие значения с дерева | 10.81 МБ/с | OpenBSD | 5.40 |
| снятие значения настроек службы | 19.05 МБ/с | OpenBSD | 9.50 |
| потоковая сборка значения | 90.35 МБ/с | OpenBSD | 45.00 |
| запись значения в текст | 65.32 МБ/с | OpenBSD | 32.00 |
| обход владеющего значения | 3173.58 МБ/с | OpenIndiana | 1580.00 |
| выделения на снятие значения | 63.00 выд./док. | Alpine | 126.00 |

Дно почти везде держит OpenBSD, но не везде: обход дерева со множеством разделов, обход
владеющего значения и просадку от подачи кусками держит OpenIndiana, а расход выделений
— Alpine. Расход этот расходится по **стандартной библиотеке, а не по системе**: 63
выделения на снятие значения у libstdc++ против 57 у libc++, границей проходит короткий
запас строки.

Прежняя оговорка «выделения и просадку задавать впритык» снята: просадка при дне 0.99
получила порог 1.90, а не 1.00. Показатель этот от быстродействия машины и правда не
зависит, но зависит от устройства кучи, и на двенадцати машинах он гуляет там, где на
пяти стоял.

### Про пороги

**Пороги назначены по замеру на стендах, а не расчётом.** Берётся наихудший из снятых
там показателей — по каждому сценарию свой, ибо «самой медленной машины» не существует —
и делится надвое. Запас двукратный берётся не на разброс внутри прогона, а на разброс
**между обходами**: одна и та же машина между двумя раскладками даёт до шестой части
расхождения на неизменном коде.

### Прогон на пяти стендах, 10.08.2026

Прогнано 10.08.2026 на пяти стендах: испытания — 49 из 49 всюду, замеры — 16 из
16 в пороге всюду. Пропускная способность чтения крупного файла:

| Стенд | МБ/с | отставание от рабочей машины |
|---|---:|---:|
| Fedora 42 ARM64 | 279.67 | быстрее |
| Fedora amd64 | 81.95 | 2.7× |
| NetBSD amd64 | 60.42 | 3.6× |
| FreeBSD amd64 | 52.75 | 4.1× |
| OpenBSD amd64 | 35.91 | 6.1× |

Отставание по сценариям расходится: у OpenBSD оно шестикратно на чтении и
девятикратно на сборке дерева настроек службы. Пороги поэтому назначены не общим
делителем, а по наименьшему замеру каждого сценария.

#### Прежняя разметка, замещена 21.08.2026

**Пороги назначены по замеру на стендах, а не расчётом.** Берётся наименьший из
снятых там показателей и делится надвое — запас на разброс между прогонами,
доходящий на стендах до трети. Показатели, не зависящие от быстродействия
машины, — количество выделений и просадка от подачи кусками — заданы впритык:
выделений всюду ровно два, просадка нигде не превысила 0.98.

Новые пороги проверены на всех пяти стендах: ни один сценарий их не нарушил.


### Дно по стендам, 21.08.2026 (второй обход)

Обход двенадцати машин, три прогона отдельными процессами. Правились лишь пороги с
существенной поправкой: ужесточение мельче четверти есть удача съёма, а не сведения о
коде, и порог, подтянутый под неё, откажет при следующем обходе. Дно прочих сценариев
сошлось с прежним обходом в пределах процентов.

Правлены два порога — оба у сценариев, заведённых после прежнего обхода и оттого
размеченных по одной рабочей машине:

| сценарий | дно | машина | было | стало |
|---|---|---|---|---|
| сборка значения вызовами | 5528.00 обр./с | openbsd | 25000 | 2764.00 |
| поиск значения по имени | 5580.83 обр./с | openbsd | 25000 | 2790.41 |

Оба показателя меряют перебор имён во владеющем значении, и дно у всех кодеков вышло
одинаковым — 5.4-5.6 тысячи обращений в секунду у OpenBSD. Одинаковость эта не
совпадение: перебор там один и тот же, и квадратичность его общая.

## Сличение со сторонними реализациями

Стенды сравнения с `inih`, `iniparser`, `SimpleIni`, `mINI` и `GKeyFile` лежат в
[tools/benchmark/ini](../../../tools/benchmark/ini), отчёт по ним —
в [COMPARISON.md](COMPARISON.md). Собираются они отдельно от библиотеки и в
состав её не входят.

Сличение это оказалось полезнее собственных замеров: три дефекта нашли именно
стенды сравнения, а не набор. Сценарий `обход дерева со множеством разделов`
заведён по итогам первого из них — стенд показал 0.25 МБ/с там, где набор
замеров ничего дурного не видел, и квадратичным оказался обход, а не сборка.
Двое прочих — уплотнение хранилища и перенос значений знак за знаком — подняли
чтение вдвое; разбор их в [COMPARISON.md](COMPARISON.md).

## Про сборку дерева вызовами

Правка дерева перестраивала указатели поиска целиком: порядковые номера записей
при вставке в середину перечня сдвигаются, и ссылающиеся на них указатели
обесцениваются. Сборка же дерева вызовами `set()` идёт вставкой в конец, где
ничего не сдвигается, и перестроение там ни к чему.

| | До | После |
|---|---:|---:|
| установок в секунду | 1 223 | 5 684 274 |
| выделений памяти на дерево | 400 327 283 | 40 067 |

Место вставки берётся из указателя последних записей разделов, а указатели
поиска наращиваются добавляемой записью. Вставка в середину — свойство в раздел,
за которым в тексте следуют другие, — по-прежнему идёт перестроением: там оно
неизбежно.

## Чего в наборе нет

**Замера расхода памяти деревом.** Дерево держит весь текст настроек вместе с
примечаниями, и расход его памяти кратен размеру файла — величина эта видна из
столбца `окт./файл` сведений о прогоне и отдельного сценария не требует.
