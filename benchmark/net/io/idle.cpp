/**
 * @file idle.cpp
 * @date 2026-07-29
 *
 * @license{LicenseRef-AWH-1.0}
 *
 * @author Yuriy Lobarev
 *
 * @telegram{forman}
 * @phone{+7 (910) 983-95-90}
 *
 * @email forman@anyks.com
 * @site https://anyks.com
 *
 * @brief Сценарий измерения обмена при множестве наблюдаемых подключений —
 *        нагрузка, ради которой создавались современные механизмы опроса:
 *        наблюдаемых подключений много, а обмен идёт на считанных единицах
 *
 * @copyright Copyright © 2026
 *
 */

/**
 * Стандартные заголовочные файлы
 */
#include <chrono>
#include <cstdio>
#include <memory>
#include <vector>

/**
 * Подключаем заголовочный файл бенчмарков сетевого движка
 */
#include "io.hpp"

/**
 * Используем стандартное пространство имён
 */
using namespace std;

/**
 * Подписываемся на пространство имён бенчмарков сетевого движка
 */
using namespace awh::benchmark::io;

/**
 * @brief Внутренние параметры и сценарии бенчмарков обмена при множестве наблюдаемых подключений
 *
 * @details Прочие сценарии набора работают в режиме, где готовы все наблюдаемые
 *          подключения разом: обмен на одном подключении, обмен на пятистах,
 *          поток на одном. В таком режиме выбор механизма опроса почти ничего
 *          не решает - стоимость оборота задаётся количеством готовых, а оно
 *          совпадает с количеством наблюдаемых. Замер на голых системных
 *          вызовах показывает это прямо: при пятистах наблюдаемых и пятистах
 *          готовых `select` и `kqueue` идут вровень, а `select` даже слегка
 *          впереди - ему нечего хранить между вызовами.
 *
 *          Расходятся они в другом режиме: наблюдаемых много, готовы единицы.
 *          Там `select` заново собирает и заново отдаёт ядру весь набор
 *          дескрипторов на каждый вызов, а `kqueue` держит подписки в ядре и
 *          возвращает только случившееся. На тысяче наблюдаемых при одном
 *          готовом разница составляет сорок пять раз, на восьми тысячах - три
 *          сотни.
 *
 *          Этот режим и есть обычная жизнь сетевого сервера: тысячи открытых
 *          подключений, из которых в каждый момент чего-то хочет сотня. Сценарий
 *          ниже воспроизводит его и потому измеряет то, чего прочие сценарии
 *          набора не измеряют вовсе, - стоимость самого наблюдения, отдельно от
 *          стоимости обмена
 *
 */
namespace {
	/**
	 * @brief Количество наблюдаемых подключений сценария
	 *
	 * @details Величина выбрана из двух ограничений. Снизу - разница между
	 *          механизмами опроса обязана быть различима: на четырёх тысячах
	 *          наблюдаемых при одном готовом полный просмотр набора обходится
	 *          в полторы сотни раз дороже выборки случившегося. Сверху -
	 *          динамические порты: каждое подключение занимает один, а их
	 *          в системе шестнадцать тысяч
	 *
	 */
	static constexpr size_t IDLE_WATCHED = 4000;
	/**
	 * @brief Запас описателей файлов сверх занимаемых подключениями
	 *
	 * @details Сверх пары описателей на всякое подключение прогон занимает
	 *          слушающий сокет, сам движок событий и то, что открыто точкой
	 *          входа набора. Запас взят с избытком: цена ему нулевая, а
	 *          нехватка обошлась бы прогоном, оборванным на середине
	 *
	 */
	static constexpr size_t IDLE_RESERVE = 64;
	/**
	 * @brief Количество обменивающихся подключений сценария
	 *
	 * @details Один процент от наблюдаемых. Доля выбрана так, чтобы стоимость
	 *          самого обмена не заслоняла стоимость наблюдения: чем меньше
	 *          обменивающихся, тем чище измеряется работа с подписками
	 *
	 */
	static constexpr size_t IDLE_ACTIVE = 40;
	/**
	 * @brief Требуемое количество обменов замера
	 *
	 */
	static constexpr size_t IDLE_ROUNDS = 50000;
	/**
	 * @brief Требуемое количество обменов прогрева
	 *
	 */
	static constexpr size_t IDLE_WARMUP = 5000;
	/**
	 * @brief Размер порции установления подключений
	 *
	 * @details Очередь ожидающих принятия подключений ограничена ядром - на
	 *          macOS это сто двадцать восемь, - и подать четыре тысячи
	 *          подключений разом нельзя: лишние будут отброшены и переданы
	 *          заново по таймауту повтора TCP, что растянуло бы установление
	 *          на минуты. Подключения поэтому подаются порциями, и каждая
	 *          порция принимается сервером прежде, чем подаётся следующая
	 *
	 */
	static constexpr size_t IDLE_BATCH = 64;
	/**
	 * @brief Предельный срок установления одной порции подключений в секундах
	 *
	 */
	static constexpr double IDLE_DEADLINE = 10.0;
	/**
	 * @brief Шаг опроса событий при установлении подключений в миллисекундах
	 *
	 */
	static constexpr int32_t IDLE_POLL = 100;
	/**
	 * @brief Порог скорости обмена при множестве наблюдаемых в обменах в секунду
	 *
	 * @details Порог откалиброван по отладочной сборке репозитория с двукратным
	 *          запасом: она даёт 223 тысячи обменов в секунду, оптимизированная -
	 *          264 тысячи. Ловит порог именно то, ради чего сценарий заведён:
	 *          работу движка с подписками. Появление в горячем пути обхода всех
	 *          наблюдаемых подключений вместо выборки случившихся обрушит
	 *          показатель на порядки, а не на проценты, так что двукратного
	 *          запаса ему хватает с избытком
	 *
	 */
	static constexpr double IDLE_THRESHOLD = 100000.0;
	/**
	 * @brief Порог количества выделений памяти на один обмен
	 *
	 * @details Установившийся обмен выделений памяти не выполняет, и наблюдение
	 *          за молчащими подключениями тем более: подписка на них поставлена
	 *          однажды и не меняется
	 *
	 */
	static constexpr double ALLOCATIONS_THRESHOLD = 0.5;
	/**
	 * @brief Порог количества системных вызовов на один обмен
	 *
	 * @details Обмен требует четырёх обращений к ядру - по приёму и передаче на
	 *          каждой стороне - плюс возврат за готовностью. Наблюдение за
	 *          молчащими подключениями обращений к ядру стоить не должно вовсе:
	 *          подписки на них стоят в ядре и на каждый оборот не переставляются.
	 *          Порог задан с тем же запасом, что и в сценариях обмена
	 *
	 */
	static constexpr double SYSCALLS_THRESHOLD = 6.5;

	/**
	 * @brief Структура состояния одного подключения сценария
	 *
	 */
	typedef struct Connection {
		// Идентификатор события подключения
		awh::event::id_t id;
		// Количество принятых октетов текущего сообщения
		size_t received;
		/**
		 * @brief Конструктор
		 *
		 */
		explicit Connection() noexcept : id(0), received(0) {}
	} connection_t;

	/**
	 * @brief Структура состояния прогона сценария
	 *
	 */
	typedef struct State {
		// Флаг остановки цикла событий
		bool stop;
		// Флаг активности замера
		bool measuring;
		// Количество выполненных обменов прогрева
		size_t warmed;
		// Количество выполненных обменов замера
		size_t done;
		// Количество принятых сервером подключений
		size_t accepted;
		// Момент начала замера
		std::chrono::steady_clock::time_point start;
		// Момент окончания замера
		std::chrono::steady_clock::time_point finish;
		/**
		 * @brief Конструктор
		 *
		 */
		explicit State() noexcept :
		 stop(false), measuring(false), warmed(0), done(0), accepted(0) {}
	} state_t;

	/**
	 * @brief Функция учёта выполненного обмена
	 *
	 * @param state состояние прогона сценария
	 * @return      результат учёта (true - обмен следует продолжать)
	 *
	 */
	static bool account(state_t & state) noexcept {
		// Если замер ещё не начат
		if(!state.measuring){
			// Считаем выполненный обмен прогрева
			state.warmed++;
			// Если прогрев ещё не завершён
			if(state.warmed < IDLE_WARMUP)
				// Продолжаем обмен
				return true;
			// Включаем режим замера
			state.measuring = true;
			// Включаем учёт выделений памяти
			awh::benchmark::counting(true);
			// Включаем учёт системных вызовов
			awh::benchmark::syscall::counting(true);
			// Запоминаем момент начала замера
			state.start = std::chrono::steady_clock::now();
			// Продолжаем обмен
			return true;
		}
		// Считаем выполненный обмен замера
		state.done++;
		// Если требуемое количество обменов ещё не выполнено
		if(state.done < IDLE_ROUNDS)
			// Продолжаем обмен
			return true;
		// Запоминаем момент окончания замера
		state.finish = std::chrono::steady_clock::now();
		// Закрываем окно замера: счётчики обязаны остановиться там же, где часы
		awh::benchmark::counting(false);
		awh::benchmark::syscall::counting(false);
		// Останавливаем цикл событий
		state.stop = true;
		// Прекращаем обмен
		return false;
	}
	/**
	 * @brief Функция прогона сценария обмена при множестве наблюдаемых подключений
	 *
	 * @details Устанавливается заданное количество подключений, все они остаются
	 *          под наблюдением до конца прогона, но обмен идёт только на первых
	 *          сорока. Обмен начинается лишь после того, как установлены все
	 *          подключения: иначе окно замера захватило бы часть установления, и
	 *          количество наблюдаемых во время замера не было бы постоянным
	 *
	 * @return итоги прогона сценария
	 *
	 */
	static outcome_t watched() noexcept {
		// Итоги прогона сценария
		outcome_t result;
		// Состояние прогона сценария
		state_t state;
		// Полезная нагрузка одного обмена
		static uint8_t payload[ECHO_PAYLOAD] = {0};
		// Создаём объект асинхронного движка ввода-вывода
		awh::engine::io_t io(framework(), logger());
		// Получаем свободный порт петлевого интерфейса
		const uint16_t number = port();
		// Добавляем новое событие сервера
		const awh::event::id_t server = io.event(awh::event::node_t::SERVER, awh::event::family_t::IPV4, awh::event::type_t::STREAM, awh::event::protocol_t::TCP);
		// Устанавливаем порт события сервера
		io.setSourcePort(server, number);
		// Если инициализация движка не выполнена
		if(!io.initialize())
			// Выводим пустые итоги прогона
			return result;
		// Устанавливаем опции события сервера
		io.setOptions(server, options());
		// Устанавливаем адрес события сервера
		io.setAddress(server, awh::event::address_t::IPV4, "127.0.0.1");
		// Устанавливаем функцию обратного вызова на принятие входящего подключения
		io.on(server, static_cast <awh::engine::callback::accept_t> ([&io, &state](const awh::event::id_t sid, const awh::event::id_t cid) noexcept -> void {
			/**
			 * Устанавливаем опции принятого подключения вместе с немедленным обрывом
			 *
			 * @note Прогон занимает четыре тысячи динамических портов, и без
			 *       немедленного обрыва все они остались бы в TIME_WAIT на тридцать
			 *       секунд, отравив последующие сценарии набора
			 */
			io.setOptions(cid, options() | awh::event::options::HARD_CLOSE);
			/**
			 * Устанавливаем функцию обратного вызова на чтение из принятого подключения:
			 * сервер возвращает принятые октеты как есть, разбор сообщения на его
			 * стороне не нужен - границы восстанавливает принимающий клиент
			 */
			io.on(cid, [&io](const awh::event::id_t eid, const uint8_t * data, const size_t size) noexcept -> void {
				// Отправляем принятые данные обратно клиенту
				io.send(eid, data, size);
			});
			// Считаем принятое сервером подключение
			state.accepted++;
		}));
		// Выполняем фиксацию настроек события сервера
		io.commit(server);
		/**
		 * Переводим событие сервера в режим прослушивания
		 *
		 * @note Предел задаётся по количеству наблюдаемых подключений сценария, а
		 *       не общим для набора значением: этот параметр ограничивает не
		 *       глубину очереди ожидающих принятия, а количество одновременно
		 *       обслуживаемых сервером подключений, и общего значения в тысячу
		 *       двадцать четыре сценарию не хватает. Сверх предела движок
		 *       принимает подключение и тут же закрывает его, сообщая об отмене
		 */
		io.listen(server, IDLE_WATCHED);
		// Запускаем событие сервера
		io.launch(server);
		// Список состояний клиентских подключений
		vector <unique_ptr <connection_t>> clients;
		// Резервируем память под состояния клиентских подключений
		clients.reserve(IDLE_WATCHED);
		/**
		 * Выполняем установление требуемого количества подключений порциями
		 */
		for(size_t i = 0; i < IDLE_WATCHED; i++){
			// Создаём состояние клиентского подключения
			clients.push_back(unique_ptr <connection_t> (new connection_t));
			// Получаем состояние созданного подключения
			connection_t * connection = clients.back().get();
			// Добавляем новое событие клиента
			connection->id = io.event(awh::event::node_t::CLIENT, awh::event::family_t::IPV4, awh::event::type_t::STREAM, awh::event::protocol_t::TCP);
			// Устанавливаем порт назначения события клиента
			io.setTargetPort(connection->id, number);
			// Устанавливаем опции события клиента вместе с немедленным обрывом соединения
			io.setOptions(connection->id, options() | awh::event::options::HARD_CLOSE);
			// Устанавливаем адрес привязки события клиента
			io.setAddress(connection->id, awh::event::address_t::IPV4, "0.0.0.0");
			// Устанавливаем адрес назначения события клиента
			io.setTarget(connection->id, "127.0.0.1");
			/**
			 * Устанавливаем функцию обратного вызова на чтение из события клиента:
			 * границы сообщения восстанавливаются накоплением принятых октетов,
			 * поскольку поток TCP вправе доставить ответ по частям
			 */
			io.on(connection->id, [&io, &state, connection](const awh::event::id_t eid, const uint8_t *, const size_t size) noexcept -> void {
				// Накапливаем количество принятых октетов текущего сообщения
				connection->received += size;
				// Если сообщение принято не полностью
				if(connection->received < ECHO_PAYLOAD)
					// Ожидаем оставшуюся часть сообщения
					return;
				// Учитываем принятое сообщение
				connection->received -= ECHO_PAYLOAD;
				// Если обмен следует продолжать
				if(::account(state))
					// Отправляем следующее сообщение обмена
					io.send(eid, payload, ECHO_PAYLOAD);
			});
			// Выполняем фиксацию настроек события клиента
			io.commit(connection->id);
			// Выполняем подключение клиента к серверу
			io.connect(connection->id);
			// Запускаем событие клиента
			io.launch(connection->id);
			// Если порция подключений подана не полностью, продолжаем её подачу
			if((((i + 1) % IDLE_BATCH) != 0) && ((i + 1) < IDLE_WATCHED))
				// Переходим к следующему подключению порции
				continue;
			// Запоминаем момент начала установления порции подключений
			const auto started = std::chrono::steady_clock::now();
			/**
			 * Дожидаемся принятия сервером всех поданных подключений
			 */
			while(state.accepted <= i){
				// Если опрос событий завершился ошибкой
				if(!io.poll(IDLE_POLL))
					// Прекращаем прогон сценария
					return result;
				// Если порция подключений не установилась в отведённый срок
				if(std::chrono::duration <double> (std::chrono::steady_clock::now() - started).count() > IDLE_DEADLINE){
					// Сообщаем о неустановившейся порции: молча усечённый прогон читался бы как полный
					::fprintf(stderr, "idle: подключения не установились за %.0f с, подано %zu, принято %zu\n", IDLE_DEADLINE, (i + 1), state.accepted);
					// Выводим пустые итоги прогона
					return result;
				}
			}
		}
		/**
		 * Начинаем обмен на подключениях, которым он назначен
		 *
		 * @note Обмен запускается здесь, а не в функции обратного вызова на
		 *       подключение, именно ради постоянства количества наблюдаемых:
		 *       к этому моменту все подключения установлены и находятся под
		 *       наблюдением, и оно уже не меняется до конца прогона
		 */
		for(size_t i = 0; i < IDLE_ACTIVE; i++)
			// Отправляем первое сообщение обмена
			io.send(clients[i]->id, payload, ECHO_PAYLOAD);
		/**
		 * Запускаем опрос событий до выполнения требуемого количества обменов
		 */
		while(!state.stop && io.poll());
		// Устанавливаем количество выполненных операций
		result.operations = state.done;
		// Устанавливаем объём переданных данных с учётом обоих направлений обмена
		result.bytes = (state.done * ECHO_PAYLOAD * 2);
		// Устанавливаем затраченное время
		result.seconds = std::chrono::duration <double> (state.finish - state.start).count();
		// Снимаем показатели окружения по итогам замера
		collect(result);
		// Выполняем деинициализацию движка
		io.deinitialize();
		// Выводим итоги прогона сценария
		return result;
	}
	/**
	 * @brief Функция получения итогов прогона сценария
	 *
	 * @note Итоги снимаются с единственного прогона: повторный расходовал бы
	 *       динамические порты операционной системы
	 *
	 * @return итоги прогона сценария
	 *
	 */
	static const outcome_t & measured() noexcept {
		// Итоги прогона сценария обмена при множестве наблюдаемых подключений
		static const outcome_t result = ::watched();
		// Выводим итоги прогона сценария
		return result;
	}
	/**
	 * @brief Функция прогона сценария скорости обмена при множестве наблюдаемых подключений
	 *
	 * @return результат измерения
	 *
	 */
	static awh::benchmark::result_t exchanges() noexcept {
		// Результат измерения
		awh::benchmark::result_t result;
		// Причина невозможности прогона по окружению
		std::string reason = "";
		/**
		 * Если описателей файлов на прогон не хватает, измерение не выполняется
		 *
		 * @note Прогон занимает описатель на КАЖДУЮ сторону всякого наблюдаемого
		 *       подключения, и делает это в одном процессе. Там, где предела не
		 *       хватает, подключения не устанавливаются, и сценарий отдаёт ноль
		 *       операций - а это не провал движка, а невозможность прогона по
		 *       окружению, и объявлять её провалом означало бы ложную тревогу
		 */
		if(!descriptors(((IDLE_WATCHED + 1) * 2) + IDLE_RESERVE, reason)){
			// Отмечаем измерение как не выполнявшееся
			result.skipped = true;
			// Устанавливаем причину, по которой измерение не выполнялось
			result.reason = reason;
			// Выводим результат измерения
			return result;
		}
		// Получаем итоги прогона сценария
		const outcome_t & outcome = ::measured();
		// Устанавливаем измеренное значение
		result.value = perSecond(outcome);
		// Убеждаемся, что сценарий выполнил хоть одну операцию
		validate(result, outcome);
		// Устанавливаем сведения о прогоне
		result.details = details(outcome);
		// Выводим результат измерения
		return result;
	}
	/**
	 * @brief Функция прогона сценария учёта выделений памяти на один обмен
	 *
	 * @return результат измерения
	 *
	 */
	static awh::benchmark::result_t allocations() noexcept {
		// Результат измерения
		awh::benchmark::result_t result;
		// Причина невозможности прогона по окружению
		std::string reason = "";
		/**
		 * Если описателей файлов на прогон не хватает, измерение не выполняется
		 *
		 * @note Прогон занимает описатель на КАЖДУЮ сторону всякого наблюдаемого
		 *       подключения, и делает это в одном процессе. Там, где предела не
		 *       хватает, подключения не устанавливаются, и сценарий отдаёт ноль
		 *       операций - а это не провал движка, а невозможность прогона по
		 *       окружению, и объявлять её провалом означало бы ложную тревогу
		 */
		if(!descriptors(((IDLE_WATCHED + 1) * 2) + IDLE_RESERVE, reason)){
			// Отмечаем измерение как не выполнявшееся
			result.skipped = true;
			// Устанавливаем причину, по которой измерение не выполнялось
			result.reason = reason;
			// Выводим результат измерения
			return result;
		}
		// Получаем итоги прогона сценария
		const outcome_t & outcome = ::measured();
		// Устанавливаем измеренное значение
		result.value = perOperation(outcome);
		// Убеждаемся, что сценарий выполнил хоть одну операцию
		validate(result, outcome);
		// Устанавливаем сведения о прогоне
		result.details = details(outcome);
		// Выводим результат измерения
		return result;
	}
	/**
	 * @brief Функция прогона сценария учёта системных вызовов на один обмен
	 *
	 * @return результат измерения
	 *
	 */
	static awh::benchmark::result_t syscalls() noexcept {
		// Результат измерения
		awh::benchmark::result_t result;
		// Если учёт системных вызовов недоступен
		if(!awh::benchmark::syscall::available()){
			// Отмечаем измерение как не выполнявшееся
			result.skipped = true;
			// Устанавливаем причину, по которой измерение не выполнялось
			result.reason = awh::benchmark::syscall::reason();
			// Выводим результат измерения
			return result;
		}
		// Причина невозможности прогона по окружению
		std::string reason = "";
		/**
		 * Если описателей файлов на прогон не хватает, измерение не выполняется
		 *
		 * @note Прогон занимает описатель на КАЖДУЮ сторону всякого наблюдаемого
		 *       подключения, и делает это в одном процессе. Там, где предела не
		 *       хватает, подключения не устанавливаются, и сценарий отдаёт ноль
		 *       операций - а это не провал движка, а невозможность прогона по
		 *       окружению, и объявлять её провалом означало бы ложную тревогу
		 */
		if(!descriptors(((IDLE_WATCHED + 1) * 2) + IDLE_RESERVE, reason)){
			// Отмечаем измерение как не выполнявшееся
			result.skipped = true;
			// Устанавливаем причину, по которой измерение не выполнялось
			result.reason = reason;
			// Выводим результат измерения
			return result;
		}
		// Получаем итоги прогона сценария
		const outcome_t & outcome = ::measured();
		// Устанавливаем измеренное значение
		result.value = perSyscall(outcome);
		// Убеждаемся, что сценарий выполнил хоть одну операцию
		validate(result, outcome);
		// Устанавливаем сведения о прогоне
		result.details = details(outcome);
		// Выводим результат измерения
		return result;
	}

	// Регистрируем сценарий обмена при множестве наблюдаемых подключений
	static const bool gExchanges = awh::benchmark::add(
		"net/io/idle/exchanges", "обменов/с", IDLE_THRESHOLD,
		awh::benchmark::bound_t::MINIMUM, &::exchanges
	);
	// Регистрируем сценарий учёта выделений памяти на один обмен
	static const bool gAllocations = awh::benchmark::add(
		"net/io/idle/allocations-per-exchange", "выделений", ALLOCATIONS_THRESHOLD,
		awh::benchmark::bound_t::MAXIMUM, &::allocations
	);
	// Регистрируем сценарий учёта системных вызовов на один обмен
	static const bool gSyscalls = awh::benchmark::add(
		"net/io/idle/syscalls-per-exchange", "вызовов", SYSCALLS_THRESHOLD,
		awh::benchmark::bound_t::MAXIMUM, &::syscalls
	);
};
