# Сравнение транспорта QUIC с реализацией AWH

Отчёт по прогону сценариев `benchmark/proto/quic` через две реализации
транспорта QUIC на одной машине, одним компилятором, на одной нагрузке, тем же
криптографическим бэкендом и с одинаковой работой потребителя. Стенды сравнения
лежат в [tools/benchmark/quic](../../../tools/benchmark/quic) — там же описано,
как их получить, собрать и запустить.

Собственный отчёт подмодуля — [PERFORMANCE.md](PERFORMANCE.md); здесь измеряется
не сам движок, а его место рядом с эталонной реализацией.

## Что с чем сравнивается

| Реализация | Версия | Модель |
|---|---|---|
| **AWH** | ветка `v5` | движок протокола без ввода-вывода, синтетические часы, инъекция времени параметром |
| **ngtcp2** | 1.26.0 | движок протокола без ввода-вывода, синтетические часы, инъекция времени параметром |

**msquic, mvfst и quiche в таблице отсутствуют не по недосмотру.** msquic (Microsoft)
и mvfst (Meta) сращены с собственной платформой ввода-вывода — рабочими потоками,
сокетами, циклом событий — и вне её не существуют: прогнать их движок в памяти по
синтетическим часам, не переписав платформу, нельзя, а сравнивать переписанное
бессмысленно. Это то же соображение, по которому в сравнении парсеров HTTP/1.x
отсутствуют curl и Apache httpd. quiche (Cloudflare) ввода-вывода не выполняет, но
время берёт из системных часов внутренними вызовами, а не параметром, — в общей
детерминированной методике её замер был бы несопоставим. picoquic и quicly этой
методике поддаются и годятся для расширения сравнения, но тянут отдельный
криптографический стек (picotls), которого в проекте нет, а сравнивать важно на
общем с AWH бэкенде BoringSSL. Подробный разбор — в
[README стендов](../../../tools/benchmark/quic/README.md).

ngtcp2 выбрана как эталонная реализация QUIC на языке C и ближайший архитектурный
аналог AWH: тоже без ввода-вывода, тоже с приёмом и отдачей пакетов вызовами и
временем-параметром на каждый вызов. Только это и позволяет прогнать обе стороны
в памяти по одним синтетическим часам и получить честное число.

## Условия

* Apple M4 Max, 16 ядер, macOS 26.5.2, Apple clang 21.0.0, `arm64`.
* Оба стенда собраны с `-O3 -DNDEBUG`; библиотека AWH — сборкой в режиме `Release`.
* Криптографический бэкенд общий — встроенный в проект BoringSSL.
* Показатели пропускной способности — медиана шести прогонов (первый, разгонный,
  отброшен); разброс между прогонами 1–6 % (шире — на множестве потоков).
* Объём передачи 64 МБ, блок постановки в очередь 64 КБ, транспортные параметры и
  критерий завершения совпадают с `benchmark/proto/quic` и одинаковы у обоих стендов.
* Приёмник в обоих стендах копирует принятые данные в буфер приложения и на равный
  объём продвигает окна управления потоком — работа потребителя одинакова.
* Рукопожатие (генерация сертификата, обмен ключами) выполняется до измерения и в
  замер пропускной способности не входит.

**Учёт выделений памяти охватывает у каждого движка только его собственный
распределитель.** У AWH выделения считают перегруженные операторы языка, у ngtcp2 —
переданный движку распределитель `ngtcp2_mem`. Общий криптографический слой
BoringSSL в учёт не попадает ни у одного из стендов, поэтому показатель отражает
работу с памятью именно транспорта.

## Пропускная способность

| Сценарий | AWH | ngtcp2 | Отношение |
|---|---:|---:|---:|
| `throughput/single-stream` | 1789 МБ/с | **1880 МБ/с** | ngtcp2 в 1.05× |
| `throughput/multi-stream` (64 потока) | 1374 МБ/с | **1900 МБ/с** | ngtcp2 в 1.38× |

Обе реализации держат передачу в диапазоне единиц гигабайт в секунду на
оптимизированной сборке — один порядок величины. На одном потоке разрыв сузился
до паритета (в 1.05×), на множестве потоков ngtcp2 впереди (в 1.38×).
Абсолютные значения зависят от загрузки машины — этот прогон снят на рабочем
столе с фоновой нагрузкой, поэтому оба абсолюта ниже прежних; отношение, снятое
обоими стендами подряд в одной сессии, устойчивее.

Различие по-прежнему видно при переходе от одного потока к 64: показатель скорости
AWH падает, а ngtcp2 держится ровно. По числу выделений памяти на множестве потоков
AWH, напротив, эталон обходит (см. ниже); остаток отставания — по объёму выделенной
памяти, на росте пер-стримовых буферов отправки и приёма, и это следующее конкретное
направление доработки.

## Выделения памяти

| Сценарий | AWH | ngtcp2 |
|---|---:|---:|
| выделений на датаграмму, один поток | **0.04** | 0.08 |
| выделений на датаграмму, 64 потока | **0.05** | 0.08 |
| выделено на 64 МБ, один поток | **1.2 МБ (0.02×)** | 6.2 МБ (0.10×) |
| выделено на 64 МБ, 64 потока (безлимит) | 153 МБ (2.39×) | **6.2 МБ (0.10×)** |
| выделено на 64 МБ, 64 потока (метка 64 КБ) | 35.5 МБ (0.55×) | **6.2 МБ (0.10×)** |
| выделено на 64 МБ, 64 потока (метка 16 КБ) | **9.0 МБ (0.14×)** | 6.2 МБ (0.10×) |

Число выделений и объём выделенного здесь разделены намеренно: это две разные
статьи, и у AWH они ведут себя по-разному.

**По числу выделений AWH плотнее эталона на всех сценариях** — 0.04 на датаграмму
на одном потоке, 0.05 на 64 потоках (0.02 при ограниченном стейджинге) против ровных
0.08 у ngtcp2. Типовой пакет несёт один STREAM-фрейм, а его учётная запись
размещается в инлайн-хранилище пакета без обращения к куче (см. ниже). Прежде этот
путь был главным источником выделений на множестве потоков — на нём и держались
прежние 0.49 на датаграмму.

**Объём выделенного на множестве потоков зависит от режима отправки, и здесь важно
не сравнивать разные модели API.** Безлимитные 153 МБ (2.39×) против 6.2 МБ у ngtcp2
выглядят провалом, но это сравнение copy-in-режима AWH с zero-copy-моделью ngtcp2 —
яблоки против апельсинов:

- **ngtcp2 безлимитного режима не имеет.** Его `writev_stream` пакует данные напрямую
  из буфера приложения и берёт лишь то, что влезает в пакет сейчас; движок держит
  только неподтверждённое (по cwnd), тело хранит приложение. Отсюда ровные 6.2 МБ
  независимо от объёма передачи и числа потоков — стейджинг у него ограничен *всегда*.
- **AWH `send()` в безлимитном режиме** — copy-in: приложение сваливает всё (~1 МБ на
  поток, обгоняя сеть), движок стейджит весь backlog в непрерывный `txBuffer`. Из
  153 МБ около 130 МБ — его геометрический рост (профиль: ~5 удвоений `0→пик` на
  поток, а не рекуррентный churn: `erase()` сохраняет ёмкость). Это цена удобства
  безлимитного приёма, а не изъян движка. Pull-модель `dataSource` тут не отличается —
  она стейджит через тот же `txBuffer`; разница именно copy-in-буфер против zero-copy.
- **При сопоставимом ограничении стейджинга** (`sendWaterMarks` — backpressure) AWH
  выходит на порядок ngtcp2 по обеим осям одновременно: **9.0 МБ (0.14×) при метке
  16 КБ, 35.5 МБ (0.55×) при 64 КБ, 69.8 МБ (1.09×) при 256 КБ** — против 6.2 МБ. И
  ограничение не снижает, а *повышает* пропускную способность (лучше локальность,
  меньше накладных): на 64 потоках 1405 → ~1735 МБ/с. Сценарий
  `quic/throughput/multi-stream-bounded` (метка 64 КБ) прогоняется обоими стендами.

Иными словами, честное like-for-like сравнение — это ограниченный AWH против всегда
ограниченного ngtcp2: **9–35 МБ против 6.2 МБ**, тот же порядок величины, а не 25×.
Сегментация `txBuffer` на пул блоков сознательно *не* делалась: даже сняв 2× рост,
безлимитный режим обязан удержать весь сваленный backlog (~70 МБ) — хуже, чем 9 МБ
через метки, при большем риске на пути ретрансмиссии. Рычаг памяти отправки — это
метки, а не переписывание горячего пути. Приёмный буфер при этом уже переведён на
пул блоков (см. ниже, 179 → 153 МБ на безлимите).

## Снятые накладные расходы

По итогам этого сравнения сняты несколько статей из тех, что отделяли AWH от эталона.

**Копия ретрансмиссии.** Учётная запись пакета хранила копию каждого отправленного
блока данных потока на случай ретрансмиссии; в передаче без потерь эта копия —
чистые накладные: выделение и полный проход по всем отправленным данным, тут же
освобождаемые при подтверждении. Теперь свежая отправка кладёт в учётную запись
только ссылку на буфер отправки потока (`sid`+`offset`+`size`), буфер удерживается
до подтверждения и уплотняется лишь по подтверждённому префиксу, а при потере
данные перечитываются из буфера (RFC 9000 §13.3). Результат детерминирован и от
машины не зависит: выделения снизились с **3.00 до 2.02** на датаграмму, объём
выделенного в однопоточном сценарии — с **73 до 9.7 МБ** (в 7.5 раза), в
многопоточном — с **246 до 182 МБ**.

**Обход потоков при упаковке.** Сборка каждого пакета обходила весь список потоков
в поисках управляющих фреймов — стоимость, растущая с числом потоков, тогда как у
отправителя данных таких фреймов нет вовсе. Теперь обход идёт по короткому списку
потоков с ожидающими управляющими фреймами (по образцу списка готовых к выдаче),
а не по всему списку. По профилю доля этого обхода в горячем пути упала с **15 %
до 3 %** на 64 потоках — до уровня однопоточного сценария; стоимости на число
потоков в упаковке практически не осталось.

**Пул учётных записей отправки.** Учётная запись отправленного пакета выделялась
и освобождалась на каждый пакет. Теперь записи берутся из объектного пула и
возвращаются в него после подтверждения, а рабочий список готовых к выдаче потоков
переведён на отсортированный вектор с выборкой по `lower_bound` вместо покаждого
выделения. В установившемся режиме на одном потоке попакетного выделения не
осталось: выделения на датаграмму упали с **2.02 до 0.04**, удержание в
однопоточном сценарии — с **9.7 до 1.3 МБ**, и по этой метрике AWH на одном потоке
опустился ниже эталона (0.02× против 0.10×).

**Инлайн-хранилище учётной записи пакета.** На множестве потоков объектный пул
записей осушался быстрее, чем пополнялся: сборка пакета сливает данные многих
потоков одним заходом (burst), а пополнение пула идёт лишь на подтверждениях -
профиль показал, что промах пула приходился примерно на каждый второй пакет, и
запись бралась с пустым вектором отправленных блоков, вектор рос с нуля - выделение
на пакет. Это давало **95 % всех выделений** на множестве потоков (найдено не
догадкой, а бэктрейс-профилировкой подменённого `operator new` и счётчиками
попаданий пула). Теперь список отправленных блоков потока размещается в самой
учётной записи в инлайн-хранилище (small buffer optimization) на типовой пакет с
одним STREAM-фреймом, и обращения к куче на пакет не происходит независимо от
состояния пула. Выделения на множестве потоков упали с **0.49 до 0.02 на
датаграмму** - ниже и однопоточного сценария AWH, и эталона (0.08).

**Пул блоков приёмного буфера.** Собранные данные потока копились до выдачи
приложению в непрерывной строке `rxReady`, растущей удвоением ёмкости; на 64 потоках
это давало около 49 МБ одноразового роста (по ~9 удвоений на поток). Теперь приёмный
буфер сегментирован на блоки фиксированного размера (`chunked_fifo_t`), которые
берутся из общего пула соединения и возвращаются в него при выдаче: новый поток берёт
готовый блок вместо роста с нуля. На множестве потоков объём выделенного упал со
**179 до 153 МБ** (2.80×→2.39×); на одном потоке — с 1.4 до 1.2 МБ. Число выделений
при этом чуть выросло (0.02→0.05 на датаграмму, дробление дозаписи по границам
блоков), оставаясь ниже эталона (0.08). Наибольший выигрыш пул даёт не здесь, а на
воркоадах с частым открытием/закрытием коротких потоков (запросы HTTP/3), где рост
буфера повторялся бы на каждый поток; сценарий с 64 заранее открытыми долгоживущими
потоками — для пула наихудший и его недооценивает. Отправной буфер оставлен
непрерывным: он той же природы (одноразовый рост с сохранением ёмкости), но его
сегментация задевает ретрансмиссию срезом по смещению и уплотнение префикса —
критичный путь; выигрыш при этом ограничен полом одновременной буферизации, а память
отправки уже снимается метками до 0.62×.

Эти правки проверены полным набором тестов транспорта (включая потери,
ретрансмиссию, сброс потоков, управление потоком) на macOS и FreeBSD, а отправной
путь фасада клиента и сервера — новыми интеграционными тестами loopback поверх
реальных сокетов (`tests/proto/quic/facade.cpp`: частичный приём, сигнал `writable`,
источник данных, датаграмма).

## Итог

На оптимизированной сборке AWH держится в том же порядке величины, что и
эталонная реализация QUIC на языке C: по скорости — паритет на одном потоке
(в 1.05×) и примерно в 1.4× на 64 потоках; по числу выделений — плотнее эталона на
всех сценариях (0.04–0.05 против ровных 0.08 у ngtcp2). Для реализации, написанной
с нуля и сверенной с ngtcp2 на уровне протокола (см.
[tools/interop](../../../tools/interop)), это добротный уровень.

По объёму выделенной памяти на множестве потоков разрыв кажется большим — 2.39×
против 0.10× — но только при сравнении разных моделей API: безлимитного copy-in
режима AWH против всегда ограниченной zero-copy модели ngtcp2. При честном
like-for-like (ограничив стейджинг отправки метками, как ограничена модель ngtcp2)
AWH выходит на порядок эталона по обеим осям сразу: **9–35 МБ против 6.2 МБ** и
пропускная при этом растёт до ~1735 МБ/с. Безлимитные 153 МБ — это цена удобства
режима, где приложение сваливает данные без backpressure, а не изъян движка;
приёмный буфер дополнительно переведён на пул блоков (2.80× → 2.39×). Сегментация
буфера отправки сознательно не делалась: рычаг памяти отправки — водяные метки, а
не переписывание горячего пути ретрансмиссии.

## Как воспроизвести

```sh
# Сборка библиотеки AWH с оптимизацией и обоих стендов
cmake -S . -B build-release -DCMAKE_BUILD_TYPE=Release -DCMAKE_BUILD_BENCHMARKS=YES
cmake --build build-release --target awh_BENCHMARK_proto -j 8
./tools/benchmark/quic/build.sh

# Прогон (медиана не менее пяти прогонов, первый отбрасывается)
/tmp/rival-quic/awh
/tmp/rival-quic/ngtcp2
```

Пропускная способность зависит от машины и режима сборки; для сравнимых цифр
снимайте оба стенда в одной конфигурации. Показатель выделений сравним между
запусками и машинами напрямую.
