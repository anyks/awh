# Сведение туннелей между движками: план работ и отчёт

Записка о работах по объединению машин туннелями VPN. Ведётся сессией движка
IOCP. План поставлен владельцем 21.08.2026.

## Исходное положение

**Главная задача не сделана.** Настоящего объединения машин туннелем VPN не
было: были щупы, проверявшие отдельные куски пути. Проверка щупом не заменяет
поднятой связи между двумя машинами.

Сделанным считать можно только следующее:

- **Запись в туннельное устройство под MS Windows работает** на обоих драйверах,
  tap-windows6 и Wintun. Дефекта записи нет.
- **Дефект первого пакета найден и исправлен.** Условие постановки в очередь
  стояло отрицанием: первый пакет уходил в очередь вместо устройства. До правки
  первый оборот проходил 7 раз из 8 с потерей нулевого куска, после — 8 из 8.
  Дефект общий для всех пяти движков.
- **Драйверы в поставку не входят** (решение владельца от 20.08.2026): ни
  `wintun.dll`, ни драйвер OpenVPN. Их ставит пользователь по инструкции.

## План работ

### Шаг 1. Зона адреса IPv6

Обсудить с Гришей. Он этой задачей занимался, и возможны два расклада:

- **он её решил** — взять у него разбор и тонкости, не изобретая заново;
- **не решил** — значит задача общая для обоих движков, и решать её вместе.

Туда же — прочие найденные дефекты (`launch` после отказа `commit`): спросить,
как он их видит, прежде чем чинить у себя.

**Зафиксировано владельцем:** у дейтаграммного сервера нет и не может быть
вызова `listen()`, а состояние `LISTENING` к нему не относится вовсе. `listen()`
существует только для поточного сервера TCP и SCTP. Смотреть на это состояние при
разборе дейтаграммных путей — ошибка, и она уже была допущена.

### Шаг 2. Туннели VPN

Возвращаться к туннелям только после закрытия шага 1.

Связывать **машины настоящей работой**, а не проверять щупами. Требуется
убедиться, что связь по VPN между разными машинами и движками поднимается и
работает без изъянов.

**tap-windows6 и Wintun проверять по отдельности.** Это два разных устройства
туннеля с разными путями работы, и каждое сводится со **всеми** прочими
движками, в **обе** стороны. Проверка одного драйвера с одним движком ничего не
говорит о втором.

Требование владельца: **полная работоспособность всех со всеми во всех
направлениях.**

### Шаг 3. Итоговый отчёт

Отчёт с таблицей, из которой видно, какая машина с какой объединена, какими
движками и в каком направлении.

**Движком считать способ работы, а не название системы.** У MS Windows это
tap-windows6 и Wintun — два разных движка. У Linux это `epoll` и `io_uring` —
тоже два разных.

**Вид записи в отчёте.** Числа обязаны быть говорящими: не «7 из 7», а сколько
именно октетов отправлено одним движком и сколько получено другим.

| движок-отправитель | машина | → | движок-получатель | машина | отправлено | получено | итог |
|--------------------|--------|---|-------------------|--------|-----------|----------|------|
| tap-windows6 | Windows 10 x86-64 | → | kqueue | FreeBSD 15.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | FreeBSD 15.1 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | kqueue | FreeBSD 15.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | FreeBSD 15.1 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | kqueue | NetBSD 10.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | NetBSD 10.1 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | kqueue | NetBSD 10.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | NetBSD 10.1 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | kqueue | OpenBSD 7.9 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | OpenBSD 7.9 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | kqueue | OpenBSD 7.9 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | OpenBSD 7.9 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | epoll | Debian 12.15 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| epoll | Debian 12.15 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | epoll | Debian 12.15 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| epoll | Debian 12.15 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | epoll | Alpine 3.24.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| epoll | Alpine 3.24.1 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | epoll | Alpine 3.24.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| epoll | Alpine 3.24.1 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | io_uring | Alpine 3.24.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| io_uring | Alpine 3.24.1 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | io_uring | Alpine 3.24.1 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| io_uring | Alpine 3.24.1 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | Event Ports | Solaris 11.4 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Event Ports | Solaris 11.4 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | Event Ports | Solaris 11.4 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Event Ports | Solaris 11.4 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | epoll | Fedora 44 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| epoll | Fedora 44 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | epoll | Fedora 44 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| epoll | Fedora 44 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | kqueue | DragonFly 6.4.2 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | DragonFly 6.4.2 | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | kqueue | DragonFly 6.4.2 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| kqueue | DragonFly 6.4.2 | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| tap-windows6 | Windows 10 x86-64 | → | Event Ports | OpenIndiana | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Event Ports | OpenIndiana | → | tap-windows6 | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Wintun | Windows 10 x86-64 | → | Event Ports | OpenIndiana | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |
| Event Ports | OpenIndiana | → | Wintun | Windows 10 x86-64 | 5 × 1028 = 5140 | 5 × 1028 = 5140 | сошлось |

Прогнано 22.08.2026 на перезагруженной машине FreeBSD, при одном живом сервере и
одном занятом гнезде — проверено перед замером. Октеты сняты с журналов ОБЕИХ
сторон и сошлись на каждом шаге переноса: «Прочитано из посредника», «Отправлено
в клиента/сервер», «Прочитано из клиента», «Отправлено в туннель».

Драйвер задавался ЯВНО доводом `--driver`, а не выбором по умолчанию: иначе `AUTO`
взял бы Wintun в обоих прогонах, и про tap-windows6 сказать было бы нечего.

Строка заполняется по прогону; сводных долей вида «7/7» в отчёте быть не должно.

**ВАЖНО: вся матрица снята на коде БЕЗ правки движков** (`e01e90fe3`), потому что правка
вошла позже, коммитом `6fc5a34a4`. Числа от этого не меняются, и это ПРОВЕРЕНО ПЕРЕСЪЁМКОЙ, а не
выведено рассуждением. 22.08.2026 деревья обновлены до `6fc5a34a4`, образцы пересобраны,
пары пересняты обоими драйверами:

| машина | движок | сумма образца до → после | числа |
|--------|--------|--------------------------|-------|
| Fedora 44 | epoll | пересобран | 5140/5140, оба драйвера |
| OpenIndiana | Event Ports | 2097004371 → 1439482865 | 5140/5140, оба драйвера |
| DragonFly 6.4.2 | kqueue | 1756393487 → НЕ СМЕНИЛАСЬ | пересъёмка не удалась |

Числа Fedora и OpenIndiana вровень с прежними.

**DragonFly переснять не вышло — упор в диск стенда, а не ошибка прогона.** Три захода
подряд, каждый раз сумма образца НЕ менялась:

    заход 1: диск забит после git pull  → CMake Error: No space left on device
    заход 2: 8.4 ГБ после сбора мусора  → ar: libawh.a: No space left on device
    заход 3: 8.4 ГБ, рваный архив снесён → Error 2, диск ушёл в -128K

Машина 15.8 ГБ, и полная пересборка после обновления дерева в неё не влезает: `.git` там
2.8 ГБ, плюс подмодули, третья сторона и сборка С ПОКРЫТИЕМ (`libawh_gcov.a` — 337 МБ).
Сбор мусора HAMMER2 возвращает 8.4 ГБ, но сборка съедает их целиком за минуты.

Оттого четыре строки DragonFly остаются снятыми на `e01e90fe3`. Доводы, по каким они
всё же годны, но НИ ОДИН из них не есть пересъёмка:

- числа сняты дважды, при первом заходе и после освобождения диска, оба раза 5140/5140;
- на двух других машинах пересъёмка проведена, и числа не изменились ни на единицу;
- правка меняет ход лишь на пути отказа, а здесь адрес назначается.

**Каждый из трёх отказов был бы неотличим от успеха**, не сличай мы сумму: образец
оставался на месте, время файла правдоподобным, а вывод уходил в фоновый файл.

**Свежесть образца доказывать СМЕНОЙ КОНТРОЛЬНОЙ СУММЫ (`cksum`), а не временем файла.**
Пояса стендов расходятся до восьми часов, и одно и то же время значит на разных машинах
разное. Сумма от часов не зависит. Ею же ловится и молчаливый отказ сборки: на DragonFly
`git pull` забил диск под ноль, сборка упала с «No space left on device», образец остался
на месте со старой суммой — по выводу и времени файла это неотличимо от успеха.

Причина, по какой правка чисел не задевает: она меняет ход ТОЛЬКО когда назначение адреса
не прошло, а в прогонах матрицы адрес назначается — устройства поднимаются с адресами.
Правленая ветвь там не исполняется вовсе.

**Четыре строки DragonFly сняты ДВАЖДЫ.** Первый заход прошёл при забитом под ноль
диске (15.8 из 15.8 ГБ), и числа его были под сомнением. После `hammer2 bulkfree /`
(вернул 949 МБ) оба драйвера пересняты на свободном диске — числа совпали в точности.
Сомнение снято замером, а не рассуждением.

## Движки и машины

| движок | система | машина |
|--------|---------|--------|
| tap-windows6 | MS Windows 10, x86-64 | `forman@10.100.1.102` |
| Wintun | MS Windows 10, x86-64 | `forman@10.100.1.102` |
| kqueue | macOS | рабочая машина |
| kqueue | FreeBSD 15.1 | `forman@10.100.1.207` |
| kqueue | FreeBSD 14.1 | `anyks@anyks.com` |
| kqueue | NetBSD, OpenBSD | стенды |
| epoll | Linux | стенды |
| io_uring | Linux | стенды |
| Event Ports | Solaris, OpenIndiana | стенды |

## Помехи и их разбор

**Экран на `anyks.com` — не дефект, обходить не нужно.** Там работает `ipfw` со
строгим набором: завершающее правило `65535 deny ip from any to any`, а все
разрешающие привязаны к `via igb0`. Пакеты через `tun0` не совпадают ни с одним
из них и отбрасываются. Это рабочий сервер владельца, трогать его нельзя.
**Туннельные пары гонять на `forman@10.100.1.207`** — там экрана нет вовсе, ни
`pf`, ни `ipfw`, модули не загружены (проверено 21.08.2026).

## Уровень устройства туннеля не обязан совпадать у сторон

Устройства туннеля работают на разных уровнях: `tap-windows6` и путь illumos через
`/dev/net/<имя>` переносят КАДРЫ канального уровня, а Wintun и устройства `tun` у
BSD и Linux — ПАКЕТЫ сетевого. Отсюда напрашивается вывод, что пары с разными
уровнями сойтись не могут, и вывод этот НЕВЕРЕН.

**Опровергнуто замером 22.08.2026.** Пара «tap-windows6 (канальный) ↔ kqueue либо
epoll (сетевой)» сходится, и не однажды: DragonFly 6.4.2, Fedora 44, а до того
FreeBSD 15.1, NetBSD 10.1, OpenBSD 7.9, Alpine 3.24.1 обоими движками — везде
5 × 1028 = 5140 октетов в каждую сторону без потерь.

Причина в устройстве движка: приписывание и снятие заголовка кадра, а равно
ответы на разрешение адресов, ложатся на САМ ДВИЖОК — это записано примечанием у
`sun/iface.cpp` и так же сделано поверх `tap-windows6`. Переходник между уровнями
не отсутствует, он встроен, оттого уровни устройств совпадать и не обязаны.

**Пара «Wintun (сетевой) ↔ Event Ports illumos (канальный)» проверена особо** — именно
её раскладка объявляла заведомо негодной, «ходить нечему». Сошлась: 5140/5140 в обе
стороны, 22.08.2026. Так же сошлась и пара Solaris ↔ NetBSD, снятая с другого конца.

**Как применять:** отказ пары НЕ объяснять несовпадением уровней, пока это не
доказано отдельно. Заранее объявленный «отказ по устройству» легко принять за
подтверждение, когда пара не сошлась совсем по другой причине.

## Как снимаются числа

Октеты берутся СЧЁТОМ СТРОК журнала обеих сторон, а не выводом `ping`:

    посредник: grep -c 'Прочитано из посредника'   (приём с переноса UDP)
    туннель:   grep -c 'Отправлено в туннель'      (выдача в устройство)

Отметка снимается ПРЯМО ПЕРЕД пуском, направления гоняются по одному. Мерить двумя
обращениями, между которыми идёт обмен, нельзя — отметки съезжают, и выходит «16
против 14» на ровном месте (обжигался 22.08.2026 на Debian).

## Три ловушки уборки, каждая даёт ложный отказ

1. **`TaskStop` снимает ssh, а не процесс на встречной машине.** Два клиента пережили
   уборку и держали устройство; следующий заход лёг с «описатель туннеля испорчен».
   Снимать по номеру: `ps -W` → `taskkill /PID` у MS Windows, `pgrep -x` → `kill` у POSIX.
2. **Мёртвый адрес валит следующий прогон.** Убитый клиент оставляет адрес за прежним
   устройством, и следующий заход другим драйвером получает `EEXIST` и ложится с
   «Tunnel device was left without an address» — НАПЕЧАТАВ ПЕРЕД ЭТИМ «Устройство
   туннеля поднято!». Снимать адрес перед прогоном.
3. **Уборку проверять счётом оставшегося**, а не возвратом команды. `taskkill` по
   номеру из чужого вывода молча не срабатывает, если номер взят не из того столбца.

## Стенд MS Windows: как собирать и запускать

Машина `forman@10.100.1.102` — **MSYS2**, а не Git Bash, как значилось в прежних
записях. Отсюда три условия, каждое стоило захода:

1. **`/mingw64/bin` обязателен в пути.** Без него образцы не запускаются ВОВСЕ и
   МОЛЧА: код возврата 127, вывода нет. Конвейер (`| tail`, `| grep`) этот код
   съедает, и выглядит всё так, будто программа отработала и вышла. Звать так:
   `export PATH=/mingw64/bin:/usr/bin:$PATH`.
2. **Генератор сборки — Ninja, а не Makefile.** `mingw32-make net-tun-client`
   отвечает «No rule to make target», хотя ключ `CMAKE_BUILD_NET_TUN_APP_SAMPLE`
   стоит `ON`. Собирать `ninja -j2 net-tun-client net-tun-server`.
3. **Вывод образца уходит в консоль Windows, минуя поток.** Перенаправление в файл
   даёт пустоту, `ssh -tt` открывает conhost и теряет его же. Судить о работе по
   делу: поднялось ли устройство, встал ли адрес, сошлись ли октеты.

Образцы кладутся в `build/samples/net/tun/` (не `build/sample/`).

## Снятие процессов и адресов у MS Windows

`TaskStop` и разрыв ssh процесс на машине НЕ снимают. Порядок такой:

    ps -W | grep -i net-tun | awk '{print $4}'   # номер именно ЧЕТВЁРТЫМ столбцом
    cmd //c "taskkill /PID <номер> /F"
    powershell -Command "Get-NetIPAddress -IPAddress 10.78.* | ForEach-Object { Remove-NetIPAddress ... }"

Адреса снимать обязательно: оставшийся за прежним устройством адрес даёт следующему
прогону `EEXIST`. Имя устройства из `ipconfig` брать бесполезно — оно приходит в
местной кодировке и в переписке нечитаемо; брать `InterfaceIndex` из PowerShell.

## Дефект «доклад об отказе без самого отказа» (закрыт 22.08.2026)

Ветвь «адрес устройству не назначен» докладывала об отказе честно — отклик состояния
FAILURE, отклик ошибки, запись в журнал, — но выводов из доклада не делала никаких:
событие на чтение заводилось следом БЕЗУСЛОВНО, признак итога оставался истинным, и
узел поднимался поверх негодного устройства. Посылки уходили в никуда, а наружу
вылезала претензия о подмене адреса устройства злоумышленником — обвинение
постороннее, уводящее разбор в сторону.

Соседняя ветвь того же места — отказ по описателю — поступает правильно: снимает
состояние, оставляет признак итога ложным, события не заводит. Здесь это было забыто.

**Опознаётся в журнале двумя строками подряд:**

    Ошибка события туннеля: Описание=Tunnel device was left without an address
    Устройство туннеля поднято!

**Три наблюдения, три системы, три движка, снятые независимо:**

| кто | пара | движок | причина в среде |
|-----|------|--------|-----------------|
| Гриша | Solaris ↔ NetBSD | Event Ports | у vnic нет привязки IPv4 |
| я | Windows/Wintun ↔ DragonFly | IOCP | адрес числится за прежним устройством |
| я | OpenIndiana сервером | Event Ports | у vnic awhv0 нет привязки IPv4 |

Средовая причина у каждого своя, дефект движка за всеми ими один. **Средовая причина
дефект ПРЯЧЕТ:** сняв её (`ifconfig <устройство> plumb`, снятие занятого адреса), пару
поднимаешь, она идёт, и до движка не докапываешься. Так я и упустил его первым разом.

Правки влиты во все пять движков коммитом `6fc5a34a4`.

**Замер до и после, движок IOCP, 22.08.2026.** Условие одно и то же: адрес `10.78.1.2`
оставлен за прежним устройством туннеля (клиент на tap-windows6 снят жёстко, адрес не
снялся), после чего заводится второй узел на тот же адрес драйвером Wintun.

| | до правки (03:18) | после правки (04:22) |
|---|---|---|
| доклад | `Tunnel device was left without an address` | тот же, дословно |
| следом | `Устройство туннеля поднято!` | `Событие туннеля выполнено с ошибкой` |
| подключений | 1 | **0** |
| исход | узел работал впустую | узел не поднялся |

Доклад в обоих прогонах один и тот же — разница ровно в том, чем он заканчивается.

**Проверка `IoTunnelRefusesCommitWithoutAddressTest`** (Гришина, `tests/net/io/tunnel.cpp`)
ищет отказ ПЕРЕБОРОМ ОБРАЗЦОВ АДРЕСА и потому утверждает не везде: у BSD на устройстве
точка-точка годен любой адрес, включая широковещательный и групповой — там она отступает
с внятной причиной. Случай MS Windows (адрес годен, но занят прежним устройством) перебором
образцов не воссоздаётся В ПРИНЦИПЕ: сколько ни перебирай, каждый образец свободен.
Воссоздавать надо не адрес, а ЗАНЯТОСТЬ.
