/**
 * @file tunnel.cpp
 * @date 2026-08-20
 *
 * @license{LicenseRef-AWH-1.0}
 *
 * @author Yuriy Lobarev
 *
 * @telegram{forman}
 * @phone{+7 (910) 983-95-90}
 *
 * @email forman@anyks.com
 * @site https://anyks.com
 *
 * @brief Проверки туннельных устройств асинхронного движка ввода-вывода
 *
 * @details Туннели прежде не проверялись ничем: ни сборка, ни наборы их не трогали
 *          вовсе. Дефекты движка, вскрытые работами по переносу под MS Windows
 *          (приём из устройства, привязка к очереди событий, установка опций,
 *          порядок сноса, снятие с учёта дескриптора, пакет чужого семейства),
 *          вернулись бы молча и узнал бы о них потребитель, а не набор
 *
 * @note Заведение туннельного устройства требует надзорных прав на ВСЕХ системах, а
 *       у MS Windows требует ещё и установленного стороннего драйвера. Оттого
 *       проверки эти пропускаются там, где устройство завести не вышло: опыт
 *       поставить не на чем, и объявлять это отказом движка было бы неверно
 *
 * @copyright Copyright © 2026
 *
 */

/**
 * Подключаем заголовочный файлы проекта
 */
#include "io.hpp"

/**
 * @brief Восполнение средств POSIX, отсутствующих у MS Windows
 *
 * @note Обращение `::closesocket` у MS Windows приходит из winsock2.h, а у прочих
 *       систем его нет вовсе - там имя это приводится к `::close` посредником
 *       тестового окружения. Без подключения проверка собиралась ОДНОЙ системой из
 *       девяти, и молчание это выдало себя лишь переносом на FreeBSD
 *
 */
#include "../../posix.hpp"
#include "../../../include/net/eth/eth.hpp"


/**
 * Стандартные модули
 */
#include <chrono>
#include <string>
#include <memory>
#include <vector>

/**
 * Для операционной системы MS Windows
 *
 * @note Заголовки эти принадлежат POSIX и у MS Windows отсутствуют:
 *       соответствующие им объявления приходят там из winsock2.h,
 *       подключаемого через единую точку sys/win32.hpp
 *
 */
#if _WIN32 || _WIN64
	/**
	 * Подключаем единую точку подключения системных заголовков MS Windows
	 */
	#include <sys/win32.hpp>
/**
 * Для всех остальных операционных систем
 */
#else
	/**
	 * Системные заголовочные файлы
	 */
	#include <arpa/inet.h>
	#include <netinet/in.h>
	#include <sys/socket.h>
#endif

/**
 * @brief Защита объявлений от макросов операционной системы
 *
 * @note Пара эта нужна по той же причине, что и в соседних проверках: заголовки
 *       MS Windows заводят макросами имена, какие движок называет своими членами
 *
 */
#include <sys/macro/suppress.hpp>

/**
 * Подключаем модуль волокон
 */
#include <sys/fiber.hpp>

/**
 * @brief Адреса концов испытуемого туннеля
 *
 * @details Берутся они из блока, отведённого под частные сети (RFC 1918), и берутся
 *          нарочно НЕ из тех блоков, какими пользуются проверки подключений:
 *          устройство туннеля заводится на всю машину, и совпадение адресов
 *          столкнуло бы проверки между собой через таблицу маршрутов
 *
 */
static constexpr const char * TUNNEL_LOCAL = "10.77.0.1";
static constexpr const char * TUNNEL_PEER  = "10.77.0.2";

/**
 * @brief Генерация случайного порта в диапазоне 49152-65535
 *
 * @note Помощник этот повторяет соседний из static.cpp намеренно: связывание у него
 *       внутреннее, и общего состояния между файлами не возникает
 *
 * @return случайный порт
 *
 */
static uint16_t tunnelPort() noexcept {
	/**
	 * Нижняя и верхняя границы разряда портов, отведённого проверкам
	 *
	 * @details Разряд вычисляется ОДИН раз за процесс и лежит ВНЕ того, каким
	 *          распоряжается система: подробности у `__awh_test_port_range__`
	 */
	static const std::pair <uint16_t, uint16_t> range = [](void) noexcept -> std::pair <uint16_t, uint16_t> {
		// Границы разряда, отводимого проверкам
		uint16_t begin = 0, end = 0;
		// Выполняем выбор разряда портов для проверок
		__awh_test_port_range__(begin, end);
		// Выводим выбранный разряд портов
		return std::make_pair(begin, end);
	}();
	// Нижняя граница разряда портов, отведённого проверкам
	const uint16_t BEGIN = range.first;
	// Верхняя граница разряда портов, отведённого проверкам
	const uint16_t END = range.second;
	// Счётчик выданных портов
	static uint16_t count = 0;
	// Выводим следующий порт разряда
	return static_cast <uint16_t> (BEGIN + ((static_cast <uint32_t> (::getpid()) + (count++) * 7) % (END - BEGIN)));
}


/**
 * @brief Функция проверки надзорных прав у текущего процесса
 *
 * @details Заведение туннельного устройства требует надзорных прав на ВСЕХ системах, и
 *          это единственная причина отказа, какую проверка вправе принять за негодность
 *          окружения. Всё прочее - отсутствие драйвера, неверная настройка, дефект
 *          движка - от проверки не скрывается: она объявляет отказ и называет обе
 *          возможные причины
 *
 * @warning Прежде проверки пропускались по одному лишь пустому опознавателю события, а
 *          он пуст при ЛЮБОЙ причине. Обратный опыт это и вскрыл: возвращённый в код
 *          дефект настройки драйвера обратил обе проверки в ПРОПУСК вместо отказа.
 *          Пропуск - это молчаливое отключение, и стеречь проверка в таком виде не
 *          стерегла ничего
 *
 * @warning Заводить устройство пробой напрямую, минуя движок, для этого НЕ ГОДИТСЯ, и
 *          это тоже установлено обратным опытом: проба шла бы тем же кодом заведения,
 *          что и движок, и на том же дефекте отвечала бы тем же отказом. Проба обязана
 *          спрашивать окружение, а не разбираемый код
 *
 * @return признак наличия надзорных прав
 *
 */
static bool tunnelPrivileged() noexcept {
	/**
	 * Для операционной системы MS Windows
	 */
	#if _WIN32 || _WIN64
		// Описатель маркера доступа текущего процесса
		HANDLE token = nullptr;
		// Если маркер доступа текущего процесса получить не удалось
		if(!::OpenProcessToken(::GetCurrentProcess(), TOKEN_QUERY, &token))
			// Выводим отсутствие надзорных прав
			return false;
		// Сведения о повышении прав процесса
		TOKEN_ELEVATION elevation{};
		// Размер полученных сведений
		DWORD size = static_cast <DWORD> (sizeof(elevation));
		// Признак наличия надзорных прав
		bool result = false;
		// Если сведения о повышении прав получены
		if(::GetTokenInformation(token, TokenElevation, &elevation, sizeof(elevation), &size))
			// Запоминаем признак наличия надзорных прав
			result = (elevation.TokenIsElevated != 0);
		// Выполняем закрытие маркера доступа
		::CloseHandle(token);
		// Выводим признак наличия надзорных прав
		return result;
	/**
	 * Для всех остальных операционных систем
	 */
	#else
		// Выводим признак работы от имени надзорного пользователя
		return (::geteuid() == 0);
	#endif
}

/**
 * @brief Драйвер устройств туннеля, задаваемый проверкой
 *
 * @note Свой перечень заведён нарочно: `awh::eth::iface_t::driver_t` объявлен ТОЛЬКО
 *       под MS Windows, и ссылка на него в теле проверки не собралась бы у прочих
 *       систем вовсе
 *
 */
enum class tunnel_driver_t : uint8_t {
	AUTO   = 0x00, // Выбор драйвера отдан модулю
	WINTUN = 0x01, // Кольцо в общей памяти, только сетевой уровень
	TAP    = 0x02  // Дескриптор файла, канальный уровень с адресами
};

/**
 * @brief Закрепление драйвера, каким заводятся устройства туннеля
 *
 * @details Драйверов у MS Windows два, и несут они РАЗНОЕ: Wintun переносит пакеты
 *          сетевого уровня кольцом в общей с драйвером памяти, tap-windows6 - кадры
 *          канального уровня через дескриптор файла. Выбор по умолчанию (`AUTO`)
 *          отдан модулю, и модуль решает его тем, лежит ли `wintun.dll` рядом с
 *          двоичным файлом
 *
 * @warning Оттого весь набор ВСЕГДА шёл по tap-windows6, и не по замыслу, а потому что
 *          библиотеки рядом не оказалось: путь Wintun не проверялся НИ РАЗУ. Положи
 *          кто-нибудь `wintun.dll` рядом - и весь набор молча уехал бы на другой путь,
 *          а расхождения никто бы не заметил. Установлено сессией стороны Windows
 *          23.08.2026: когда путь Wintun впервые запустили руками, нашлось три дефекта
 *          подряд
 *
 * @note Оттого драйвер задаётся ЯВНО. Проверка обязана знать, что именно она проверяет,
 *       а не выводить это из состава каталога сборки
 *
 * @note Прочих систем это не касается вовсе: драйвер там один
 *
 * @param io     объект сетевого движка, которому задаётся драйвер
 * @param driver драйвер устройств туннеля для закрепления
 *
 */
/**
 * @brief Число устройств драйвера tap-windows6, стоящих на машине
 *
 * @details Устройства эти ставит УСТАНОВЩИК ДРАЙВЕРА, и число их конечно - движок лишь
 *          отбирает свободное среди готовых. Тем MS Windows и отличается от систем
 *          POSIX, где устройство заводится по требованию и считать нечего
 *
 * @note Без этого счёта проверки на несколько устройств ОТКАЗЫВАЛИ на машине, где
 *       устройство одно, изображая дефект движка нехваткой оснащения. Замерено на
 *       стенде Windows ARM64 24.08.2026: там стоит единственный адаптер, и
 *       IoTunnelRaisesManyDevicesTest падал на втором туннеле из восьми
 *
 * @return число отысканных устройств
 *
 */
[[maybe_unused]] static uint16_t tunnelDevicesCount() noexcept {
	/**
	 * Для операционной системы MS Windows
	 */
	#if _WIN32 || _WIN64
		// Размер буфера под состав устройств машины
		ULONG size = 0;
		// Спрашиваем потребный размер буфера
		if(::GetAdaptersAddresses(AF_UNSPEC, GAA_FLAG_SKIP_ANYCAST | GAA_FLAG_SKIP_MULTICAST | GAA_FLAG_SKIP_DNS_SERVER, nullptr, nullptr, &size) != ERROR_BUFFER_OVERFLOW)
			// Выводим нулевое число устройств: состав машины неизвестен
			return 0;
		// Буфер под состав устройств машины
		std::vector <uint8_t> buffer(static_cast <size_t> (size), 0);
		// Объект списка устройств машины
		PIP_ADAPTER_ADDRESSES addresses = reinterpret_cast <PIP_ADAPTER_ADDRESSES> (buffer.data());
		// Выполняем получение состава устройств машины
		if(::GetAdaptersAddresses(AF_UNSPEC, GAA_FLAG_SKIP_ANYCAST | GAA_FLAG_SKIP_MULTICAST | GAA_FLAG_SKIP_DNS_SERVER, nullptr, addresses, &size) != ERROR_SUCCESS)
			// Выводим нулевое число устройств
			return 0;
		// Число отысканных устройств туннеля
		uint16_t found = 0;
		/**
		 * Перебираем устройства машины
		 */
		for(PIP_ADAPTER_ADDRESSES item = addresses; item != nullptr; item = item->Next){
			// Если описание устройства не получено
			if(item->Description == nullptr)
				// Переходим к следующему устройству
				continue;
			// Если описание устройства принадлежит драйверу tap-windows6
			if(::wcsstr(item->Description, L"TAP-Windows") != nullptr)
				// Увеличиваем счётчик отысканных устройств
				found++;
		}
		// Выводим число отысканных устройств
		return found;
	/**
	 * Для всех остальных операционных систем
	 */
	#else
		// Устройств этого драйвера у прочих систем нет вовсе
		return 0;
	#endif
}

/**
 * @brief Признак доступности драйвера Wintun
 *
 * @details Драйвер этот заводит адаптер ПО ТРЕБОВАНИЮ и при выходе процесса уносит его
 *          с собою, оттого числом устройств он не ограничен вовсе - в отличие от
 *          tap-windows6, устройства которого ставит установщик драйвера
 *
 * @note Библиотека ищется ТЕМ ЖЕ порядком, каким её ищет сам движок: рядом с двоичным
 *       файлом и в системном каталоге. Иначе проверка судила бы о доступности не по
 *       тому, что доступно движку
 *
 * @return признак доступности драйвера Wintun
 *
 */
[[maybe_unused]] static bool tunnelWintunAvailable() noexcept {
	/**
	 * Для операционной системы MS Windows
	 */
	#if _WIN32 || _WIN64
		// Выполняем загрузку библиотеки драйвера Wintun
		HMODULE handle = ::LoadLibraryExW(L"wintun.dll", nullptr, LOAD_LIBRARY_SEARCH_APPLICATION_DIR | LOAD_LIBRARY_SEARCH_SYSTEM32);
		// Если библиотека драйвера загружена
		if(handle != nullptr){
			// Освобождаем загруженную библиотеку
			::FreeLibrary(handle);
			// Выводим признак доступности драйвера
			return true;
		}
		// Выводим признак недоступности драйвера
		return false;
	/**
	 * Для всех остальных операционных систем
	 */
	#else
		// Драйвера этого у прочих систем нет вовсе
		return false;
	#endif
}

static void tunnelPinDriver([[maybe_unused]] awh::engine::io_t * io, [[maybe_unused]] const tunnel_driver_t driver) noexcept {
	/**
	 * Для операционной системы MS Windows
	 */
	#if _WIN32 || _WIN64
		// Если объект сетевого движка не передан
		if(io == nullptr)
			// Выходим из функции
			return;
		/**
		 * Определяем запрошенный драйвер
		 */
		switch(static_cast <uint8_t> (driver)){
			// Если запрошен драйвер Wintun
			case static_cast <uint8_t> (tunnel_driver_t::WINTUN):
				// Закрепляем драйвер Wintun
				io->setDriver(awh::eth::iface_t::driver_t::WINTUN);
			break;
			// Если запрошен драйвер tap-windows6
			case static_cast <uint8_t> (tunnel_driver_t::TAP):
				// Закрепляем драйвер tap-windows6
				io->setDriver(awh::eth::iface_t::driver_t::TAP);
			break;
			// Для всех прочих случаев отдаём выбор модулю
			default: io->setDriver(awh::eth::iface_t::driver_t::AUTO);
		}
	#endif
}

/**
 * @brief Закрепление драйвера, способного поднять нужное число устройств
 *
 * @details Устройства tap-windows6 ставит установщик драйвера, и число их конечно, а
 *          Wintun заводит адаптер по требованию. Оттого проверке, которой нужно
 *          несколько устройств, драйвер выбирается ПО СОСТАВУ МАШИНЫ: хватает
 *          постоянных - берётся tap-windows6, не хватает - берётся Wintun
 *
 * @warning Прежде здесь стоял жёсткий tap-windows6, и на машине с единственным
 *          устройством проверки эти отступали пропуском, хотя Wintun поднял бы их
 *          беспрепятственно. Замерено на стенде Windows ARM64 24.08.2026
 *
 * @note Туннели этих проверок несут пакеты СЕТЕВОГО уровня, а их Wintun переносит
 *       наравне с tap-windows6. Будь нужен канальный уровень с адресами, замена была
 *       бы неравноценной и делать её было бы нельзя
 *
 * @param io    объект сетевого движка
 * @param count потребное число устройств
 * @return      название выбранного драйвера для доклада
 *
 */
static const char * tunnelPinForCount([[maybe_unused]] awh::engine::io_t * io, [[maybe_unused]] const uint16_t count) noexcept {
	/**
	 * Для операционной системы MS Windows
	 */
	#if _WIN32 || _WIN64
		// Если постоянных устройств на машине хватает
		if(tunnelDevicesCount() >= count){
			// Закрепляем драйвер tap-windows6
			tunnelPinDriver(io, tunnel_driver_t::TAP);
			// Выводим название выбранного драйвера
			return "tap-windows6";
		}
		// Закрепляем драйвер Wintun: устройства он заводит по требованию
		tunnelPinDriver(io, tunnel_driver_t::WINTUN);
		// Выводим название выбранного драйвера
		return "Wintun";
	/**
	 * Для всех остальных операционных систем
	 */
	#else
		// Драйверов у прочих систем нет вовсе
		return "";
	#endif
}


/**
 * @brief Охранник узла туннеля: снос обязан пройти при любом исходе
 *
 * @details Утверждение, не оправдавшееся посреди проверки, возвращает управление
 *          немедленно, и снос узла, стоящий ниже по телу, не исполняется вовсе.
 *          У систем, где устройство переживает процесс (MS Windows, системы Sun),
 *          на нём остаётся назначенный адрес - и СЛЕДУЮЩИЙ прогон получает
 *          устройство занятым
 *
 * @warning Беда эта САМОВОСПРОИЗВОДЯЩАЯСЯ: отказ оставляет остаток, остаток
 *          обеспечивает отказ, и набор из круга сам не выходит. Установлено на
 *          стенде Windows 21.08.2026 - три прогона подряд красными по одной
 *          причине, каждый готовил её следующему
 *
 * @note Разрушение охранника происходит и при обычном выходе, и при отказе
 *       утверждения: снос узла в обоих случаях один и тот же, а повторный снос
 *       движок принимает без последствий
 *
 */
struct TunnelGuard {
	// Объект движка событий
	awh::engine::io_t * io;
	// Идентификатор узла туннеля
	awh::event::id_t id;
	/**
	 * @brief Конструктор охранника узла туннеля
	 *
	 * @param io объект движка событий
	 * @param id идентификатор узла туннеля
	 *
	 */
	TunnelGuard(awh::engine::io_t * io, const awh::event::id_t id) noexcept : io(io), id(id) {}
	/**
	 * @brief Деструктор охранника узла туннеля
	 *
	 */
	~TunnelGuard() noexcept {
		// Если узел туннеля заведён
		if((this->io != nullptr) && (this->id > 0))
			// Сносим узел туннеля вместе с его настройкой
			this->io->destroy(this->id);
	}
};

/**
 * @brief Функция проверки готовности окружения к заведению туннелей
 *
 * @details Системы Sun устройств уровня IP не заводят вовсе: там связь заводится
 *          административно (`dladm create-vnic` поверх etherstub плюс `ipadm create-ip`),
 *          и движок отыскивает свободную перебором имён. Имя связи ему НЕ НАЗЫВАЕТСЯ:
 *          назвав одно, проверка получила бы одну связь на все туннели разом
 *
 * @warning Отсутствие подготовленных связей у систем Sun - негодность окружения, а не
 *          дефект движка: завести их проверка не вправе, это настройка стенда. Спрашивается
 *          она у самой системы, а не у разбираемого кода
 *
 * @note Приставка имени берётся из краткого имени библиотеки - ровно оттуда же, откуда
 *       берёт её сам движок. Второго места, где имя писалось бы руками, нет
 *
 * @param count число связей, потребное проверке
 * @return      признак готовности окружения
 *
 */
static bool tunnelReady([[maybe_unused]] const uint16_t count) noexcept {
	/**
	 * Для операционных систем Sun
	 */
	#if __sun__
		// Число отысканных связей
		uint16_t found = 0;
		/**
		 * Перебираем связи, заведённые распорядителем машины
		 */
		for(uint16_t i = 0; (i < 0x100) && (found < count); i++){
			// Приводим краткое имя библиотеки к нижнему регистру
			std::string prefix(AWH_SHORT_NAME "_tun");
			// Переводим приставку в нижний регистр
			for(char & letter : prefix)
				// Переводим очередной знак приставки
				letter = static_cast <char> (::tolower(static_cast <uint8_t> (letter)));
			// Формируем путь к очередной связи
			const std::string path = ("/dev/net/" + prefix + std::to_string(i));
			// Если связь заведена, считаем её
			if(::access(path.c_str(), F_OK) == 0)
				// Увеличиваем счётчик отысканных связей
				found++;
		}
		// Выводим признак готовности окружения
		return (found >= count);
	/**
	 * Для операционной системы MS Windows
	 */
	#elif _WIN32 || _WIN64
		/**
		 * Выводим признак готовности окружения
		 *
		 * @note Драйвер Wintun выручает там, где устройств tap-windows6 не хватает: он
		 *       заводит адаптер по требованию и числом устройств не ограничен. Оттого
		 *       доступность его означает готовность к любому их числу
		 */
		return ((tunnelDevicesCount() >= count) || tunnelWintunAvailable());
	/**
	 * Для всех остальных операционных систем
	 */
	#else
		// Устройства движок заводит сам, готовить нечего
		return true;
	#endif
}

/**
 * @brief Тест полного оборота жизни туннельного устройства
 *
 * @details Оборот проходится ДВАЖДЫ подряд одним и тем же движком, и это
 *          существенно. Половина дефектов, вскрытых работами по переносу, выдавала
 *          себя не первым заведением, а вторым: дескриптор, не снятый с учёта при
 *          сносе, оставлял за собой запись, и следующее устройство получало отказ
 *          привязки к очереди событий
 *
 * @note Проверяется здесь именно СОГЛАСИЕ движка на каждом шаге, а не обмен: обмен
 *       требует встречной стороны, и разбирается он проверкой соседней
 *
 */
TEST_F(IoFixture, IoTunnelLifecycleTest){
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	/**
	 * Проходим оборот жизни устройства дважды
	 */
	for(uint8_t round = 0; round < 2; round++){
		// Роспись оборота для сообщений об отказе
		const std::string sign = std::string("оборот=") + std::to_string(round + 1);
		/**
		 * Заводим событие туннеля
		 *
		 * @note Здесь заводится ТОЛЬКО узел: само устройство движок заводит фиксацией
		 *       настроек, когда имя ему уже названо. Пустой опознаватель означает, что
		 *       движок не записал узла, а отказ заведения устройства спрашивается ниже,
		 *       у commit
		 */
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Ставим охранника узла: снос обязан пройти и при отказе утверждения
		const TunnelGuard guard(this->_io.get(), tid);
		// Узел обязан завестись: устройство заводится позже, фиксацией настроек
		ASSERT_GT(tid, 0u) << sign << ": движок не записал узел туннеля";
		/**
		 * Устанавливаем опции события туннеля
		 *
		 * @details Отказ здесь был дефектом движка: разбор опций у туннеля шёл общим
		 *          путём с каталогами, файлами и каналами, а тот отвечал отказом
		 *          10038 - «дескриптор не является сокетом». Туннель им и не является
		 */
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC)) << sign;
		// Устанавливаем адреса концов туннеля
		ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL)) << sign;
		ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER)) << sign;
		// Фиксируем событие туннеля
		// Устройство заводится фиксацией настроек: годность окружения проверена выше пробой
		ASSERT_TRUE(this->_io->commit(tid)) << sign << ": права есть, а устройство не заведено - нет драйвера туннельных устройств либо дефект движка";
		// Запускаем событие туннеля
		ASSERT_TRUE(this->_io->launch(tid)) << sign;
		/**
		 * Спрашиваем адрес У САМОЙ СИСТЕМЫ, а не у согласия движка
		 *
		 * @details Утверждение ASSERT_TRUE(setAddress(...)) говорит лишь то, что вызов
		 *          ответил согласием. Дошёл ли адрес до устройства, оно НЕ говорит вовсе,
		 *          и проверка остаётся зелёной при устройстве без адреса
		 *
		 * @warning Внутримашинная проверка эту разницу увидеть НЕ МОЖЕТ: оба конца - одно
		 *          и то же устройство, и обмен через него идёт даже тогда, когда адреса
		 *          на нём нет. Вскрылось это первой же попыткой свести две машины
		 *
		 * @note Разницу нашёл агент стороны Windows 21.08.2026: там за все 3.8 секунды
		 *       жизни устройства запрошенный адрес не появлялся НИ РАЗУ, а проверка
		 *       проходила. У систем POSIX адрес ставится честно - проверено слежением на
		 *       стенде Alpine, - но утверждать это обязана сама проверка, а не мы
		 */
		{
			// Получаем название заведённого устройства туннеля
			const std::string iface = this->_io->getIface(tid);
			// Название устройства обязано быть известно
			ASSERT_FALSE(iface.empty()) << sign;
			// Создаём объект работы с сетью
			awh::eth_t eth(this->_fmk.get(), this->_log.get());
			/**
			 * Спрашиваем, чьим стал НАШ адрес, а не какой адрес у устройства
			 *
			 * @details Опрос адреса устройства отдаёт ПЕРВЫЙ из его адресов, и на
			 *          системах, где устройство постоянно, первым легко оказывается
			 *          чужой - остаток прежнего прогона. Сличение первого адреса с
			 *          запрошенным обвиняло тогда движок в том, чего он не делал
			 *
			 * @warning У систем POSIX эта разница НЕ ВИДНА вовсе: устройство уходит
			 *          вместе с процессом, адрес у него всегда один, и сличение
			 *          первого выглядит безупречным. Вскрылось на MS Windows, где
			 *          переходник tap-windows6 переживает прогон и несёт несколько
			 *          адресов разом (нашёл агент стороны Windows 21.08.2026)
			 *
			 * @note Обратный вопрос свободен от этой слепоты: он ищет НАШ адрес среди
			 *       всех адресов машины и отвечает, какому устройству тот принадлежит.
			 *       Лишние адреса на устройстве ответу не мешают
			 */
			// Объект разбора запрошенного адреса устройства
			awh::net_addr_t expected(this->_fmk.get(), this->_log.get());
			// Разбираем запрошенный адрес устройства
			ASSERT_TRUE(expected.parse(TUNNEL_LOCAL)) << sign;
			// Объект запрошенного адреса устройства
			std::unique_ptr <awh::net::addr_t> address = std::make_unique <awh::net::addr_net_ipv4_t> ();
			/**
			 * Переносим разобранный адрес в объект нужного вида
			 *
			 * @note Порядок октетов взят LITTLE намеренно: он совпадает с тем, каким
			 *       адрес лежит в структурах системы, и годен для прямого сличения
			 */
			awh_cast <awh::net::addr_net_ipv4_t *> (address.get())->address = expected.v4(awh::net_addr_t::endian_t::LITTLE);
			// Спрашиваем, какому устройству принадлежит запрошенный адрес
			const std::string owner = eth.iface.name(address.get());
			// Запрошенный адрес обязан принадлежать заведённому устройству
			ASSERT_EQ(owner, iface)
				<< sign << ": движок согласился поставить адрес " << TUNNEL_LOCAL
				<< ", а система числит его "
				<< (owner.empty() ? std::string("ни за одним устройством") : (std::string("за устройством \"") + owner + "\""));
		}
		/**
		 * Крутим опрос недолго
		 *
		 * @details Обороты эти нужны не ради пакетов, а ради самой привязки: отказ
		 *          привязки устройства к очереди событий выдавал себя именно здесь -
		 *          опрос возвращал отказ с кодом 6, «неверный описатель»
		 */
		const auto start = std::chrono::steady_clock::now();
		while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - start).count() < 300))
			ASSERT_TRUE(this->_io->poll(50)) << sign;
		// Снимаем событие туннеля
		ASSERT_TRUE(this->_io->destroy(tid)) << sign;
	}
	// Сворачиваем движок
	ASSERT_TRUE(this->_io->deinitialize());
}

/**
 * @brief Тест переноса пакета из туннеля наружу
 *
 * @details Проверяется весь путь целиком: пакет, посланный на адрес встречной стороны
 *          туннеля, забирается системой в устройство, читается движком, заворачивается
 *          посредником и уходит несущей связью - той самой, на другом конце которой
 *          стоит сервер этой же проверки. Дошедшее сличается с пакетом IPv4
 *
 * @details Проверка эта закрывает дефект настройки драйвера у MS Windows: драйвер
 *          tap-windows6 в режиме переноса пакетов сетевого уровня отбирает их по
 *          настроенной сети, а настраивался он числами, зашитыми в коде. Обмен по
 *          IPv4 через устройство не шёл ВОВСЕ, и выдавала себя беда лишь молчанием
 *          туннеля - ни отказа, ни записи в журнале. Установлено щупом на стенде
 *          Windows ARM64 20.08.2026
 *
 * @note Несущая связь заведена на петле: проверке нужен не обмен между машинами, а
 *       доказательство того, что пакет через туннель ПРОШЁЛ
 *
 */
TEST_F(IoFixture, IoTunnelCarriesPacketTest){
	// Счётчик пакетов, дошедших несущей связью
	uint16_t carried = 0;
	// Код отказа отправки дейтаграммы в устройство
	int32_t failure = 0;
	// Признак того, что дошедшее оказалось пакетом IPv4
	bool addressed = false;
	// Порт несущей связи
	const uint16_t bearer = tunnelPort();
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	/**
	 * Заводим событие туннеля
	 *
	 * @note Здесь заводится ТОЛЬКО узел: устройство движок заводит фиксацией
	 *       настроек. Пустой опознаватель означает, что движок не записал
	 *       узла, а отказ заведения устройства спрашивается ниже, у commit
	 */
	const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
	// Ставим охранника узла: снос обязан пройти и при отказе утверждения
	const TunnelGuard guard(this->_io.get(), tid);
	// Узел обязан завестись: устройство заводится позже, фиксацией настроек
	ASSERT_GT(tid, 0u) << "движок не записал узел туннеля";
	// Заводим событие посредника
	const awh::event::id_t mid = this->_io->event(awh::event::node_t::MEDIATOR, awh::event::family_t::IPV4);
	ASSERT_GT(mid, 0u);
	// Заводим событие сервера несущей связи
	const awh::event::id_t sid = this->_io->event(
		awh::event::node_t::SERVER, awh::event::family_t::IPV4,
		awh::event::type_t::DATAGRAM, awh::event::protocol_t::UDP
	);
	ASSERT_GT(sid, 0u);
	// Заводим событие клиента несущей связи
	const awh::event::id_t cid = this->_io->event(
		awh::event::node_t::CLIENT, awh::event::family_t::IPV4,
		awh::event::type_t::DATAGRAM, awh::event::protocol_t::UDP
	);
	ASSERT_GT(cid, 0u);
	// Устанавливаем опции событий
	ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
	ASSERT_TRUE(this->_io->setOptions(sid, awh::event::options::NO_SIGPIPE | awh::event::options::REUSE_ADDR | awh::event::options::NO_IO_BLOCK));
	ASSERT_TRUE(this->_io->setOptions(cid, awh::event::options::NO_SIGPIPE | awh::event::options::NO_IO_BLOCK));
	// Устанавливаем адреса концов туннеля
	ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL));
	ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER));
	// Устанавливаем встречную сторону посреднику
	ASSERT_TRUE(this->_io->setTarget(mid, TUNNEL_PEER));
	// Устанавливаем адрес и порт несущей связи
	ASSERT_TRUE(this->_io->setAddress(sid, awh::event::address_t::IPV4, "127.0.0.1"));
	ASSERT_TRUE(this->_io->setSourcePort(sid, bearer));
	ASSERT_TRUE(this->_io->setTarget(cid, "127.0.0.1"));
	ASSERT_TRUE(this->_io->setTargetPort(cid, bearer));
	// Связываем посредника с клиентом несущей связи
	ASSERT_TRUE(this->_io->splice(mid, cid));
	/**
	 * Считаем прочитанное посредником
	 *
	 * @note Отклик чтения ставится ПОСРЕДНИКУ, а не серверу несущей связи: у события
	 *       сервера отклик этот не заводится вовсе - тот отвечает отказом «отклик чтения
	 *       данных этому виду события задать нельзя», - а посредник как раз и держит то,
	 *       что прочитано из туннеля
	 *
	 * @note Сличается именно ВИД прочитанного, а не его размер: пакет собирает система, и
	 *       утверждать его длину значило бы утверждать поведение системы
	 */
	this->_io->on(mid, static_cast <awh::engine::callback::read_t> (
		[&carried, &addressed]([[maybe_unused]] const awh::event::id_t mid, const uint8_t * data, const size_t size) noexcept -> void {
			// Считаем дошедший пакет
			carried++;
			// Если дошедшее оказалось пакетом IPv4
			if((data != nullptr) && (size > 0) && ((data[0] >> 4) == 4))
				// Запоминаем опознание пакета IPv4
				addressed = true;
		}
	));
	// Фиксируем события
	// Устройство заводится фиксацией настроек: годность окружения проверена выше пробой
	ASSERT_TRUE(this->_io->commit(tid)) << "права есть, а устройство не заведено - нет драйвера туннельных устройств либо дефект движка";
	ASSERT_TRUE(this->_io->commit(sid));
	ASSERT_TRUE(this->_io->commit(cid));
	ASSERT_TRUE(this->_io->commit(mid));
	/**
	 * Подключаем несущую связь к своему же серверу
	 *
	 * @note Без этого дейтаграммный сокет несущей связи остаётся без встречной стороны:
	 *       очередь принимает данные и отчитывается об отправке, а наружу не уходит
	 *       ничего
	 */
	ASSERT_TRUE(this->_io->connect(cid));
	/**
	 * Запускаем события
	 *
	 * @note Посредник запуску вручную НЕ подлежит и отвечает на него отказом: ведёт его
	 *       та связь, с которой он соединён, и своей жизни у него нет
	 */
	ASSERT_TRUE(this->_io->launch(sid));
	ASSERT_TRUE(this->_io->launch(cid));
	ASSERT_TRUE(this->_io->launch(tid));
	/**
	 * Поднимаем устройство туннеля
	 *
	 * @details Движок устройство заводит и адреса ему назначает, но признака UP не
	 *          ставит: без него устройство остаётся опущенным, маршрута через него
	 *          нет и пакету идти некуда. У MS Windows драйвер поднимает устройство
	 *          сам, оттого проверка там проходила и без этого; у систем POSIX пакет
	 *          не доходил вовсе, и выдавала себя беда только счётчиком «дошло=0»
	 */
	{
		// Получаем название устройства туннеля
		const std::string iface = this->_io->getIface(tid);
		// Название устройства обязано быть известно
		ASSERT_FALSE(iface.empty());
		// Создаём объект работы с сетью
		awh::eth_t eth(this->_fmk.get(), this->_log.get());
		// Поднимаем устройство туннеля
		ASSERT_TRUE(eth.iface.flag(iface, awh::event::eth_flag_t::UP, awh::event::mode_t::ENABLED));
	}
	/**
	 * Шлём дейтаграмму на адрес встречной стороны туннеля
	 *
	 * @details Обмен ведётся сокетом системы, а не движком: проверке нужен пакет,
	 *          пришедший В туннель извне движка, - иначе она проверяла бы движок
	 *          сам собою
	 */
	awh::net::socket_t fd = static_cast <awh::net::socket_t> (::socket(AF_INET, SOCK_DGRAM, IPPROTO_UDP));
	ASSERT_NE(fd, awh::net::invalid_socket_t);
	// Адрес встречной стороны туннеля
	struct sockaddr_in peer{};
	// Устанавливаем семейство адреса встречной стороны
	peer.sin_family = AF_INET;
	// Устанавливаем порт встречной стороны
	peer.sin_port = htons(9);
	// Устанавливаем адрес встречной стороны
	peer.sin_addr.s_addr = ::inet_addr(TUNNEL_PEER);
	// Полезная нагрузка отправляемой дейтаграммы
	const char payload[] = "AWH-TUNNEL-PROBE";
	// Отсчёт времени ожидания
	const auto start = std::chrono::steady_clock::now();
	/**
	 * Крутим опрос, подсылая дейтаграммы
	 */
	while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - start).count() < 3000) && (carried == 0)){
		// Отправляем дейтаграмму на адрес встречной стороны туннеля
		/**
		 * Отказ отправки запоминаем
		 *
		 * @warning Прежде итог отправки не смотрели вовсе, и это скрывало причину: при
		 *          недоступном маршруте система отвечает отказом сразу, а проверка
		 *          отчитывалась одним лишь «дошло=0» - неотличимо от беды в переносе
		 */
		if(::sendto(fd, payload, sizeof(payload) - 1, 0, reinterpret_cast <struct sockaddr *> (&peer), sizeof(peer)) < 0)
			// Запоминаем код отказа отправки
			failure = errno;
		// Выполняем оборот опроса
		ASSERT_TRUE(this->_io->poll(100));
	}
	// Закрываем сокет отправки
	::closesocket(fd);
	// Роспись итога для сообщений об отказе
	const std::string sign = std::string("дошло=") + std::to_string(carried) + " опознано=" + (addressed ? "да" : "нет") +
	                         " отказ отправки=" + ((failure != 0) ? ::strerror(failure) : "нет");
	/**
	 * Отказ отправки означает негодное окружение, а не дефект движка
	 *
	 * @details Пакет к встречной стороне туннеля идёт через устройство, и пакетный
	 *          фильтр системы вправе его не пустить: у стенда FreeBSD правила ipfw
	 *          разрешают обмен лишь сетевой связью и петлёй, а всё прочее отвергают
	 *          отказом «Permission denied» ещё на отправке. Пакет тогда не доходит
	 *          даже до устройства, и проверять движку нечего
	 *
	 * @warning Пробой окружения здесь служит errno самой системы, а не поведение
	 *          разбираемого кода: спрашивается тот, кто отказ и вынес. Правила
	 *          заслона проверка не трогает и трогать не вправе - они принадлежат
	 *          хозяину стенда
	 */
	/**
	 * Негодностью окружения признаются ТОЛЬКО запрет и недостижимость
	 *
	 * @warning Прежде сюда попадал всякий код отказа, и это прятало дефект: у драйвера
	 *          Wintron под MS Windows отправка отвечает `EAGAIN` три секунды подряд,
	 *          потому что система держит её в ожидании разрешения адреса встречной
	 *          стороны, а дать его некому - устройство там чисто сетевого уровня, и
	 *          маршрут на встречную сторону движок не заводит. «Пока не могу» - это
	 *          незавершённая настройка, то есть ровно то, что проверка обязана ловить,
	 *          а не «мне не разрешили»
	 *
	 * @note Установлено Андреем на стенде Windows ARM64 20.08.2026 сличением двух
	 *       драйверов: под tap-windows6 отправка проходит, под Wintun отказывает
	 */
	const bool forbidden = ((failure == EACCES) || (failure == EPERM) ||
	                        (failure == ENETUNREACH) || (failure == EHOSTUNREACH));
	// Если пакет не дошёл, а система назвала запрет либо недостижимость
	if((carried == 0) && forbidden){
		// Сворачиваем движок
		this->_io->deinitialize();
		// Пропускаем проверку
		GTEST_SKIP() << "система запретила пакету идти в устройство туннеля (" << ::strerror(failure) << "): заслон либо отсутствие маршрута";
	}
	// Через туннель обязан пройти хотя бы один пакет
	ASSERT_GE(carried, 1) << sign;
	// Дошедшее обязано оказаться пакетом IPv4
	ASSERT_TRUE(addressed) << sign;
	// Сворачиваем движок
	ASSERT_TRUE(this->_io->deinitialize());
}

/**
 * @brief Проверка обратного направления туннеля: пакет, вписанный движком
 *
 * @details Соседняя `IoTunnelCarriesPacketTest` доказывает лишь направление ЧТЕНИЯ -
 *          «система → устройство → движок». Направление записи - «движок → устройство
 *          → система» - не было закреплено ни одной проверкой НИ НА ОДНОЙ системе, и
 *          половина договора туннеля жила непроверенной
 *
 * @details Пакет здесь собирается руками, а не берётся у системы: движку надлежит
 *          отдать устройству ровно то, что ему подали, - голый пакет IP без всякой
 *          метки семейства впереди. Собранное принимает обычный дейтаграммный сокет
 *          системы, то есть проверяется весь путь целиком, вплоть до стека IP
 *
 * @note Установлено Андреем на стенде Windows ARM64 21.08.2026: путь этот исправен и
 *       под tap-windows6, и под Wintun, - но проверки на него не было, и три дня
 *       разбора ушли на мусор стенда, какой такая проверка отсеяла бы сразу
 *
 */
TEST_F(IoFixture, IoTunnelAcceptsWrittenPacketTest){
	// Порт приёмника собранного пакета
	const uint16_t port = tunnelPort();
	// Полезная нагрузка вписываемого пакета
	static constexpr const char PAYLOAD[] = "AWH-INJECT";
	// Размер полезной нагрузки
	static constexpr size_t PAYLOAD_SIZE = sizeof(PAYLOAD) - 1;
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	// Заводим событие туннеля
	const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
	// Ставим охранника узла: снос обязан пройти и при отказе утверждения
	const TunnelGuard guard(this->_io.get(), tid);
	// Узел обязан завестись: устройство заводится позже, фиксацией настроек
	ASSERT_GT(tid, 0u) << "движок не записал узел туннеля";
	// Устанавливаем опции события
	ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
	// Устанавливаем адреса концов туннеля
	ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL));
	ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER));
	// Устройство заводится фиксацией настроек: годность окружения проверена выше пробой
	ASSERT_TRUE(this->_io->commit(tid)) << "права есть, а устройство не заведено - нет драйвера туннельных устройств либо дефект движка";
	// Запускаем событие туннеля
	ASSERT_TRUE(this->_io->launch(tid));
	/**
	 * Поднимаем устройство туннеля
	 *
	 * @details Признака UP движок не ставит, а без него устройство остаётся опущенным
	 *          и стек IP пакета с него не берёт
	 */
	{
		// Получаем название устройства туннеля
		const std::string iface = this->_io->getIface(tid);
		// Название устройства обязано быть известно
		ASSERT_FALSE(iface.empty());
		// Создаём объект работы с сетью
		awh::eth_t eth(this->_fmk.get(), this->_log.get());
		// Поднимаем устройство туннеля
		ASSERT_TRUE(eth.iface.flag(iface, awh::event::eth_flag_t::UP, awh::event::mode_t::ENABLED));
	}
	// Заводим приёмник собранного пакета
	awh::net::socket_t fd = static_cast <awh::net::socket_t> (::socket(AF_INET, SOCK_DGRAM, IPPROTO_UDP));
	ASSERT_NE(fd, awh::net::invalid_socket_t);
	// Адрес, каким приёмник занимает порт
	struct sockaddr_in bound{};
	// Устанавливаем семейство адреса приёмника
	bound.sin_family = AF_INET;
	// Устанавливаем порт приёмника
	bound.sin_port = htons(port);
	// Слушаем на всех адресах: пакет придёт устройством туннеля
	bound.sin_addr.s_addr = INADDR_ANY;
	// Занимаем порт приёмником
	ASSERT_EQ(::bind(fd, reinterpret_cast <struct sockaddr *> (&bound), sizeof(bound)), 0)
	 << "порт " << port << ", код отказа " << __awh_last_error__();
	/**
	 * Ставим приёмнику срок ожидания
	 *
	 * @note Срок нужен обеим пробам ниже, и потому ставится один раз здесь
	 */
	{
		// Срок ожидания приёмника
		#if _WIN32 || _WIN64
			// Срок ожидания задаётся числом миллисекунд
			const DWORD timeout = 500;
		#else
			// Срок ожидания задаётся раздельно секундами и микросекундами
			struct timeval timeout{0, 500000};
		#endif
		// Устанавливаем срок ожидания приёмнику
		::setsockopt(fd, SOL_SOCKET, SO_RCVTIMEO, reinterpret_cast <const char *> (&timeout), sizeof(timeout));
	}
	/**
	 * Собираем пакет IPv4 с дейтаграммой UDP руками
	 *
	 * @details Идёт он от встречной стороны туннеля к своей: движку надлежит отдать
	 *          устройству ровно эти октеты, а системе - принять их как пришедшие извне
	 */
	uint8_t packet[28 + PAYLOAD_SIZE] = {0};
	// Размер собранного пакета
	const uint16_t length = static_cast <uint16_t> (sizeof(packet));
	// Устанавливаем вид протокола IPv4 и длину заголовка в 20 октетов
	packet[0] = 0x45;
	// Устанавливаем полную длину пакета
	packet[2] = static_cast <uint8_t> (length >> 8);
	packet[3] = static_cast <uint8_t> (length & 0xFF);
	// Устанавливаем опознаватель пакета
	packet[4] = 0x13;
	packet[5] = 0x37;
	// Устанавливаем срок жизни пакета
	packet[8] = 64;
	// Устанавливаем протокол UDP
	packet[9] = 17;
	// Адрес источника: встречная сторона туннеля
	const uint32_t source = ::inet_addr(TUNNEL_PEER);
	// Адрес назначения: своя сторона туннеля
	const uint32_t target = ::inet_addr(TUNNEL_LOCAL);
	// Переносим адрес источника в заголовок
	::memcpy(packet + 12, &source, sizeof(source));
	// Переносим адрес назначения в заголовок
	::memcpy(packet + 16, &target, sizeof(target));
	/**
	 * Считаем контрольную сумму заголовка
	 *
	 * @warning Нулевая сумма заголовка IP незаконна всегда, и пакет с нею система
	 *          отбрасывает молча
	 */
	{
		// Накопитель суммы
		uint32_t acc = 0;
		// Складываем заголовок словами по два октета
		for(uint8_t i = 0; i < 20; i += 2)
			// Прибавляем очередное слово заголовка
			acc += static_cast <uint32_t> ((packet[i] << 8) | packet[i + 1]);
		// Сворачиваем перенос в саму сумму
		while(acc >> 16)
			// Прибавляем перенос к младшей части
			acc = (acc & 0xFFFF) + (acc >> 16);
		// Обращаем сумму и переносим её в заголовок
		const uint16_t sum = static_cast <uint16_t> (~acc);
		packet[10] = static_cast <uint8_t> (sum >> 8);
		packet[11] = static_cast <uint8_t> (sum & 0xFF);
	}
	// Устанавливаем порт источника дейтаграммы
	packet[20] = static_cast <uint8_t> (port >> 8);
	packet[21] = static_cast <uint8_t> (port & 0xFF);
	// Устанавливаем порт назначения дейтаграммы
	packet[22] = static_cast <uint8_t> (port >> 8);
	packet[23] = static_cast <uint8_t> (port & 0xFF);
	// Устанавливаем длину дейтаграммы вместе с её заголовком
	packet[24] = static_cast <uint8_t> ((8 + PAYLOAD_SIZE) >> 8);
	packet[25] = static_cast <uint8_t> ((8 + PAYLOAD_SIZE) & 0xFF);
	/**
	 * Сумму дейтаграммы оставляем нулевой
	 *
	 * @note Для IPv4 нуль здесь законен и означает «сумма не считана», - в отличие от
	 *       суммы заголовка IP выше
	 */
	// Переносим полезную нагрузку в дейтаграмму
	::memcpy(packet + 28, PAYLOAD, PAYLOAD_SIZE);
	// Адрес встречной стороны туннеля для запрашивающей посылки
	struct sockaddr_in outward{};
	// Устанавливаем семейство адреса встречной стороны
	outward.sin_family = AF_INET;
	// Устанавливаем порт встречной стороны
	outward.sin_port = htons(port);
	// Устанавливаем адрес встречной стороны
	outward.sin_addr.s_addr = ::inet_addr(TUNNEL_PEER);
	// Признак того, что вписанный пакет дошёл до стека IP
	bool accepted = false;
	// Признак того, что движок хотя бы раз принял пакет к записи
	bool written = false;
	// Код отказа посылки, запрашивающей ответ
	int32_t failure = 0;
	// Отсчёт времени ожидания
	const auto start = std::chrono::steady_clock::now();
	/**
	 * Крутим опрос, вписывая пакет в каждом обороте
	 *
	 * @warning Оборот опроса здесь обязателен: в неблокирующем режиме отдача лишь
	 *          копит пакет в очереди, а отдаёт его устройству уже опрос
	 *
	 * @warning Вписывать пакет ОДИН раз нельзя: адреса устройству назначает система, и
	 *          у MS Windows они встают лишь через несколько секунд после подъёма. Пакет,
	 *          вписанный до того, уходит в никуда, и проверка краснела бы на исправном
	 *          движке. Оттого пакет и подсылается в каждом обороте - ровно так же, как
	 *          соседняя проверка подсылает дейтаграммы со стороны системы
	 */
	while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - start).count() < 10000) && !accepted){
		/**
		 * Шлём дейтаграмму встречной стороне туннеля
		 *
		 * @details Нужна она не встречной стороне - её нет вовсе, - а заслону системы:
		 *          у MS Windows незапрошенная дейтаграмма извне отвергается, покуда
		 *          правило заслона её не разрешит, а вот ОТВЕТ на свою же посылку
		 *          проходит. Вписываемый ниже пакет как раз таким ответом и выглядит:
		 *          идёт он с того же адреса и порта, куда ушла эта посылка
		 *
		 * @warning Без неё проверка краснела на исправном движке, и отличить заслон от
		 *          дефекта было нечем: петля заслоном не гонится вовсе, а посылка своему
		 *          же адресу гонится как местная, - обе пробы негодности не показывали
		 */
		/**
		 * Отказ запрашивающей посылки запоминаем
		 *
		 * @details Идёт она через устройство туннеля, и пакетный фильтр системы вправе
		 *          её не пустить: у стенда FreeBSD правила заслона выписаны через lo0 и
		 *          igb0, а обмен через tun0 не совпадает ни с одним и падает в общий
		 *          отказ. Тогда до устройства не доходит НИЧЕГО, и проверять движку
		 *          нечего - ровно так же, как у соседней проверки чтения
		 */
		if(::sendto(fd, PAYLOAD, static_cast <int32_t> (PAYLOAD_SIZE), 0, reinterpret_cast <struct sockaddr *> (&outward), sizeof(outward)) < 0)
			// Запоминаем код отказа посылки
			failure = errno;
		/**
		 * Прямой запрет оспаривать нечем - ждать оставшиеся секунды незачем
		 *
		 * @note Замечено Гришей 21.08.2026: сторож стоял ПОСЛЕ полного ожидания, и на
		 *       заслонённом стенде проверка пропускалась за 10299 мс против 3155 мс у
		 *       соседней. На числа это не влияло, а время на каждом таком стенде тратило
		 */
		if((failure == EACCES) || (failure == EPERM) ||
		   (failure == ENETUNREACH) || (failure == EHOSTUNREACH))
			// Прекращаем ожидание: до устройства не доходит ничего
			break;
		// Вписываем собранный пакет в устройство туннеля
		written = (this->_io->send(tid, reinterpret_cast <const char *> (packet), sizeof(packet)) > 0) || written;
		// Выполняем оборот опроса
		ASSERT_TRUE(this->_io->poll(100));
		// Буфер под принятое приёмником
		char buffer[64] = {0};
		// Выполняем чтение принятого приёмником
		const int32_t bytes = ::recv(fd, buffer, static_cast <int32_t> (sizeof(buffer)), 0);
		// Если принятое совпало с вписанным - пакет прошёл весь путь
		if((bytes == static_cast <int32_t> (PAYLOAD_SIZE)) && (::memcmp(buffer, PAYLOAD, PAYLOAD_SIZE) == 0))
			// Запоминаем, что вписанный пакет дошёл
			accepted = true;
	}
	// Закрываем приёмник
	::closesocket(fd);
	/**
	 * Отказ системы означает негодное окружение, а не дефект движка
	 *
	 * @details Сторож этот взят у соседней проверки чтения дословно, и не случайно:
	 *          обе шлют дейтаграмму через устройство туннеля, обе получают отказ от
	 *          одного и того же пакетного фильтра, и разнобой в доводах только сбивал
	 *          бы с толку
	 *
	 * @warning Негодностью признаются ТОЛЬКО запрет и недостижимость, названные самой
	 *          системой. Пропуск по ним ничего не гасит: будь движок сломан, посылка
	 *          прошла бы, пакет не пришёл, и проверка покраснела бы как надо
	 *
	 * @note Установлено Гришей на стенде FreeBSD 21.08.2026: правила заслона там
	 *       выписаны через lo0 и igb0, обмен через tun0 падает в общий отказ
	 *       `65535 deny ip from any to any`, и проверка обвиняла исправный движок.
	 *       Доказано счётчиком правила с эталоном покоя: прогон - прирост 31,
	 *       десять секунд покоя - прирост 0
	 */
	const bool forbidden = ((failure == EACCES) || (failure == EPERM) ||
	                        (failure == ENETUNREACH) || (failure == EHOSTUNREACH));
	// Если пакет не дошёл, а система назвала запрет либо недостижимость
	if(!accepted && forbidden){
		// Сворачиваем движок
		this->_io->deinitialize();
		// Пропускаем проверку
		GTEST_SKIP() << "система запретила пакету идти в устройство туннеля (" << ::strerror(failure) << "): заслон либо отсутствие маршрута";
	}
	// Движок обязан был принять пакет к записи хотя бы раз
	ASSERT_TRUE(written) << "движок отказался вписать пакет в устройство туннеля";
	// Вписанный пакет обязан дойти до стека IP своей же машины
	ASSERT_TRUE(accepted) << "движок принял пакет, а устройство его не отдало системе: направление записи туннеля не работает";
	// Сворачиваем движок
	ASSERT_TRUE(this->_io->deinitialize());
}

/**
 * @brief Проверка доставки ПЕРВОГО пакета, вписанного в туннель
 *
 * @details Соседняя `IoTunnelAcceptsWrittenPacketTest` вписывает пакет в каждом обороте
 *          опроса, и потеря первого прикрывается вторым - потому она этого дефекта не
 *          ловила. Здесь пакет вписывается РОВНО ОДИН раз, и готовность устройства
 *          дожидается заранее, спрашиванием адреса у самой системы, а не выдержкой
 *
 * @note Дефект, ради какого проверка заведена: отдача туннеля в неблокирующем режиме
 *       писала в устройство лишь при НЕПУСТОЙ очереди, а первый пакет клала в очередь
 *       и события записи не поднимала. Накопленное не выходило оттуда никогда: терялся
 *       ровно один пакет, всегда первый, и молча - отдача отвечала согласием
 *
 * @note Установлено Андреем 21.08.2026 на трёх межмашинных парах: в первом обороте
 *       доходило 7 кусков из 8, и пропадал всегда кусок номер ноль. После правки первый
 *       же оборот даёт 8 из 8 на всех трёх парах
 *
 */
TEST_F(IoFixture, IoTunnelDeliversFirstWrittenPacketTest){
	// Порт приёмника собранного пакета
	const uint16_t port = tunnelPort();
	// Полезная нагрузка вписываемого пакета
	static constexpr const char PAYLOAD[] = "AWH-FIRST";
	// Размер полезной нагрузки
	static constexpr size_t PAYLOAD_SIZE = sizeof(PAYLOAD) - 1;
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	// Заводим событие туннеля
	const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
	// Ставим охранника узла: снос обязан пройти и при отказе утверждения
	const TunnelGuard guard(this->_io.get(), tid);
	// Узел обязан завестись: устройство заводится позже, фиксацией настроек
	ASSERT_GT(tid, 0u) << "движок не записал узел туннеля";
	// Устанавливаем опции события
	ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
	// Устанавливаем адреса концов туннеля
	ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL));
	ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER));
	// Устройство заводится фиксацией настроек
	ASSERT_TRUE(this->_io->commit(tid)) << "права есть, а устройство не заведено - нет драйвера туннельных устройств либо дефект движка";
	// Запускаем событие туннеля
	ASSERT_TRUE(this->_io->launch(tid));
	// Получаем название устройства туннеля
	const std::string iface = this->_io->getIface(tid);
	// Название устройства обязано быть известно
	ASSERT_FALSE(iface.empty());
	// Создаём объект работы с сетью
	awh::eth_t eth(this->_fmk.get(), this->_log.get());
	// Поднимаем устройство туннеля
	ASSERT_TRUE(eth.iface.flag(iface, awh::event::eth_flag_t::UP, awh::event::mode_t::ENABLED));
	/**
	 * Дожидаемся, покуда адрес встанет у самой системы
	 *
	 * @warning Выдержкой здесь пользоваться нельзя: у MS Windows адрес встаёт секундами
	 *          позже подъёма, а у систем POSIX сразу, и всякое число секунд оказалось бы
	 *          либо ложным отказом, либо напрасным ожиданием. Спрашивается потому сама
	 *          система - и ожидание кончается тем мигом, когда она адрес показала
	 */
	{
		// Признак того, что адрес у системы появился
		bool ready = false;
		// Отсчёт времени ожидания готовности
		const auto since = std::chrono::steady_clock::now();
		// Крутим опрос, покуда система не покажет адрес
		while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - since).count() < 15000) && !ready){
			// Спрашиваем у системы адрес устройства
			const std::unique_ptr <awh::net::addr_t> actual = eth.iface.getAddress(iface, awh::event::family_t::IPV4);
			// Если адрес получен и совпал с ожидаемым - устройство готово
			ready = ((actual != nullptr) && (actual->size == 4) &&
			         (awh_cast <const awh::net::addr_net_ipv4_t *> (actual.get())->address == ::inet_addr(TUNNEL_LOCAL)));
			// Выполняем оборот опроса
			ASSERT_TRUE(this->_io->poll(100));
		}
		// Адрес обязан встать: без него проверять доставку бессмысленно
		ASSERT_TRUE(ready) << "система так и не показала адреса на устройстве туннеля: проверять доставку нечем";
	}
	// Заводим приёмник собранного пакета
	awh::net::socket_t fd = static_cast <awh::net::socket_t> (::socket(AF_INET, SOCK_DGRAM, IPPROTO_UDP));
	ASSERT_NE(fd, awh::net::invalid_socket_t);
	// Адрес, каким приёмник занимает порт
	struct sockaddr_in bound{};
	// Устанавливаем семейство адреса приёмника
	bound.sin_family = AF_INET;
	// Устанавливаем порт приёмника
	bound.sin_port = htons(port);
	// Слушаем на всех адресах: пакет придёт устройством туннеля
	bound.sin_addr.s_addr = INADDR_ANY;
	// Занимаем порт приёмником
	ASSERT_EQ(::bind(fd, reinterpret_cast <struct sockaddr *> (&bound), sizeof(bound)), 0)
	 << "порт " << port << ", код отказа " << __awh_last_error__();
	/**
	 * Ставим приёмнику срок ожидания
	 */
	{
		// Срок ожидания приёмника
		#if _WIN32 || _WIN64
			// Срок ожидания задаётся числом миллисекунд
			const DWORD timeout = 500;
		#else
			// Срок ожидания задаётся раздельно секундами и микросекундами
			struct timeval timeout{0, 500000};
		#endif
		// Устанавливаем срок ожидания приёмнику
		::setsockopt(fd, SOL_SOCKET, SO_RCVTIMEO, reinterpret_cast <const char *> (&timeout), sizeof(timeout));
	}
	// Адрес встречной стороны туннеля для запрашивающей посылки
	struct sockaddr_in outward{};
	// Устанавливаем семейство адреса встречной стороны
	outward.sin_family = AF_INET;
	// Устанавливаем порт встречной стороны
	outward.sin_port = htons(port);
	// Устанавливаем адрес встречной стороны
	outward.sin_addr.s_addr = ::inet_addr(TUNNEL_PEER);
	// Код отказа посылки, запрашивающей ответ
	int32_t failure = 0;
	/**
	 * Шлём дейтаграмму встречной стороне: заслон системы иначе не пустит ответа
	 */
	if(::sendto(fd, PAYLOAD, static_cast <int32_t> (PAYLOAD_SIZE), 0, reinterpret_cast <struct sockaddr *> (&outward), sizeof(outward)) < 0)
		// Запоминаем код отказа посылки
		failure = errno;
	// Собираем пакет IPv4 с дейтаграммой UDP руками
	uint8_t packet[28 + PAYLOAD_SIZE] = {0};
	// Размер собранного пакета
	const uint16_t length = static_cast <uint16_t> (sizeof(packet));
	// Устанавливаем вид протокола IPv4 и длину заголовка в 20 октетов
	packet[0] = 0x45;
	// Устанавливаем полную длину пакета
	packet[2] = static_cast <uint8_t> (length >> 8);
	packet[3] = static_cast <uint8_t> (length & 0xFF);
	// Устанавливаем опознаватель пакета
	packet[4] = 0x24;
	packet[5] = 0x01;
	// Устанавливаем срок жизни пакета
	packet[8] = 64;
	// Устанавливаем протокол UDP
	packet[9] = 17;
	// Адрес источника: встречная сторона туннеля
	const uint32_t source = ::inet_addr(TUNNEL_PEER);
	// Адрес назначения: своя сторона туннеля
	const uint32_t target = ::inet_addr(TUNNEL_LOCAL);
	// Переносим адреса в заголовок
	::memcpy(packet + 12, &source, sizeof(source));
	::memcpy(packet + 16, &target, sizeof(target));
	/**
	 * Считаем контрольную сумму заголовка
	 */
	{
		// Накопитель суммы
		uint32_t acc = 0;
		// Складываем заголовок словами по два октета
		for(uint8_t i = 0; i < 20; i += 2)
			// Прибавляем очередное слово заголовка
			acc += static_cast <uint32_t> ((packet[i] << 8) | packet[i + 1]);
		// Сворачиваем перенос в саму сумму
		while(acc >> 16)
			// Прибавляем перенос к младшей части
			acc = (acc & 0xFFFF) + (acc >> 16);
		// Обращаем сумму и переносим её в заголовок
		const uint16_t sum = static_cast <uint16_t> (~acc);
		packet[10] = static_cast <uint8_t> (sum >> 8);
		packet[11] = static_cast <uint8_t> (sum & 0xFF);
	}
	// Устанавливаем порты дейтаграммы
	packet[20] = static_cast <uint8_t> (port >> 8);
	packet[21] = static_cast <uint8_t> (port & 0xFF);
	packet[22] = static_cast <uint8_t> (port >> 8);
	packet[23] = static_cast <uint8_t> (port & 0xFF);
	// Устанавливаем длину дейтаграммы вместе с её заголовком
	packet[24] = static_cast <uint8_t> ((8 + PAYLOAD_SIZE) >> 8);
	packet[25] = static_cast <uint8_t> ((8 + PAYLOAD_SIZE) & 0xFF);
	// Переносим полезную нагрузку в дейтаграмму
	::memcpy(packet + 28, PAYLOAD, PAYLOAD_SIZE);
	/**
	 * Вписываем пакет РОВНО ОДИН раз
	 *
	 * @warning Повторять вписывание здесь нельзя: ровно этим соседняя проверка и
	 *          прикрывала дефект - второй пакет застаёт очередь непустой и уходит
	 *          в устройство сам, а потеря первого остаётся незамеченной
	 */
	ASSERT_GT(this->_io->send(tid, reinterpret_cast <const char *> (packet), sizeof(packet)), 0u)
		<< "движок отказался вписать пакет в устройство туннеля";
	// Признак того, что вписанный пакет дошёл до стека IP
	bool accepted = false;
	// Отсчёт времени ожидания
	const auto start = std::chrono::steady_clock::now();
	/**
	 * Крутим опрос, ожидая единственного вписанного пакета
	 */
	while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - start).count() < 5000) && !accepted){
		// Выполняем оборот опроса
		ASSERT_TRUE(this->_io->poll(100));
		// Буфер под принятое приёмником
		char buffer[64] = {0};
		// Выполняем чтение принятого приёмником
		const int32_t bytes = ::recv(fd, buffer, static_cast <int32_t> (sizeof(buffer)), 0);
		// Если принятое совпало с вписанным - пакет прошёл весь путь
		if((bytes == static_cast <int32_t> (PAYLOAD_SIZE)) && (::memcmp(buffer, PAYLOAD, PAYLOAD_SIZE) == 0))
			// Запоминаем, что вписанный пакет дошёл
			accepted = true;
	}
	// Закрываем приёмник
	::closesocket(fd);
	/**
	 * Отказ системы означает негодное окружение, а не дефект движка
	 */
	const bool forbidden = ((failure == EACCES) || (failure == EPERM) ||
	                        (failure == ENETUNREACH) || (failure == EHOSTUNREACH));
	// Если пакет не дошёл, а система назвала запрет либо недостижимость
	if(!accepted && forbidden){
		// Сворачиваем движок
		this->_io->deinitialize();
		// Пропускаем проверку
		GTEST_SKIP() << "система запретила пакету идти в устройство туннеля (" << ::strerror(failure) << "): заслон либо отсутствие маршрута";
	}
	// Первый же вписанный пакет обязан дойти
	ASSERT_TRUE(accepted) << "потерян ПЕРВЫЙ вписанный пакет: отдача приняла его, а устройству он не достался";
	// Сворачиваем движок
	ASSERT_TRUE(this->_io->deinitialize());
}

/**
 * @brief Адреса концов испытуемого туннеля IPv6
 *
 * @details Берутся они из блока местных адресов (ULA, RFC 4193) и берутся нарочно
 *          НЕ из тех, какими пользуются соседние проверки: устройство туннеля
 *          заводится на всю машину, и совпадение адресов столкнуло бы проверки
 *          между собой через таблицу маршрутов
 *
 */
static constexpr const char * TUNNEL_LOCAL6 = "fd77:aa::1";
static constexpr const char * TUNNEL_PEER6  = "fd77:aa::2";

/**
 * @brief Тест служебного обмена узла на устройстве IPv6
 *
 * @details Ядро на ВСЯКОМ поднятом устройстве IPv6 заводит адрес связи (FE80::/10)
 *          и само шлёт с него оповещения групповым адресам (FF00::/8) - разрешение
 *          соседей, оповещения слушателей, поиск маршрутизатора. Источник таких
 *          пакетов не совпадает с настройками туннеля, и движок объявлял их
 *          подменой адреса: «Attacker replaced the network interface's IP address».
 *          Туннель IPv6 разваливался сам собою, без всякого нападения и без единого
 *          пакета извне
 *
 * @details Проверке оттого не нужно ничего подсылать: обмен этот возникает сам,
 *          стоит устройству подняться. Довольно поднять его и убедиться, что
 *          отклика ошибки за отведённое время НЕ пришло
 *
 * @note Оборот проходится дважды по той же причине, что и в проверке жизненного
 *       цикла: половина дефектов туннелей выдавала себя не первым заведением, а
 *       вторым
 *
 */
TEST_F(IoFixture, IoTunnelIPv6HousekeepingTest){
	// Счётчик откликов ошибки и счётчик объявлений подмены адреса
	uint16_t errors = 0, attacks = 0;
	// Текст первого отказа, снятый для росписи итога
	std::string reason;
	// Миг работы, на котором пришёл отказ
	std::string stage("заведение");
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	/**
	 * Проходим оборот жизни устройства дважды
	 */
	for(uint8_t round = 0; round < 2; round++){
		// Роспись оборота для сообщений об отказе
		const std::string sign = std::string("оборот=") + std::to_string(round + 1);
		/**
		 * Заводим событие туннеля семейства IPv6
		 *
		 * @note Здесь заводится ТОЛЬКО узел: устройство движок заводит фиксацией
		 *       настроек. Пустой опознаватель означает, что движок не записал
		 *       узла, а отказ заведения устройства спрашивается ниже, у commit
		 */
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV6);
		// Ставим охранника узла: снос обязан пройти и при отказе утверждения
		const TunnelGuard guard(this->_io.get(), tid);
		// Узел обязан завестись: устройство заводится позже, фиксацией настроек
		ASSERT_GT(tid, 0u) << sign << ": движок не записал узел туннеля";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC)) << sign;
		// Устанавливаем адреса концов туннеля
		ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV6, TUNNEL_LOCAL6)) << sign;
		ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER6)) << sign;
		/**
		 * Считаем отклики ошибки, отдельно отмечая объявления подмены адреса
		 */
		this->_io->on(tid, static_cast <awh::engine::callback::error_t> (
			[&errors, &attacks, &reason, &stage]([[maybe_unused]] const awh::event::id_t tid, const awh::event::error_t code, const std::string & text) noexcept -> void {
				// Считаем отклик ошибки
				errors++;
				// Запоминаем текст первого отказа вместе с его разновидностью и мигом прихода
				if(reason.empty())
					// Устанавливаем причину первого отказа
					reason = (std::string("вид=") + std::to_string(static_cast <uint16_t> (code)) + " «" + text + "» миг=" + stage);
				// Если движок объявил подмену адреса
				if(text.find("Attacker replaced") != std::string::npos)
					// Считаем объявление подмены адреса
					attacks++;
			}
		));
		// Фиксируем настройки события туннеля
		// Устройство заводится фиксацией настроек: годность окружения проверена выше пробой
		ASSERT_TRUE(this->_io->commit(tid)) << sign << ": права есть, а устройство не заведено - нет драйвера туннельных устройств либо дефект движка";
		// Запускаем событие туннеля
		ASSERT_TRUE(this->_io->launch(tid)) << sign;
		/**
		 * Поднимаем устройство туннеля
		 *
		 * @note Без признака UP служебного обмена не возникает вовсе: ядро заводит
		 *       адрес связи и начинает оповещения лишь на поднятом устройстве. Опыт
		 *       на опущенном устройстве не поставить - там тихо всегда
		 */
		const std::string iface = this->_io->getIface(tid);
		ASSERT_FALSE(iface.empty()) << sign;
		{
			// Создаём объект работы с сетью
			awh::eth_t eth(this->_fmk.get(), this->_log.get());
			// Поднимаем устройство туннеля
			ASSERT_TRUE(eth.iface.flag(iface, awh::event::eth_flag_t::UP, awh::event::mode_t::ENABLED)) << sign;
		}
		/**
		 * Крутим опрос, покуда ядро ведёт служебный обмен
		 *
		 * @note Полторы секунды взяты с запасом: разрешение соседа и оповещение
		 *       слушателей ядро шлёт сразу за подъёмом устройства, а проверка
		 *       неповторимости адреса занимает у систем до секунды
		 */
		// Отмечаем миг работы: устройство поднято, идёт опрос
		stage = "опрос";
		const auto start = std::chrono::steady_clock::now();
		while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - start).count() < 1500))
			ASSERT_TRUE(this->_io->poll(50)) << sign;
		// Отмечаем миг работы: снятие узла
		stage = "снятие";
		// Снимаем событие туннеля
		ASSERT_TRUE(this->_io->destroy(tid)) << sign;
	}
	// Роспись итога для сообщений об отказе
	const std::string sign = std::string("отказов=") + std::to_string(errors) + " подмен=" + std::to_string(attacks) +
	                         ((errors > 0) ? (" первый отказ: " + reason) : std::string(""));
	/**
	 * Снятое число печатается ВСЕГДА, даже при нуле
	 *
	 * @warning Нуль тут двусмыслен: он значит либо что мерка отбирает служебный обмен,
	 *          либо что обмена на этой машине не возникло вовсе. Различает их только
	 *          число, снятое ДО правки. Без печати проверка молча проходила бы там, где
	 *          обмена нет, не проверив ничего - тот же вид молчаливого отключения, что
	 *          и пропуск по признаку, пустому при любой причине
	 */
	std::cout << "служебный обмен IPv6: " << sign << std::endl;
	// Служебный обмен узла подменой адреса объявлен быть не может
	ASSERT_EQ(attacks, 0) << sign;
	// Отказов при служебном обмене не возникает вовсе
	ASSERT_EQ(errors, 0) << sign;
	// Сворачиваем движок
	ASSERT_TRUE(this->_io->deinitialize());
}

/**
 * @brief Тест служебного обмена узла на устройстве IPv4
 *
 * @details Устроена проверка ровно как её двойник по IPv6 и по той же причине: ядро
 *          на поднятом устройстве ведёт свой обмен, источник которого с настройками
 *          туннеля не совпадает, а движок объявляет такой пакет подменой адреса
 *          устройства. У IPv4 состав обмена иной - оповещение слушателей и разрешение
 *          имён в сегменте идут групповым адресам 224.0.0.0/4, выдача адреса уходит с
 *          ПУСТОГО источника 0.0.0.0 на широковещательный 255.255.255.255, канальные
 *          адреса берутся из 169.254.0.0/16
 *
 * @note Состав служебного обмена IPv4 от системы ЗАВИСИТ сильнее, чем у IPv6: у одной
 *       машины он возникает сам, у другой требует настройки. Оттого проверка при нуле
 *       откликов не отчитывается «прошла молча», а печатает снятое число - иначе она
 *       проходила бы и на машине, где обмена не возникает вовсе, ничего не проверив
 *
 * @warning Сличать признаки полагается ОКТЕТАМИ пакета, а не старшими байтами числа:
 *          адрес приходит в разбор сырым из буфера, без обращения порядка, и на
 *          обратнопорядковой машине сравнение со старшим байтом молча перестанет
 *          ловить
 *
 */
TEST_F(IoFixture, IoTunnelIPv4HousekeepingTest){
	// Счётчик откликов ошибки и счётчик объявлений подмены адреса
	uint16_t errors = 0, attacks = 0;
	// Текст первого отказа, снятый для росписи итога
	std::string reason;
	// Миг работы, на котором пришёл отказ
	std::string stage("заведение");
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	/**
	 * Проходим оборот жизни устройства дважды
	 */
	for(uint8_t round = 0; round < 2; round++){
		// Роспись оборота для сообщений об отказе
		const std::string sign = std::string("оборот=") + std::to_string(round + 1);
		/**
		 * Заводим событие туннеля семейства IPv4
		 *
		 * @note Здесь заводится ТОЛЬКО узел: устройство движок заводит фиксацией
		 *       настроек. Пустой опознаватель означает, что движок не записал
		 *       узла, а отказ заведения устройства спрашивается ниже, у commit
		 */
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Ставим охранника узла: снос обязан пройти и при отказе утверждения
		const TunnelGuard guard(this->_io.get(), tid);
		// Узел обязан завестись: устройство заводится позже, фиксацией настроек
		ASSERT_GT(tid, 0u) << sign << ": движок не записал узел туннеля";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC)) << sign;
		// Устанавливаем адреса концов туннеля
		ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL)) << sign;
		ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER)) << sign;
		/**
		 * Считаем отклики ошибки, отдельно отмечая объявления подмены адреса
		 */
		this->_io->on(tid, static_cast <awh::engine::callback::error_t> (
			[&errors, &attacks, &reason, &stage]([[maybe_unused]] const awh::event::id_t tid, const awh::event::error_t code, const std::string & text) noexcept -> void {
				// Считаем отклик ошибки
				errors++;
				// Запоминаем текст первого отказа вместе с его разновидностью и мигом прихода
				if(reason.empty())
					// Устанавливаем причину первого отказа
					reason = (std::string("вид=") + std::to_string(static_cast <uint16_t> (code)) + " «" + text + "» миг=" + stage);
				// Если движок объявил подмену адреса
				if(text.find("Attacker replaced") != std::string::npos)
					// Считаем объявление подмены адреса
					attacks++;
			}
		));
		// Фиксируем настройки события туннеля
		// Устройство заводится фиксацией настроек: годность окружения проверена выше пробой
		ASSERT_TRUE(this->_io->commit(tid)) << sign << ": права есть, а устройство не заведено - нет драйвера туннельных устройств либо дефект движка";
		// Запускаем событие туннеля
		ASSERT_TRUE(this->_io->launch(tid)) << sign;
		/**
		 * Поднимаем устройство туннеля
		 *
		 * @note Без признака UP служебного обмена не возникает вовсе: ядро заводит
		 *       адреса и начинает оповещения лишь на поднятом устройстве
		 */
		const std::string iface = this->_io->getIface(tid);
		ASSERT_FALSE(iface.empty()) << sign;
		{
			// Создаём объект работы с сетью
			awh::eth_t eth(this->_fmk.get(), this->_log.get());
			// Поднимаем устройство туннеля
			ASSERT_TRUE(eth.iface.flag(iface, awh::event::eth_flag_t::UP, awh::event::mode_t::ENABLED)) << sign;
		}
		/**
		 * Крутим опрос, покуда ядро ведёт служебный обмен
		 */
		// Отмечаем миг работы: устройство поднято, идёт опрос
		stage = "опрос";
		const auto start = std::chrono::steady_clock::now();
		while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - start).count() < 1500))
			ASSERT_TRUE(this->_io->poll(50)) << sign;
		// Отмечаем миг работы: снятие узла
		stage = "снятие";
		// Снимаем событие туннеля
		ASSERT_TRUE(this->_io->destroy(tid)) << sign;
	}
	// Роспись итога для сообщений об отказе
	const std::string sign = std::string("отказов=") + std::to_string(errors) + " подмен=" + std::to_string(attacks) +
	                         ((errors > 0) ? (" первый отказ: " + reason) : std::string(""));
	/**
	 * Печатаем снятое число всегда
	 *
	 * @note Нуль здесь двусмыслен: он значит либо что мерка служебный обмен отбирает,
	 *       либо что обмена на этой машине не возникло вовсе. Различает их только
	 *       снятое ДО правки число, оттого оно и печатается
	 */
	std::cout << "служебный обмен IPv4: " << sign << std::endl;
	// Служебный обмен узла подменой адреса объявлен быть не может
	ASSERT_EQ(attacks, 0) << sign;
	// Отказов при служебном обмене не возникает вовсе
	ASSERT_EQ(errors, 0) << sign;
	// Сворачиваем движок
	ASSERT_TRUE(this->_io->deinitialize());
}

/**
 * @brief Адрес, назначить который система отказывается
 *
 * @details Групповой адрес устройству не принадлежит и принадлежать не может:
 *          назначение его системы отвергают. Годится он тем, что отказ приходит
 *          от САМОЙ системы, а не подстроен проверкой, и приходит на всех
 *          системах разом, не требуя от каждой своего способа сломать назначение
 *
 */
static constexpr const char * TUNNEL_REFUSED[] = {"255.255.255.255", "224.0.0.1", "240.0.0.1", "0.0.0.1"};

/**
 * @brief Образцы адресов IPv6, назначить которые система откажется
 *
 * @details Заведены оттого, что семейства расходятся: на NetBSD всякий образец IPv4 из
 *          перечня выше система принимает - замерено щупом 23.08.2026 по всем восьми,
 *          включая адрес самой машины, - а петлю IPv6 отвергает с `EINVAL`. Без этого
 *          перечня условие отказа там не создавалось ничем
 *
 * @note Петля IPv6 годится образцом ровно потому, что адрес этот законен сам по себе, а
 *       негоден ИМЕННО как адрес устройства: система не даёт присвоить `::1` ничему,
 *       кроме петлевого устройства
 *
 */
static constexpr const char * TUNNEL_REFUSED6[] = {"::1"};

/**
 * @brief Тест отказа фиксации при неназначенном адресе устройства
 *
 * @details Устройство без адреса работать не может, и движок это замечает. Но доклад
 *          об отказе ничем не заканчивался: сразу за ним заводилось событие на чтение
 *          и ставились признаки READ, WRITE и CLOSE, признак итога оставался истинным,
 *          и узел поднимался. Движок сам объявлял устройство негодным - и тут же брал
 *          его в работу
 *
 * @details Расплата видна была не там, где беда: пакеты уходили в никуда, а наружу
 *          шло обвинение «Attacker replaced the network interface's IP address» -
 *          сторож видел на устройстве чужой адрес и объявлял подмену. Причина
 *          называлась не та, и разбор уходил искать нападение вместо невставшего
 *          адреса
 *
 * @note Установлено замером 22.08.2026 на паре Solaris ↔ NetBSD: у устройства awhtun0
 *       была лишь привязка IPv6, привязки IPv4 не было вовсе, адрес не встал, и все
 *       пять посылок ушли в никуда - 5140 октетов отправлено, 0 принято, в обе стороны.
 *       Дефект нашёлся во ВСЕХ ПЯТИ движках: kqueue, epoll, io_uring, Event Ports, IOCP
 *
 * @warning Проверка утверждает отказ ФИКСАЦИИ, а не запуска: правленое место лежит в
 *          commit, и устройство заводится именно там. Утверждать отказ launch нельзя -
 *          до него при исправном движке дело уже не доходит
 *
 */
/**
 * @brief Число туннелей, поднимаемых проверкой множественности
 *
 * @note Восемь взяты не наугад: у NetBSD и OpenBSD система поставляет ровно четыре узла
 *       устройств, и меньшее число прошло бы, не проверив ничего
 *
 */
static constexpr uint16_t TUNNEL_MANY = 8;

/**
 * @brief Тест подъёма нескольких туннельных устройств разом
 *
 * @details Фреймворк не вправе ограничивать разработчика одним туннелем: машина бывает
 *          узловой, и туннелей ей нужно столько, сколько он закажет. Проверка поднимает
 *          их подряд, ДЕРЖА все прежние живыми, и требует, чтобы поднялись все
 *
 * @details Проверяются три вещи разом, и каждая закрывает свой дефект:
 *            - число поднятых: у NetBSD давало 3 из 8, у OpenBSD 4 из 8, у Sun 0 из 8;
 *            - РАЗЛИЧНОСТЬ имён: у Sun перебор садился на одну связь восемь раз подряд,
 *              и «поднято восемь» было неправдой;
 *            - второй оборот тем же движком: учёт розданных связей, не освобождаемый при
 *              сносе, давал 0 из 8 при восьми свободных связях.
 *          Всё установлено замерами на стендах 22.08.2026
 *
 * @note Каждому туннелю отводится СВОЯ подсеть: назначение двум устройствам одного
 *       адреса система отвергла бы сама, и отказ этот к числу устройств отношения
 *       не имеет
 *
 */
TEST_F(IoFixture, IoTunnelRaisesManyDevicesTest){
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннелей не готово
	if(!tunnelReady(TUNNEL_MANY))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	// Выбираем драйвер по составу машины: постоянных устройств может не хватить
	const char * driver = tunnelPinForCount(this->_io.get(), TUNNEL_MANY);
	// Докладываем выбранный драйвер: на разных машинах проверка идёт разными путями
	SCOPED_TRACE(std::string("драйвер устройств: ") + driver);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	/**
	 * Проходим оборот подъёма ДВАЖДЫ
	 *
	 * @note Второй оборот ловит учёт, не освобождаемый при сносе: связи к нему обязаны
	 *       вернуться, и второй оборот обязан дать столько же, сколько первый
	 */
	for(uint8_t round = 0; round < 2; round++){
		// Роспись оборота для сообщений об отказе
		const std::string sign = std::string("оборот ") + std::to_string(round + 1);
		// Перечень опознавателей поднятых узлов
		std::vector <awh::event::id_t> raised;
		// Перечень имён заведённых устройств
		std::vector <std::string> names;
		/**
		 * Поднимаем туннели подряд, не снося прежних
		 */
		for(uint16_t i = 0; i < TUNNEL_MANY; i++){
			// Заводим узел туннеля
			const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
			// Узел обязан завестись
			ASSERT_GT(tid, 0u) << sign << ": движок не записал узел туннеля " << (i + 1);
			// Записываем узел в перечень до всякой проверки: снос обязан пройти и при отказе
			raised.push_back(tid);
			// Устанавливаем опции события туннеля
			ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC)) << sign;
			// Отводим туннелю свою подсеть, разную на разных оборотах
			const std::string local = ("10." + std::to_string(70 + round) + "." + std::to_string(i + 1) + ".1");
			// Устанавливаем адреса концов туннеля
			ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, local)) << sign;
			ASSERT_TRUE(this->_io->setTarget(tid, ("10." + std::to_string(70 + round) + "." + std::to_string(i + 1) + ".2"))) << sign;
			// Фиксация настроек обязана пройти: устройство заводится именно ею
			ASSERT_TRUE(this->_io->commit(tid)) << sign << ": туннель " << (i + 1) << " из " << TUNNEL_MANY << " не поднялся";
			// Получаем имя заведённого устройства
			const std::string name = this->_io->getIface(tid);
			// Имя заведённого устройства обязано быть известно
			ASSERT_FALSE(name.empty()) << sign << ": движок не назвал устройство туннеля " << (i + 1);
			/**
			 * Имя обязано отличаться от имён всех прежних
			 *
			 * @warning Совпадение означает, что движок выдал одно устройство дважды, и
			 *          число поднятых туннелей - неправда. Установлено на стенде Solaris:
			 *          восемь туннелей сели на одну связь, а счёт показывал восемь
			 */
			ASSERT_EQ(std::find(names.begin(), names.end(), name), names.end())
				<< sign << ": устройство «" << name << "» выдано дважды";
			// Записываем имя в перечень заведённых
			names.push_back(name);
		}
		// Поднятых туннелей обязано быть столько, сколько заказано
		ASSERT_EQ(raised.size(), static_cast <size_t> (TUNNEL_MANY)) << sign;
		/**
		 * Сносим поднятые туннели, освобождая устройства
		 */
		for(const awh::event::id_t tid : raised)
			// Сносим узел туннеля
			this->_io->destroy(tid);
	}
}

/**
 * @brief Число туннелей, через какие проверяется перенос
 *
 * @note Меньше, чем у проверки подъёма: каждому туннелю здесь придаётся своя несущая
 *       связь с парой узлов, и восемь таких наборов затянули бы проверку впустую
 *
 */
static constexpr uint16_t TUNNEL_FLOWS = 3;

/**
 * @brief Тест переноса данных по КАЖДОМУ из нескольких туннелей
 *
 * @details Заведение устройства ещё ничего не говорит о переносе: устройство бывает
 *          заведено, поднято и при этом глухо. Проверка поднимает несколько туннелей
 *          разом, каждому отводит свою подсеть и свою несущую связь, и шлёт в каждый
 *          дейтаграмму СОКЕТОМ СИСТЕМЫ - не движком, иначе движок проверялся бы сам
 *          собою
 *
 * @warning Счёт ведётся ПО КАЖДОМУ туннелю отдельно: общий счётчик прошёл бы и тогда,
 *          когда переносит один туннель, а прочие молчат. Ради этого у каждого туннеля
 *          свой посредник со своим откликом чтения
 *
 * @note Отказ отправки означает негодность окружения, а не дефект движка: пакетный
 *       фильтр системы вправе не пустить пакет к встречной стороне туннеля. У стенда
 *       FreeBSD правила ipfw отвергают его с «Permission denied» ещё на отправке, и
 *       до устройства он не доходит вовсе
 *
 */
TEST_F(IoFixture, IoTunnelCarriesOnEachDeviceTest){
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннелей не готово
	if(!tunnelReady(TUNNEL_FLOWS))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	// Выбираем драйвер по составу машины: постоянных устройств может не хватить
	const char * driver = tunnelPinForCount(this->_io.get(), TUNNEL_FLOWS);
	// Докладываем выбранный драйвер: на разных машинах проверка идёт разными путями
	SCOPED_TRACE(std::string("драйвер устройств: ") + driver);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	// Число дошедшего по каждому туннелю
	std::array <uint16_t, TUNNEL_FLOWS> carried{};
	// Код отказа отправки по каждому туннелю
	std::array <int32_t, TUNNEL_FLOWS> failures{};
	// Перечень опознавателей узлов туннелей
	std::vector <awh::event::id_t> raised;
	// Перечень адресов встречных сторон
	std::vector <std::string> peers;
	/**
	 * Поднимаем туннели с их несущими связями
	 */
	for(uint16_t i = 0; i < TUNNEL_FLOWS; i++){
		// Роспись туннеля для сообщений об отказе
		const std::string sign = std::string("туннель ") + std::to_string(i + 1);
		// Отводим туннелю свою подсеть
		const std::string local = ("10.79." + std::to_string(i + 1) + ".1");
		// Устанавливаем адрес встречной стороны той же подсети
		const std::string peer = ("10.79." + std::to_string(i + 1) + ".2");
		// Отводим несущей связи свой порт
		const uint16_t bearer = tunnelPort();
		// Заводим узлы туннеля и его несущей связи
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		const awh::event::id_t mid = this->_io->event(awh::event::node_t::MEDIATOR, awh::event::family_t::IPV4);
		const awh::event::id_t sid = this->_io->event(awh::event::node_t::SERVER, awh::event::family_t::IPV4, awh::event::type_t::DATAGRAM, awh::event::protocol_t::UDP);
		const awh::event::id_t cid = this->_io->event(awh::event::node_t::CLIENT, awh::event::family_t::IPV4, awh::event::type_t::DATAGRAM, awh::event::protocol_t::UDP);
		// Узлы обязаны завестись
		ASSERT_GT(tid, 0u) << sign;
		ASSERT_GT(mid, 0u) << sign;
		ASSERT_GT(sid, 0u) << sign;
		ASSERT_GT(cid, 0u) << sign;
		// Записываем узел туннеля в перечень до всякой проверки: снос обязан пройти и при отказе
		raised.push_back(tid);
		// Запоминаем адрес встречной стороны
		peers.push_back(peer);
		// Устанавливаем опции событий
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC)) << sign;
		ASSERT_TRUE(this->_io->setOptions(sid, awh::event::options::NO_SIGPIPE | awh::event::options::REUSE_ADDR | awh::event::options::NO_IO_BLOCK)) << sign;
		ASSERT_TRUE(this->_io->setOptions(cid, awh::event::options::NO_SIGPIPE | awh::event::options::NO_IO_BLOCK)) << sign;
		// Устанавливаем адреса концов туннеля
		ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, local)) << sign;
		ASSERT_TRUE(this->_io->setTarget(tid, peer)) << sign;
		// Устанавливаем встречную сторону посреднику
		ASSERT_TRUE(this->_io->setTarget(mid, peer)) << sign;
		// Устанавливаем адрес и порт несущей связи
		ASSERT_TRUE(this->_io->setAddress(sid, awh::event::address_t::IPV4, "127.0.0.1")) << sign;
		ASSERT_TRUE(this->_io->setSourcePort(sid, bearer)) << sign;
		ASSERT_TRUE(this->_io->setTarget(cid, "127.0.0.1")) << sign;
		ASSERT_TRUE(this->_io->setTargetPort(cid, bearer)) << sign;
		// Связываем посредника с клиентом несущей связи
		ASSERT_TRUE(this->_io->splice(mid, cid)) << sign;
		/**
		 * Считаем прочитанное посредником ИМЕННО ЭТОГО туннеля
		 *
		 * @note Отклик чтения ставится посреднику, а не серверу несущей связи: у события
		 *       сервера отклик этот не заводится вовсе, а посредник как раз и держит то,
		 *       что прочитано из туннеля
		 */
		this->_io->on(mid, static_cast <awh::engine::callback::read_t> (
			[&carried, i]([[maybe_unused]] const awh::event::id_t mid, const uint8_t * data, const size_t size) noexcept -> void {
				// Считаем дошедшее по этому туннелю
				if((data != nullptr) && (size > 0))
					// Увеличиваем счётчик дошедшего
					carried[i]++;
			}
		));
		// Фиксируем настройки узлов
		ASSERT_TRUE(this->_io->commit(tid)) << sign << ": устройство не заведено";
		ASSERT_TRUE(this->_io->commit(sid)) << sign;
		ASSERT_TRUE(this->_io->commit(cid)) << sign;
		ASSERT_TRUE(this->_io->commit(mid)) << sign;
		// Подключаем несущую связь к своему же серверу
		ASSERT_TRUE(this->_io->connect(cid)) << sign;
		// Запускаем события
		ASSERT_TRUE(this->_io->launch(sid)) << sign;
		ASSERT_TRUE(this->_io->launch(cid)) << sign;
		ASSERT_TRUE(this->_io->launch(tid)) << sign;
		/**
		 * Поднимаем устройство туннеля
		 *
		 * @details Движок устройство заводит и адреса ему назначает, но признака UP не
		 *          ставит: без него устройство остаётся опущенным, маршрута через него
		 *          нет и пакету идти некуда
		 */
		{
			// Получаем название устройства туннеля
			const std::string iface = this->_io->getIface(tid);
			// Название устройства обязано быть известно
			ASSERT_FALSE(iface.empty()) << sign;
			// Создаём объект работы с сетью
			awh::eth_t eth(this->_fmk.get(), this->_log.get());
			// Поднимаем устройство туннеля
			ASSERT_TRUE(eth.iface.flag(iface, awh::event::eth_flag_t::UP, awh::event::mode_t::ENABLED)) << sign;
		}
	}
	// Полезная нагрузка отправляемой дейтаграммы
	const char payload[] = "AWH-MULTI-TUNNEL-PROBE";
	// Создаём сокет системы для отправки дейтаграмм
	const awh::net::socket_t fd = static_cast <awh::net::socket_t> (::socket(AF_INET, SOCK_DGRAM, IPPROTO_UDP));
	// Сокет отправки обязан создаться
	ASSERT_NE(fd, awh::net::invalid_socket_t);
	// Отсчёт времени ожидания
	const auto start = std::chrono::steady_clock::now();
	// Число туннелей, по которым перенос состоялся
	uint16_t done = 0;
	/**
	 * Крутим опрос, подсылая дейтаграммы в каждый туннель
	 */
	while((std::chrono::duration_cast <std::chrono::milliseconds> (std::chrono::steady_clock::now() - start).count() < 5000) && (done < TUNNEL_FLOWS)){
		// Обнуляем счётчик готовых туннелей
		done = 0;
		/**
		 * Переходим по всем поднятым туннелям
		 */
		for(uint16_t i = 0; i < TUNNEL_FLOWS; i++){
			// Адрес встречной стороны туннеля
			struct sockaddr_in target{};
			// Устанавливаем семейство адреса встречной стороны
			target.sin_family = AF_INET;
			// Устанавливаем порт встречной стороны
			target.sin_port = htons(9);
			// Устанавливаем адрес встречной стороны
			target.sin_addr.s_addr = ::inet_addr(peers[i].c_str());
			// Отправляем дейтаграмму на адрес встречной стороны туннеля
			if(::sendto(fd, payload, sizeof(payload) - 1, 0, reinterpret_cast <struct sockaddr *> (&target), sizeof(target)) < 0)
				// Запоминаем код отказа отправки
				failures[i] = errno;
			// Считаем туннель, по которому дошло
			if(carried[i] > 0)
				// Увеличиваем счётчик готовых туннелей
				done++;
		}
		// Выполняем оборот опроса
		ASSERT_TRUE(this->_io->poll(100));
	}
	// Закрываем сокет отправки
	::closesocket(fd);
	// Число туннелей, отправку в которые отвергло само окружение
	uint16_t refused = 0;
	// Роспись итога для сообщений об отказе
	std::string sign;
	/**
	 * Переходим по всем поднятым туннелям
	 */
	for(uint16_t i = 0; i < TUNNEL_FLOWS; i++){
		// Считаем туннель, отправку в который отвергло окружение
		if((carried[i] == 0) && ((failures[i] == EACCES) || (failures[i] == EPERM) || (failures[i] == ENETUNREACH) || (failures[i] == EHOSTUNREACH)))
			// Увеличиваем счётчик отвергнутых окружением
			refused++;
		// Дописываем роспись очередного туннеля
		sign.append(" туннель ").append(std::to_string(i + 1)).append(": дошло=").append(std::to_string(carried[i]))
		    .append(" отказ=").append((failures[i] != 0) ? ::strerror(failures[i]) : "нет").append(";");
	}
	/**
	 * Сносим поднятые туннели
	 */
	for(const awh::event::id_t tid : raised)
		// Сносим узел туннеля
		this->_io->destroy(tid);
	/**
	 * Отказ окружения по ВСЕМ туннелям означает негодное окружение, а не дефект движка
	 *
	 * @warning Пробой окружения здесь служит errno самой системы, а не поведение
	 *          разбираемого кода: спрашивается тот, кто отказ и вынес. Правила заслона
	 *          проверка не трогает и трогать не вправе - они принадлежат хозяину стенда
	 */
	if(refused == TUNNEL_FLOWS)
		// Пропускаем проверку
		GTEST_SKIP() << "отправку отвергло окружение - пакет не дошёл до устройства:" << sign;
	/**
	 * Перенос обязан состояться по КАЖДОМУ туннелю
	 */
	for(uint16_t i = 0; i < TUNNEL_FLOWS; i++)
		// Дошедшего по очередному туннелю обязано быть больше нуля
		ASSERT_GT(carried[i], 0u) << "перенос не состоялся по туннелю " << (i + 1) << ";" << sign;
}

TEST_F(IoFixture, IoTunnelRefusesCommitWithoutAddressTest){
	// Счётчик откликов ошибки и счётчик объявлений подмены адреса
	uint16_t errors = 0, attacks = 0;
	// Счётчик докладов о неназначенном адресе
	uint16_t orphans = 0;
	// Текст первого отказа, снятый для росписи итога
	std::string reason;
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	/**
	 * Закрепляем драйвер устройств туннеля: у MS Windows их два, и выбор по умолчанию
	 * решается составом каталога сборки, а не замыслом проверки
	 */
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	// Итог фиксации настроек и образец адреса, на котором отказ воспроизвёлся
	bool committed = true;
	std::string sample;
	/**
	 * Перебираем образцы адресов, покуда система не откажет назначением
	 *
	 * @warning Одного образца НЕ ДОВОЛЬНО: системы расходятся в том, что считать
	 *          негодным адресом устройства. Установлено на стенде NetBSD 22.08.2026 -
	 *          групповой адрес 224.0.0.1 система устройству назначила без возражений,
	 *          и проверка отступила, ничего не проверив. Перебор снимает зависимость
	 *          проверки от повадок одной системы
	 *
	 * @note Узел заводится СВОЙ на каждый образец: фиксация настроек проходит у узла
	 *       единожды, и повторить её на том же узле нельзя
	 */
	for(uint8_t i = 0; (i < (sizeof(TUNNEL_REFUSED) / sizeof(TUNNEL_REFUSED[0]))) && committed && (orphans == 0); i++){
		// Заводим событие туннеля
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Ставим охранника узла: снос обязан пройти и при отказе утверждения
		const TunnelGuard guard(this->_io.get(), tid);
		// Узел обязан завестись: устройство заводится позже, фиксацией настроек
		ASSERT_GT(tid, 0u) << "движок не записал узел туннеля";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
		/**
		 * Ставим адрес, назначить который система откажется
		 *
		 * @note Отказ самого вызова означает, что движок отверг адрес РАНЬШЕ места,
		 *       которое проверяется, и до дефекта дело не дошло бы вовсе. Такой
		 *       образец не годится, берём следующий
		 */
		if(!this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_REFUSED[i]))
			// Переходим к следующему образцу адреса
			continue;
		// Устанавливаем адрес встречной стороны
		ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER));
		/**
		 * Считаем отклики ошибки, отдельно отмечая доклад о неназначенном адресе
		 * и объявление подмены адреса
		 */
		this->_io->on(tid, static_cast <awh::engine::callback::error_t> (
			[&errors, &attacks, &orphans, &reason]([[maybe_unused]] const awh::event::id_t tid, const awh::event::error_t code, const std::string & text) noexcept -> void {
				// Считаем отклик ошибки
				errors++;
				// Запоминаем текст первого отказа вместе с его разновидностью
				if(reason.empty())
					// Устанавливаем причину первого отказа
					reason = (std::string("вид=") + std::to_string(static_cast <uint16_t> (code)) + " «" + text + "»");
				// Если движок доложил о неназначенном адресе
				if(text.find("left without an address") != std::string::npos)
					// Считаем доклад о неназначенном адресе
					orphans++;
				// Если движок объявил подмену адреса
				if(text.find("Attacker replaced") != std::string::npos)
					// Считаем объявление подмены адреса
					attacks++;
			}
		));
		// Запоминаем образец адреса, которым ставился опыт
		sample = TUNNEL_REFUSED[i];
		// Снимаем итог фиксации настроек события туннеля
		committed = this->_io->commit(tid);
	}
	/**
	 * Перебираем образцы адресов IPv6, покуда система не откажет назначением
	 *
	 * @details Семейства расходятся: NetBSD принимает ВСЕ восемь образцов IPv4 и оба
	 *          вида занятости, а петлю IPv6 отвергает. Без этого перебора условие
	 *          отказа там не создавалось ничем, и проверка отступала, ничего не
	 *          проверив
	 *
	 * @note Порядок здесь тот же, что и у IPv4 выше, и по тем же доводам: узел свой на
	 *       каждый образец, отказ самого `setAddress` означает негодный образец
	 */
	for(uint8_t i = 0; (i < (sizeof(TUNNEL_REFUSED6) / sizeof(TUNNEL_REFUSED6[0]))) && committed && (orphans == 0); i++){
		// Заводим событие туннеля
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV6);
		// Ставим охранника узла: снос обязан пройти и при отказе утверждения
		const TunnelGuard guard(this->_io.get(), tid);
		// Узел обязан завестись
		ASSERT_GT(tid, 0u) << "движок не записал узел туннеля IPv6";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
		// Ставим адрес, назначить который система откажется
		if(!this->_io->setAddress(tid, awh::event::address_t::IPV6, TUNNEL_REFUSED6[i]))
			// Переходим к следующему образцу адреса
			continue;
		// Устанавливаем адрес встречной стороны
		ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER6));
		// Считаем отклики ошибки тем же счётчиком
		this->_io->on(tid, static_cast <awh::engine::callback::error_t> (
			[&errors, &attacks, &orphans, &reason]([[maybe_unused]] const awh::event::id_t tid, const awh::event::error_t code, const std::string & text) noexcept -> void {
				// Считаем отклик ошибки
				errors++;
				// Запоминаем текст первого отказа вместе с его разновидностью
				if(reason.empty())
					// Устанавливаем причину первого отказа
					reason = (std::string("вид=") + std::to_string(static_cast <uint16_t> (code)) + " «" + text + "»");
				// Если движок доложил о неназначенном адресе
				if(text.find("left without an address") != std::string::npos)
					// Считаем доклад о неназначенном адресе
					orphans++;
				// Если движок объявил подмену адреса
				if(text.find("Attacker replaced") != std::string::npos)
					// Считаем объявление подмены адреса
					attacks++;
			}
		));
		// Запоминаем образец адреса, которым ставился опыт
		sample = TUNNEL_REFUSED6[i];
		// Снимаем итог фиксации настроек события туннеля
		committed = this->_io->commit(tid);
	}
	/**
	 * Второй способ создать условие: ЗАНЯТОСТЬ адреса, а не его негодность
	 *
	 * @details Отказ назначения бывает двух родов, и первым способом второй не
	 *          воссоздаётся вовсе. Негодный адрес система отвергает сама; занятый
	 *          адрес совершенно годен, но уже числится за прежним устройством, и
	 *          система отвечает «уже существует». Сколько негодных образцов ни
	 *          перебирай, каждый окажется свободен и назначится
	 *
	 * @note Способ этот - единственный, каким условие воссоздаётся у MS Windows:
	 *       адрес остаётся за устройством после жёсткого снятия процесса. Установлено
	 *       агентом стороны Windows 22.08.2026 замером до и после правки
	 *
	 * @warning Первый узел ЖИВЁТ, покуда фиксируется второй: снос его вернул бы адрес
	 *          системе, и занятости не возникло бы. Оттого охранник первого узла
	 *          объявлен в объемлющей области, а не внутри опыта
	 */
	if(committed && (orphans == 0)){
		// Заводим первый узел туннеля - тот, что займёт адрес
		const awh::event::id_t owner = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Ставим охранника занимающего узла
		const TunnelGuard holder(this->_io.get(), owner);
		// Узел обязан завестись
		ASSERT_GT(owner, 0u) << "движок не записал узел туннеля, занимающий адрес";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(owner, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
		// Устанавливаем адреса концов туннеля
		ASSERT_TRUE(this->_io->setAddress(owner, awh::event::address_t::IPV4, TUNNEL_LOCAL));
		ASSERT_TRUE(this->_io->setTarget(owner, TUNNEL_PEER));
		/**
		 * Занимающий узел обязан подняться: без него занятости не возникнет
		 *
		 * @note Отказ здесь означает, что окружение к опыту не готово - устройства
		 *       туннеля нет вовсе, - и судить по такому прогону нельзя
		 */
		if(this->_io->commit(owner)){
			// Заводим второй узел туннеля - тот, что попросит ЗАНЯТЫЙ адрес
			const awh::event::id_t rival = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
			// Ставим охранника соперничающего узла
			const TunnelGuard contender(this->_io.get(), rival);
			// Узел обязан завестись
			ASSERT_GT(rival, 0u) << "движок не записал узел туннеля, просящий занятый адрес";
			// Устанавливаем опции события туннеля
			ASSERT_TRUE(this->_io->setOptions(rival, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
			// Просим ТОТ ЖЕ адрес, что уже занят первым узлом
			ASSERT_TRUE(this->_io->setAddress(rival, awh::event::address_t::IPV4, TUNNEL_LOCAL));
			ASSERT_TRUE(this->_io->setTarget(rival, TUNNEL_PEER));
			// Считаем отклики ошибки соперничающего узла тем же счётчиком
			this->_io->on(rival, static_cast <awh::engine::callback::error_t> (
				[&errors, &attacks, &orphans, &reason]([[maybe_unused]] const awh::event::id_t tid, const awh::event::error_t code, const std::string & text) noexcept -> void {
					// Считаем отклик ошибки
					errors++;
					// Запоминаем текст первого отказа вместе с его разновидностью
					if(reason.empty())
						// Устанавливаем причину первого отказа
						reason = (std::string("вид=") + std::to_string(static_cast <uint16_t> (code)) + " «" + text + "»");
					// Если движок доложил о неназначенном адресе
					if(text.find("left without an address") != std::string::npos)
						// Считаем доклад о неназначенном адресе
						orphans++;
					// Если движок объявил подмену адреса
					if(text.find("Attacker replaced") != std::string::npos)
						// Считаем объявление подмены адреса
						attacks++;
				}
			));
			/**
			 * Запоминаем способ, которым ставился опыт, вместе с ИМЕНАМИ обоих устройств
			 *
			 * @warning Имена существенны: сядь оба узла на одно устройство - занятости
			 *          не возникнет вовсе, и «фиксация принята» означало бы не согласие
			 *          движка на занятый адрес, а непоставленный опыт
			 */
			// Снимаем итог фиксации настроек соперничающего узла
			committed = this->_io->commit(rival);
			/**
			 * Имена спрашиваются ПОСЛЕ фиксации: устройство заводится именно ею, и
			 * прежде неё имя соперничающего узла пусто всегда
			 */
			sample = std::string("занятость адреса ") + TUNNEL_LOCAL +
			         " (занял «" + this->_io->getIface(owner) + "», просит «" + this->_io->getIface(rival) + "»)";
		}
	}
	/**
	 * Третий способ создать условие: занятость адреса IPv6
	 *
	 * @details Системы расходятся не только в том, что считать негодным адресом, но и
	 *          в том, отвергать ли ДУБЛИКАТ, - и расходятся по семействам адресов
	 *          порознь. Замерено щупом 23.08.2026 на трёх стендах: FreeBSD дубликат
	 *          IPv4 принимает молча, а дубликат IPv6 отвергает с EEXIST, оставляя
	 *          адрес за прежним устройством; NetBSD и OpenBSD принимают оба. Оттого
	 *          способ по IPv4 на FreeBSD условия не создаёт вовсе, а способ по IPv6 -
	 *          создаёт, и проверка там перестаёт отступать
	 *
	 * @warning Занимающий узел ЖИВЁТ, покуда фиксируется второй, по тому же доводу,
	 *          что и у способа по IPv4 выше
	 */
	if(committed && (orphans == 0)){
		// Заводим первый узел туннеля IPv6 - тот, что займёт адрес
		const awh::event::id_t owner = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV6);
		// Ставим охранника занимающего узла
		const TunnelGuard holder(this->_io.get(), owner);
		// Узел обязан завестись
		ASSERT_GT(owner, 0u) << "движок не записал узел туннеля IPv6, занимающий адрес";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(owner, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
		// Устанавливаем адреса концов туннеля
		ASSERT_TRUE(this->_io->setAddress(owner, awh::event::address_t::IPV6, TUNNEL_LOCAL6));
		ASSERT_TRUE(this->_io->setTarget(owner, TUNNEL_PEER6));
		/**
		 * Занимающий узел обязан подняться: без него занятости не возникнет
		 *
		 * @note Отказ здесь означает, что окружение к опыту не готово - стека IPv6 у
		 *       машины нет вовсе, - и судить по такому прогону нельзя
		 */
		if(this->_io->commit(owner)){
			// Заводим второй узел туннеля - тот, что попросит ЗАНЯТЫЙ адрес IPv6
			const awh::event::id_t rival = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV6);
			// Ставим охранника соперничающего узла
			const TunnelGuard contender(this->_io.get(), rival);
			// Узел обязан завестись
			ASSERT_GT(rival, 0u) << "движок не записал узел туннеля IPv6, просящий занятый адрес";
			// Устанавливаем опции события туннеля
			ASSERT_TRUE(this->_io->setOptions(rival, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
			// Просим ТОТ ЖЕ адрес, что уже занят первым узлом
			ASSERT_TRUE(this->_io->setAddress(rival, awh::event::address_t::IPV6, TUNNEL_LOCAL6));
			ASSERT_TRUE(this->_io->setTarget(rival, TUNNEL_PEER6));
			// Считаем отклики ошибки соперничающего узла тем же счётчиком
			this->_io->on(rival, static_cast <awh::engine::callback::error_t> (
				[&errors, &attacks, &orphans, &reason]([[maybe_unused]] const awh::event::id_t tid, const awh::event::error_t code, const std::string & text) noexcept -> void {
					// Считаем отклик ошибки
					errors++;
					// Запоминаем текст первого отказа вместе с его разновидностью
					if(reason.empty())
						// Устанавливаем причину первого отказа
						reason = (std::string("вид=") + std::to_string(static_cast <uint16_t> (code)) + " «" + text + "»");
					// Если движок доложил о неназначенном адресе
					if(text.find("left without an address") != std::string::npos)
						// Считаем доклад о неназначенном адресе
						orphans++;
					// Если движок объявил подмену адреса
					if(text.find("Attacker replaced") != std::string::npos)
						// Считаем объявление подмены адреса
						attacks++;
				}
			));
			// Снимаем итог фиксации настроек соперничающего узла
			committed = this->_io->commit(rival);
			/**
			 * Имена спрашиваются ПОСЛЕ фиксации по тому же доводу, что и у IPv4 выше
			 */
			sample = std::string("занятость адреса ") + TUNNEL_LOCAL6 +
			         " (занял «" + this->_io->getIface(owner) + "», просит «" + this->_io->getIface(rival) + "»)";
		}
	}
	/**
	 * Отказ обязан прийти ОТ СИСТЕМЫ, а не от проверки
	 *
	 * @note Если ни один из трёх способов отказа не дал, опыт не поставлен:
	 *       устройство всякий раз выходило годным, и отказа фиксации ждать не от чего
	 */
	if(committed && (orphans == 0))
		// Пропускаем проверку
		GTEST_SKIP() << "система приняла негодные образцы обоих семейств и занятые адреса обоих семейств: отказ назначения не воспроизведён"
		             << " [способ: " << (sample.empty() ? std::string("не поставлен") : sample)
		             << "; докладов=" << orphans << "; отказов=" << errors
		             << ((errors > 0) ? ("; первый: " + reason) : std::string("")) << "]";
	// Роспись итога для сообщений об отказе
	const std::string sign = std::string("образец=") + sample + " фиксация=" + (committed ? "принята" : "отвергнута") +
	                         " докладов о неназначенном адресе=" + std::to_string(orphans) +
	                         " объявлений подмены=" + std::to_string(attacks) +
	                         " отказов=" + std::to_string(errors) +
	                         ((errors > 0) ? (" первый отказ: " + reason) : std::string(""));
	// Снятое печатается ВСЕГДА: без числа проверка неотличима от молча прошедшей
	std::cout << "отказ назначения адреса: " << sign << std::endl;
	// Движок обязан доложить о неназначенном адресе
	ASSERT_GT(orphans, 0) << sign << ": адрес назначен не был, а движок об этом не доложил";
	/**
	 * Доклад об отказе обязан отказом и ЗАКАНЧИВАТЬСЯ
	 *
	 * @note Ровно это утверждение и было ложным до правки: движок докладывал о
	 *       негодном устройстве и отвечал согласием на фиксацию настроек
	 */
	ASSERT_FALSE(committed) << sign << ": движок доложил о неназначенном адресе и всё равно принял фиксацию настроек";
	// Подмены адреса при неназначенном адресе движок объявлять не может
	ASSERT_EQ(attacks, 0) << sign << ": причина названа не та - вместо неназначенного адреса объявлено нападение";
	// Сворачиваем движок
	ASSERT_TRUE(this->_io->deinitialize());
}

/**
 * Для операционной системы MS Windows
 */
#if _WIN32 || _WIN64
	/**
	 * @brief Доступен ли на машине драйвер Wintun
	 *
	 * @details Спрашивается САМА СИСТЕМА - попыткой загрузить библиотеку, - а не движок.
	 *          Правило то же, что и у пробы надзорных прав выше: спроси движок, и на
	 *          собственном его дефекте проверка ответит «драйвера нет» и обратится в
	 *          пропуск вместо отказа. Пропуск - это молчаливое отключение
	 *
	 * @warning Развести «Wintun на машине нет» и «Wintun есть, а путь сломан» ответом
	 *          движка НЕЛЬЗЯ: замерено сессией стороны Windows 23.08.2026 - при явно
	 *          заданном WINTUN и убранной библиотеке фиксация отказывает, `getDriver`
	 *          честно отдаёт WINTUN, а отклик ошибки несёт код 4 «File descriptor for
	 *          tunnel is corrupted or not created», то есть СЛЕДСТВИЕ. Настоящая
	 *          причина - «wintun.dll could not be loaded, error 126» - уходит только в
	 *          журнал движка. Наружного вопроса «доступен ли драйвер» у API нет
	 *
	 * @note Оттого отступление решается ЗДЕСЬ, до движка: библиотека не грузится -
	 *       окружение не готово; грузится - всякий отказ движка есть его дефект, и
	 *       проверка обязана падать, а не отступать
	 *
	 * @return признак доступности драйвера Wintun
	 *
	 */
	static bool tunnelWintunReady() noexcept {
		// Выполняем загрузку библиотеки драйвера Wintun
		HMODULE library = ::LoadLibraryW(L"wintun.dll");
		// Если библиотеку загрузить не удалось
		if(library == nullptr)
			// Выводим отсутствие драйвера
			return false;
		// Освобождаем загруженную библиотеку
		::FreeLibrary(library);
		// Выводим наличие драйвера
		return true;
	}

	/**
	 * @brief Сетевое название устройства по его уникальному номеру
	 *
	 * @details Движок отдаёт устройство уникальным номером («{GUID}»), а заказ имени у
	 *          драйвера tap-windows6 идёт по СЕТЕВОМУ названию («awh_tap3») - тому, что
	 *          распорядитель машины видит в перечне связей. Это разные строки, и сличать
	 *          их между собой нельзя
	 *
	 * @param guid уникальный номер устройства, каким его назвал движок
	 * @return     сетевое название устройства либо пустая строка
	 *
	 */
	static std::string tunnelAliasByGuid(const std::string & guid) noexcept {
		// Если уникальный номер устройства не передан
		if(guid.empty())
			// Выводим пустое название
			return std::string();
		// Размер буфера под перечень устройств
		ULONG size = 0;
		// Спрашиваем потребный размер буфера
		if(::GetAdaptersAddresses(AF_UNSPEC, GAA_FLAG_SKIP_ANYCAST | GAA_FLAG_SKIP_MULTICAST | GAA_FLAG_SKIP_DNS_SERVER, nullptr, nullptr, &size) != ERROR_BUFFER_OVERFLOW)
			// Выводим пустое название
			return std::string();
		// Буфер под перечень устройств
		std::vector <uint8_t> buffer(size, 0);
		// Перечень устройств машины
		PIP_ADAPTER_ADDRESSES adapters = reinterpret_cast <PIP_ADAPTER_ADDRESSES> (buffer.data());
		// Выполняем опрос перечня устройств
		/**
		 * @note Сличаем с `ERROR_SUCCESS`, а не с `NO_ERROR`: второе объявляется лишь
		 *       при подключённом `windows.h`, какого в этом файле нет
		 */
		if(::GetAdaptersAddresses(AF_UNSPEC, GAA_FLAG_SKIP_ANYCAST | GAA_FLAG_SKIP_MULTICAST | GAA_FLAG_SKIP_DNS_SERVER, nullptr, adapters, &size) != ERROR_SUCCESS)
			// Выводим пустое название
			return std::string();
		/**
		 * Выполняем перебор всех устройств машины
		 */
		for(PIP_ADAPTER_ADDRESSES adapter = adapters; adapter != nullptr; adapter = adapter->Next){
			// Если устройство не названо либо уникальный номер не совпал
			if((adapter->AdapterName == nullptr) || (guid.compare(adapter->AdapterName) != 0))
				// Переходим к следующему устройству
				continue;
			// Если сетевое название устройства не задано
			if(adapter->FriendlyName == nullptr)
				// Выводим пустое название
				return std::string();
			// Размер названия в узкой кодировке
			const int32_t length = ::WideCharToMultiByte(CP_UTF8, 0, adapter->FriendlyName, -1, nullptr, 0, nullptr, nullptr);
			// Если размер названия получить не удалось
			if(length <= 1)
				// Выводим пустое название
				return std::string();
			// Буфер под название устройства
			std::string result(static_cast <size_t> (length - 1), 0);
			// Выполняем перевод названия в узкую кодировку
			::WideCharToMultiByte(CP_UTF8, 0, adapter->FriendlyName, -1, result.data(), length, nullptr, nullptr);
			// Выводим сетевое название устройства
			return result;
		}
		// Выводим пустое название
		return std::string();
	}

	/**
	 * @brief Кому принадлежит адрес IPv4, названный текстом
	 *
	 * @param fmk     объект фреймворка
	 * @param log     объект ведения журнала
	 * @param address адрес устройства, названный текстом
	 * @return        уникальный номер устройства-владельца либо пустая строка
	 *
	 */
	static std::string tunnelOwnerOf(const awh::fmk_t * fmk, const awh::log_t * log, const char * address) noexcept {
		// Создаём объект работы с сетью
		awh::eth_t eth(fmk, log);
		// Объект разбора запрошенного адреса устройства
		awh::net_addr_t expected(fmk, log);
		// Если разобрать запрошенный адрес не удалось
		if(!expected.parse(address))
			// Выводим пустого владельца
			return std::string();
		// Объект запрошенного адреса устройства
		std::unique_ptr <awh::net::addr_t> value = std::make_unique <awh::net::addr_net_ipv4_t> ();
		// Переносим разобранный адрес в объект нужного вида
		awh_cast <awh::net::addr_net_ipv4_t *> (value.get())->address = expected.v4(awh::net_addr_t::endian_t::LITTLE);
		// Выводим устройство, за каким адрес числится
		return eth.iface.name(value.get());
	}

	/**
	 * @brief Тест пересоздания устройства туннеля НА ДРУГОМ УСТРОЙСТВЕ
	 *
	 * @details Устройства драйвера tap-windows6 ставит установщик драйвера, движок лишь
	 *          ОТБИРАЕТ свободное среди готовых, а заказ имени вызовом `setIface` этот
	 *          отбор направляет. Пересоздание обязано снести прежнее устройство и завести
	 *          заказанное, перенеся на него адрес
	 *
	 * @warning Пересоздание на ТО ЖЕ устройство дефекта НЕ ПОКАЗЫВАЕТ: адрес назначается
	 *          ему заново, и остаток неотличим от назначения. Оттого проверка меняет
	 *          устройство. Замерено стороной Windows 23.08.2026: до правки пересоздание
	 *          на другое устройство ОТКАЗЫВАЛО («address is already assigned to another
	 *          interface»), новое устройство оставалось без адреса вовсе, а прежнее
	 *          держало его до перезагрузки машины
	 *
	 * @note Второе устройство берётся НЕ ПО ИМЕНИ и не числом: движок сам отбирает
	 *       свободное, а проверка лишь спрашивает, какое он взял. Имён устройств здесь
	 *       не зашито - их задаёт распорядитель машины
	 *
	 */
	TEST_F(IoFixture, IoTunnelRebuildToAnotherDeviceTest){
		// Если надзорных прав у процесса нет
		if(!tunnelPrivileged())
			// Пропускаем проверку
			GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
		// Если окружение к заведению двух туннелей не готово
		if(!tunnelReady(2))
			// Пропускаем проверку
			GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
		/**
		 * Ловим записи журнала уровня «критично» за всё пересоздание
		 *
		 * @details Утверждение это заведено по дефекту, какого набор НЕ ЛОВИЛ: движок
		 *          сводил подписки уже закрытому описателю туннеля и писал об этом
		 *          «критично» на каждом пересоздании, а прогон оставался зелёным.
		 *          Печатающая проверка проходит и при молчащем движке - ловится такое
		 *          лишь утверждением
		 *
		 * @note Уровень «критично» выбран нарочно: предупреждения при пересоздании
		 *       законны (устройство стороннего драйвера снять нельзя, и движок о том
		 *       честно докладывает), а «критично» означает беду, какой быть не должно
		 *
		 * @warning Утверждение это общее для всех движков, а не для одного MS Windows:
		 *          пересоздание устройства - обычное течение работы, и записей о беде
		 *          оно не порождает ни у одного наречия
		 */
		/**
		 * @warning Перечень держится общим владением и захватывается ЗНАЧЕНИЕМ, а не
		 *          ссылкой: подписка на журнал переживает тело проверки, а движок
		 *          пишет в журнал и при разрушении - из деструктора. Захват по ссылке
		 *          стоил обвала из `~IO()` через `Interface::destroy`
		 *
		 */
		const auto critical = std::make_shared <std::vector <std::string>> ();
		// Подписываемся на записи журнала
		this->_log->subscribe([critical](const awh::log_t::flag_t flag, const std::string_view text) noexcept {
			// Если запись сообщает о беде
			if(flag == awh::log_t::flag_t::CRITICAL)
				// Запоминаем запись для разбора
				critical->emplace_back(text);
		});
		// Выполняем инициализацию движка
		ASSERT_TRUE(this->_io->initialize());
		// Закрепляем драйвер tap-windows6 ЯВНО: у Wintun заказ имени значит другое
		tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
		// Адреса второго туннеля: свои, чтобы не спорить с первым за один адрес
		static constexpr const char * SECOND_LOCAL = "10.77.1.1";
		static constexpr const char * SECOND_PEER  = "10.77.1.2";
		// Уникальные номера устройств, взятых движком
		std::string first, second;
		// Сетевое название устройства, освобождаемого под пересоздание
		std::string alias;
		// Заводим событие первого туннеля
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Ставим охранника узла: снос обязан пройти и при отказе утверждения
		const TunnelGuard guard(this->_io.get(), tid);
		// Узел обязан завестись
		ASSERT_GT(tid, 0u) << "движок не записал узел туннеля";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
		// Устанавливаем адреса концов первого туннеля
		ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL));
		ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER));
		// Фиксация настроек первого туннеля обязана пройти
		ASSERT_TRUE(this->_io->commit(tid)) << "устройство первого туннеля не заведено";
		// Запускаем событие первого туннеля
		ASSERT_TRUE(this->_io->launch(tid));
		// Запоминаем устройство, взятое движком под первый туннель
		first = this->_io->getIface(tid);
		// Название устройства обязано быть известно
		ASSERT_FALSE(first.empty()) << "движок не назвал устройство первого туннеля";
		{
			// Заводим событие второго туннеля: он нужен лишь затем, чтобы ЗАНЯТЬ другое устройство
			const awh::event::id_t next = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
			// Узел обязан завестись
			ASSERT_GT(next, 0u) << "движок не записал узел второго туннеля";
			// Устанавливаем опции события второго туннеля
			ASSERT_TRUE(this->_io->setOptions(next, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
			// Устанавливаем адреса концов второго туннеля
			ASSERT_TRUE(this->_io->setAddress(next, awh::event::address_t::IPV4, SECOND_LOCAL));
			ASSERT_TRUE(this->_io->setTarget(next, SECOND_PEER));
			// Снимаем итог фиксации настроек второго туннеля
			const bool committed = this->_io->commit(next);
			// Запоминаем устройство, взятое движком под второй туннель
			second = this->_io->getIface(next);
			// Спрашиваем сетевое название взятого устройства, пока узел жив
			alias = tunnelAliasByGuid(second);
			// Сносим узел второго туннеля: устройство его освобождается под пересоздание
			this->_io->destroy(next);
			/**
			 * Отступаем ЧЕСТНО, если второго свободного устройства на машине нет
			 *
			 * @note Отступление решается СОСТАВОМ МАШИНЫ, а не отказом движка: устройства
			 *       ставит установщик драйвера, и на машине с единственным устройством
			 *       менять его не на что. Всё прочее - дефект, и проверка обязана падать
			 */
			if(!committed || second.empty() || (second.compare(first) == 0))
				// Пропускаем проверку
				GTEST_SKIP() << "второго свободного устройства туннеля на машине нет: первое «"
				             << first << "», второе «" << (second.empty() ? std::string("не выдано") : second) << "»";
			// Сетевое название устройства обязано быть известно: без него заказать его нечем
			ASSERT_FALSE(alias.empty()) << "система не назвала сетевое имя устройства «" << second << "»";
		}
		// Заказываем узлу первого туннеля устройство, освобождённое вторым
		ASSERT_TRUE(this->_io->setIface(tid, alias)) << "движок не принял заказ устройства «" << alias << "»";
		/**
		 * Пересоздание обязано ПРОЙТИ
		 *
		 * @note Ровно здесь и отказывало до правки: адрес оставался на прежнем устройстве,
		 *       система отвечала «address is already assigned to another interface», и
		 *       заказанное устройство доставалось узлу без адреса
		 */
		ASSERT_TRUE(this->_io->rebuild(tid)) << "пересоздание на устройство «" << alias << "» отказало";
		// Устройство узла обязано смениться на заказанное
		ASSERT_EQ(this->_io->getIface(tid), second)
			<< "заказано устройство «" << alias << "» («" << second << "»), а узел остался на «" << this->_io->getIface(tid) << "»";
		/**
		 * Адрес обязан ПЕРЕЕХАТЬ на заказанное устройство
		 *
		 * @note Стережёт первую половину дефекта: новое устройство оставалось без адреса
		 */
		ASSERT_EQ(tunnelOwnerOf(this->_fmk.get(), this->_log.get(), TUNNEL_LOCAL), second)
			<< "узел переехал на «" << second << "», а адрес " << TUNNEL_LOCAL << " числится за «"
			<< tunnelOwnerOf(this->_fmk.get(), this->_log.get(), TUNNEL_LOCAL) << "»";
		/**
		 * На ПРЕЖНЕМ устройстве адреса оставаться не должно
		 *
		 * @note Стережёт вторую половину: прежнее устройство держало адрес до перезагрузки,
		 *       отравляя все последующие прогоны
		 */
		ASSERT_NE(tunnelOwnerOf(this->_fmk.get(), this->_log.get(), TUNNEL_LOCAL), first)
			<< "узел переехал с «" << first << "», а адрес " << TUNNEL_LOCAL << " остался числиться за ним";
		// Сворачиваем движок
		ASSERT_TRUE(this->_io->deinitialize());
		/**
		 * Записей о беде за всё пересоздание быть не должно
		 *
		 * @note Спрашивается это ПОСЛЕ свёртки движка: часть подписок сводится именно
		 *       ею, и спроси мы раньше - позднюю запись утверждение бы не увидело
		 */
		{
			// Собираем пойманные записи в одно сообщение об отказе
			std::string reported;
			// Выполняем перебор всех пойманных записей
			for(const auto & item : * critical)
				// Добавляем очередную запись в сообщение об отказе
				reported.append("\n  ").append(item);
			// Записей уровня «критично» пересоздание порождать не вправе
			ASSERT_TRUE(critical->empty()) << "пересоздание устройства дало " << critical->size()
				<< " записей уровня «критично»:" << reported;
		}
	}

	/**
	 * @brief Тест жизненного цикла туннеля НА ДРАЙВЕРЕ WINTUN
	 *
	 * @details Драйверов у MS Windows два, и несут они разное: Wintun переносит пакеты
	 *          сетевого уровня кольцом в общей с драйвером памяти, tap-windows6 - кадры
	 *          канального уровня через дескриптор файла. Прочие проверки набора идут по
	 *          tap-windows6, и эта - единственная, что стережёт второй путь
	 *
	 * @warning Путь Wintun не проверялся набором НИ РАЗУ: выбор по умолчанию решался
	 *          тем, лежит ли `wintun.dll` рядом с двоичным файлом, а её там не было.
	 *          Когда путь впервые запустили руками, нашлось ТРИ дефекта подряд
	 *          (сессия стороны Windows, 23.08.2026), и все три стерегутся отсюда:
	 *          ответ «адрес уже существует» принимался за успех без сличения, на том ли
	 *          устройстве адрес; итог назначения адреса отбрасывался, и безадресное
	 *          устройство отдавалось потребителю годным; адреса не снимались при
	 *          свёртывании, и остаток отравлял следующий прогон ЧЕРЕЗ ГРАНИЦУ ДРАЙВЕРА -
	 *          по tap-windows6 зелено, по Wintun мертво
	 *
	 * @note Драйвер задаётся ЯВНО: положись проверка на выбор по умолчанию - и она
	 *       молча ушла бы на tap-windows6, отчитавшись при этом об успехе Wintun
	 *
	 */
	TEST_F(IoFixture, IoTunnelLifecycleOnWintunTest){
		// Если надзорных прав у процесса нет
		if(!tunnelPrivileged())
			// Пропускаем проверку
			GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
		// Если окружение к заведению туннелей не готово
		if(!tunnelReady(1))
			// Пропускаем проверку
			GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
		// Если драйвера Wintun на машине нет
		if(!tunnelWintunReady())
			// Пропускаем проверку
			GTEST_SKIP() << "драйвер Wintun на машине недоступен: библиотека wintun.dll не загружается";
		// Выполняем инициализацию движка
		ASSERT_TRUE(this->_io->initialize());
		// Закрепляем драйвер Wintun ЯВНО: выбор по умолчанию увёл бы проверку на другой путь
		tunnelPinDriver(this->_io.get(), tunnel_driver_t::WINTUN);
		// Название заведённого устройства туннеля
		std::string iface;
		{
			// Заводим событие туннеля
			const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
			// Ставим охранника узла: снос обязан пройти и при отказе утверждения
			const TunnelGuard guard(this->_io.get(), tid);
			// Узел обязан завестись
			ASSERT_GT(tid, 0u) << "движок не записал узел туннеля";
			// Устанавливаем опции события туннеля
			ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
			// Устанавливаем адреса концов туннеля
			ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL));
			ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER));
			/**
			 * Фиксация настроек обязана ПРОЙТИ
			 *
			 * @note Библиотека драйвера загружается - значит, окружение готово, и всякий
			 *       отказ здесь есть дефект движка, а не свойство машины. Отступление
			 *       решено выше, до движка
			 */
			ASSERT_TRUE(this->_io->commit(tid)) << "драйвер Wintun доступен, а устройство не заведено";
			// Запускаем событие туннеля
			ASSERT_TRUE(this->_io->launch(tid));
			// Получаем название заведённого устройства туннеля
			iface = this->_io->getIface(tid);
			// Название устройства обязано быть известно
			ASSERT_FALSE(iface.empty()) << "движок не назвал устройство туннеля";
			// Создаём объект работы с сетью
			awh::eth_t eth(this->_fmk.get(), this->_log.get());
			// Объект разбора запрошенного адреса устройства
			awh::net_addr_t expected(this->_fmk.get(), this->_log.get());
			// Разбираем запрошенный адрес устройства
			ASSERT_TRUE(expected.parse(TUNNEL_LOCAL));
			// Объект запрошенного адреса устройства
			std::unique_ptr <awh::net::addr_t> address = std::make_unique <awh::net::addr_net_ipv4_t> ();
			// Переносим разобранный адрес в объект нужного вида
			awh_cast <awh::net::addr_net_ipv4_t *> (address.get())->address = expected.v4(awh::net_addr_t::endian_t::LITTLE);
			/**
			 * Запрошенный адрес обязан принадлежать НАШЕМУ устройству
			 *
			 * @note Стережёт первый из трёх дефектов: ответ «адрес уже существует»
			 *       принимался за успех без сличения, на том ли устройстве адрес
			 */
			const std::string owner = eth.iface.name(address.get());
			// Сличаем владельца адреса с заведённым устройством
			ASSERT_EQ(owner, iface)
				<< "движок согласился поставить адрес " << TUNNEL_LOCAL << ", а система числит его "
				<< (owner.empty() ? std::string("ни за одним устройством") : (std::string("за устройством \"") + owner + "\""));
		}
		/**
		 * Утверждения про ОСТАТОК АДРЕСА здесь НЕТ - и это намеренно
		 *
		 * @details Оно стояло бы не на том драйвере. У Wintun адаптер заводится по
		 *          требованию и при выходе исчезает целиком, а адрес уходит вместе с
		 *          ним - снимай его движок или нет. Устройство tap-windows6 постоянно,
		 *          оттого остаток живёт именно там, и стеречь его надлежит проверками
		 *          на TAP, где второй оборот жизненного цикла его и ловит
		 *
		 * @warning Не возвращать сюда: замерено ЭТАЛОНОМ сессией стороны Windows
		 *          23.08.2026 - из рабочего движка вынули ТОЛЬКО снятие адреса в
		 *          деструкторе и прогнали обоими драйверами: у tap-windows6 адрес
		 *          10.77.0.1 остался на устройстве, у Wintun не осталось ничего.
		 *          Утверждение это на Wintun ЗЕЛЕНО ПРИ СЛОМАННОМ КОДЕ, а такое хуже,
		 *          чем его отсутствие: оно изображает охват, которого нет
		 *
		 * @note Второй ловушкой того же места: пересоздание на ТО ЖЕ устройство остатка
		 *       не показывает вовсе - адрес назначается заново, и остаток от назначения
		 *       неотличим. Всякое утверждение про остаток обязано либо менять
		 *       устройство, либо смотреть после ПОЛНОГО сноса
		 */
		// Сворачиваем движок
		ASSERT_TRUE(this->_io->deinitialize());
	}

	// Адреса концов туннеля работника: свои, чтобы не спорить с прочими проверками
	static constexpr const char * WORKER_LOCAL = "10.77.9.1";
	static constexpr const char * WORKER_PEER  = "10.77.9.2";

	/**
	 * @brief Тест закрепления драйвера, выбранного самим модулем
	 *
	 * @details Драйверов у MS Windows два, и при значении AUTO выбор между ними делает
	 *          модуль - по виду устройства и по тому, какой драйвер на машине стоит.
	 *          Спросить о сделанном выборе вызывающему больше нечем: годность драйверов
	 *          наружу не выведена
	 *
	 * @note Дефект, ради какого проверка заведена: выбор делался в местной переменной, а
	 *       поле оставалось прежним - и `getDriver` отдавал AUTO даже после того, как
	 *       устройство завелось. То есть в единственном случае, когда метод и стоит
	 *       спрашивать, он отвечал «неизвестно»
	 *
	 */
	TEST_F(IoFixture, IoTunnelRecordsChosenDriverTest){
		// Если надзорных прав у процесса нет
		if(!tunnelPrivileged())
			// Пропускаем проверку
			GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
		// Если окружение к заведению туннеля не готово
		if(!tunnelReady(1))
			// Пропускаем проверку
			GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
		// Выполняем инициализацию движка
		ASSERT_TRUE(this->_io->initialize());
		// Поручаем выбор драйвера модулю
		this->_io->setDriver(awh::eth::iface_t::driver_t::AUTO);
		// До заведения устройства выбора ещё нет, и отвечать движку нечем
		ASSERT_EQ(this->_io->getDriver(), awh::eth::iface_t::driver_t::AUTO)
			<< "движок назвал драйвер прежде, чем устройство завелось";
		// Заводим событие туннеля
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Ставим охранника узла: снос обязан пройти и при отказе утверждения
		const TunnelGuard guard(this->_io.get(), tid);
		// Узел обязан завестись
		ASSERT_GT(tid, 0u) << "движок не записал узел туннеля";
		// Устанавливаем опции события туннеля
		ASSERT_TRUE(this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC));
		// Устанавливаем адреса концов туннеля
		ASSERT_TRUE(this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL));
		ASSERT_TRUE(this->_io->setTarget(tid, TUNNEL_PEER));
		// Фиксация настроек туннеля обязана пройти
		ASSERT_TRUE(this->_io->commit(tid)) << "устройство туннеля не заведено";
		/**
		 * Выбор, сделанный модулем, обязан быть назван
		 *
		 * @note Утверждение стоит на ОТЛИЧИИ от AUTO, а не на конкретном драйвере: какой
		 *       из двух будет выбран, решает состав машины, а не замысел проверки
		 */
		ASSERT_NE(this->_io->getDriver(), awh::eth::iface_t::driver_t::AUTO)
			<< "устройство заведено, а движок драйвера так и не назвал";
		// Сносим узел туннеля
		ASSERT_TRUE(this->_io->destroy(tid));
		// Сворачиваем движок
		ASSERT_TRUE(this->_io->deinitialize());
	}

	/**
	 * @brief Работник проверки снятия адреса устройства при разрушении движка
	 *
	 * @details Проверкой не является и в общем прогоне не участвует - оттого и отключён
	 *          именем. Дело его в том, чтобы завести туннельное устройство с адресом и
	 *          завершиться ШТАТНО, отдав движок деструктору: `deinitialize` здесь не
	 *          зовётся намеренно, иначе проверялся бы совсем другой путь
	 *
	 * @note Итог работник отдаёт кодом выхода: 0 - устройство заведено и адрес назначен,
	 *       3 - завести не на чем, и родителю надлежит отступить, а не зеленеть
	 *
	 */
	TEST_F(IoFixture, DISABLED_IoTunnelDestructorWorkerTest){
		/**
		 * Включаем СТРОГУЮ ПРОВЕРКУ ОПИСАТЕЛЕЙ себе самому
		 *
		 * @details С нею закрытие уже закрытого описателя валит процесс исключением, а
		 *          без неё возвращает ложь и проходит незамеченным. Тем самым второе
		 *          последствие деструктора - закрытие описателя ПОСЛЕ сноса устройства -
		 *          становится видно родителю кодом выхода, а иначе снаружи оно от
		 *          нормы неотличимо
		 *
		 * @note Обращение берётся через `GetProcAddress`: объявления его у MinGW нет
		 *       вовсе. Ставится оно только СВОЕМУ процессу и только здесь, у работника -
		 *       общего набора эта строгость не касается
		 *
		 * @warning Проверено ЩУПОМ на самой оснастке 24.08.2026: без включения двойное
		 *          закрытие молчит, с включением процесс гибнет. Флаг реестра
		 *          `FLG_ENABLE_CLOSE_EXCEPTIONS` для этого НЕ ГОДИТСЯ - он встаёт
		 *          (NtGlobalFlag=0x00400000), а двойное закрытие всё равно проходит
		 *
		 */
		{
			// Вид обращения, включающего меры защиты процесса
			using policy_fn_t = BOOL (WINAPI *)(int, PVOID, SIZE_T);
			// Получаем обращение, включающее меры защиты процесса
			const policy_fn_t setter = reinterpret_cast <policy_fn_t> (reinterpret_cast <void *> (
				::GetProcAddress(::GetModuleHandleW(L"kernel32.dll"), "SetProcessMitigationPolicy")
			));
			// Если обращение в системе есть
			if(setter != nullptr){
				// Мера строгой проверки описателей
				PROCESS_MITIGATION_STRICT_HANDLE_CHECK_POLICY policy{};
				// Требуем исключение на обращение к неверному описателю
				policy.RaiseExceptionOnInvalidHandleReference = 1;
				// Закрепляем строгость на всё время жизни процесса
				policy.HandleExceptionsPermanentlyEnabled = 1;
				// Включаем строгую проверку описателей
				setter(static_cast <int> (ProcessStrictHandleCheckPolicy), &policy, sizeof(policy));
			}
		}
		// Если надзорных прав у процесса нет, заводить туннель не на чем
		if(!tunnelPrivileged() || !tunnelReady(1))
			// Выходим с меткой отступления: родитель обязан пропустить проверку
			::_exit(3);
		// Если инициализация движка не удалась
		if(!this->_io->initialize())
			// Выходим с меткой отступления
			::_exit(3);
		// Закрепляем драйвер tap-windows6 ЯВНО: у Wintun адаптер исчезает сам, и остатку взяться неоткуда
		tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
		// Заводим событие туннеля
		const awh::event::id_t tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Если узел туннеля не записан
		if(tid == 0)
			// Выходим с меткой отступления
			::_exit(3);
		// Устанавливаем опции события туннеля
		this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC);
		// Если адреса концов туннеля установить не удалось
		if(!this->_io->setAddress(tid, awh::event::address_t::IPV4, WORKER_LOCAL) || !this->_io->setTarget(tid, WORKER_PEER))
			// Выходим с меткой отступления
			::_exit(3);
		// Если устройство туннеля завести не удалось
		if(!this->_io->commit(tid) || !this->_io->launch(tid))
			// Выходим с меткой отступления
			::_exit(3);
		/**
		 * Убеждаемся, что адрес и вправду назначен устройству
		 *
		 * @note Без этого работник, ничего не заведший, отдал бы родителю успех, а тот
		 *       увидел бы отсутствие адреса и зазеленел бы на пустом месте
		 */
		if(tunnelOwnerOf(this->_fmk.get(), this->_log.get(), WORKER_LOCAL).empty())
			// Выходим с меткой отступления
			::_exit(3);
		/**
		 * Возвращаемся ШТАТНО: движок разрушится деструктором
		 *
		 * @warning Ни `deinitialize`, ни `destroy`, ни `::_exit` здесь звать нельзя -
		 *          каждое из них уводит с проверяемого пути
		 */
	}

	/**
	 * @brief Тест снятия адреса устройства при разрушении движка
	 *
	 * @details Деструктор `~IO` изнутри своего процесса проверкой НЕ ПОКРЫВАЕТСЯ: она
	 *          живёт в том же процессе и до его последствий не доживает. Оттого дело
	 *          делается двумя процессами - работник заводит устройство с адресом и
	 *          завершается, а родитель смотрит, что осталось в системе после него
	 *
	 * @warning Устройство здесь ОБЯЗАНО быть tap-windows6: у Wintun адаптер исчезает
	 *          при выходе процесса целиком, унося адрес с собой независимо от того,
	 *          снял ли его движок. На Wintun это утверждение зелено при сломанном коде
	 *
	 * @note Дыру эту вскрыл приём порч 23.08.2026: из деструктора вынимали снятие
	 *       адреса, и весь набор туннелей молчал - адрес 10.77.9.1 оставался на
	 *       устройстве до перезагрузки машины, отравляя все последующие прогоны
	 *
	 */
	TEST_F(IoFixture, IoTunnelDestructorDropsAddressTest){
		// Если надзорных прав у процесса нет
		if(!tunnelPrivileged())
			// Пропускаем проверку
			GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
		// Если окружение к заведению туннеля не готово
		if(!tunnelReady(1))
			// Пропускаем проверку
			GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
		// Буфер под путь к своему двоичному файлу
		wchar_t executable[MAX_PATH]{0};
		// Получаем путь к своему двоичному файлу: работник это тот же файл
		ASSERT_GT(::GetModuleFileNameW(nullptr, executable, static_cast <DWORD> (sizeof(executable) / sizeof(executable[0]))), 0u);
		// Собираем строку запуска работника: своя проверка отключена именем и зовётся особо
		std::wstring command = L"\"" + std::wstring(executable) + L"\" --gtest_also_run_disabled_tests --gtest_filter=IoFixture.DISABLED_IoTunnelDestructorWorkerTest";
		// Описание порождаемого процесса
		STARTUPINFOW startup{};
		// Устанавливаем размер описания порождаемого процесса
		startup.cb = sizeof(startup);
		// Сведения о порождённом процессе
		PROCESS_INFORMATION process{};
		// Выполняем порождение работника
		ASSERT_TRUE(::CreateProcessW(executable, command.data(), nullptr, nullptr, 0 /* FALSE: макрос снят suppress.hpp ради членов перечислений AWH */, 0, nullptr, nullptr, &startup, &process)) << "порождение работника не удалось";
		/**
		 * Ожидаем завершения работника предельным сроком
		 *
		 * @note Срок здесь обязателен: заведение устройства ходит к драйверу, и зависший
		 *       работник обернулся бы вечным ожиданием вместо отказа
		 */
		const DWORD waited = ::WaitForSingleObject(process.hProcess, 120000);
		// Код выхода работника
		DWORD state = 0;
		// Снимаем код выхода работника
		const BOOL code = ::GetExitCodeProcess(process.hProcess, &state);
		// Если работник за отведённый срок не завершился
		if(waited != WAIT_OBJECT_0)
			// Снимаем работника силой: висящий процесс держит устройство занятым
			::TerminateProcess(process.hProcess, 1);
		// Закрываем описатели порождённого процесса
		::CloseHandle(process.hThread);
		::CloseHandle(process.hProcess);
		// Работник обязан завершиться сам, а не по сроку
		ASSERT_EQ(waited, static_cast <DWORD> (WAIT_OBJECT_0)) << "работник за отведённый срок не завершился";
		// Код выхода работника обязан быть снят
		ASSERT_TRUE(code) << "код выхода работника снять не удалось";
		// Если работник отступил, заводить туннель было не на чем
		if(state == 3)
			// Пропускаем проверку
			GTEST_SKIP() << "работник туннельного устройства не завёл: отступление решается составом машины";
		/**
		 * Прочие коды выхода означают гибель работника
		 *
		 * @note Гибель эта и есть второе утверждение проверки: работник включил себе
		 *       строгую проверку описателей, и закрытие описателя ПОСЛЕ сноса устройства
		 *       валит его исключением. Молчаливым такое закрытие бывает лишь без неё
		 */
		ASSERT_EQ(state, static_cast <DWORD> (0)) << "работник завершился с кодом " << state
			<< ": при строгой проверке описателей так гибнут на закрытии уже закрытого";
		// Устройство, за каким числится адрес работника после его ухода
		const std::string owner = tunnelOwnerOf(this->_fmk.get(), this->_log.get(), WORKER_LOCAL);
		/**
		 * Адреса работника в системе оставаться не должно
		 *
		 * @note Прибираем остаток ДО утверждения: иначе отказавшая проверка оставит адрес
		 *       на устройстве до перезагрузки и отравит все последующие прогоны
		 */
		if(!owner.empty()){
			// Создаём объект работы с сетью
			awh::eth_t eth(this->_fmk.get(), this->_log.get());
			// Объект разбора адреса, оставшегося на устройстве
			awh::net_addr_t expected(this->_fmk.get(), this->_log.get());
			// Если разобрать оставшийся адрес удалось
			if(expected.parse(WORKER_LOCAL)){
				// Объект оставшегося адреса устройства
				std::unique_ptr <awh::net::addr_t> value = std::make_unique <awh::net::addr_net_ipv4_t> ();
				// Переносим разобранный адрес в объект нужного вида
				awh_cast <awh::net::addr_net_ipv4_t *> (value.get())->address = expected.v4(awh::net_addr_t::endian_t::LITTLE);
				// Снимаем остаток с устройства
				eth.iface.delAddress(owner, value.get(), nullptr);
			}
		}
		// Адрес ушедшего работника обязан быть снят его деструктором
		ASSERT_TRUE(owner.empty()) << "работник ушёл, а адрес " << WORKER_LOCAL << " остался числиться за устройством «" << owner << "»";
	}
#endif

/**
 * @brief Проверка заведения туннеля ИЗ ОТКЛИКА цикла
 *
 * @details Заведение туннельного устройства блокирующее: драйвер Wintun изредка
 *          не отвечает пять минут. Оттого оно выносится в системный пул потоков,
 *          а вызывающее волокно засыпает и цикл продолжает вертеться. Путь этот
 *          отпирается ТОЛЬКО когда отклик идёт в волокне
 *
 * @note Проверка эта берёт ветвь, которой не брала ни одна другая: во всех прочих
 *       проверках туннеля `commit` зовётся из тела проверки, то есть ВНЕ волокна,
 *       и уходит прежним, блокирующим путём. Ветвь выноса прожила мёртвой ровно
 *       потому, что её никто не брал
 *
 */
#if defined(_WIN32) || defined(_WIN64)
TEST_F(IoFixture, IoTunnelCommitFromCallbackTest){
	// Если надзорных прав у процесса нет
	if(!tunnelPrivileged())
		// Пропускаем проверку
		GTEST_SKIP() << "заведение туннельного устройства требует надзорных прав";
	// Если окружение к заведению туннеля не готово
	if(!tunnelReady(1))
		// Пропускаем проверку
		GTEST_SKIP() << "связи под туннели не заведены распорядителем машины";
	// Закрепляем драйвер устройств туннеля
	tunnelPinDriver(this->_io.get(), tunnel_driver_t::TAP);
	/**
	 * Перечень записей журнала, собранных за время проверки
	 *
	 * @warning Держится он общим владением и захватывается ЗНАЧЕНИЕМ, а не ссылкой:
	 *          подписка на журнал переживает тело проверки, а движок пишет в журнал
	 *          и при разрушении - из деструктора. Захват по ссылке дал бы обращение
	 *          к снесённой переменной, и это здесь уже случалось: обвал приходил из
	 *          `~IO()` через `Interface::destroy`
	 *
	 */
	const auto journal = std::make_shared <std::vector <std::string>> ();
	// Подписываемся на записи журнала: отказ обязан быть слышен
	this->_log->subscribe([journal]([[maybe_unused]] const awh::log_t::flag_t flag, const std::string_view text) noexcept {
		// Запоминаем запись для разбора
		journal->emplace_back(text);
	});
	// Признак того, что отклик позвался внутри волокна
	bool inside = false;
	// Признак согласия движка завести устройство из отклика
	bool raised = false;
	// Признак начала работы отклика: заслон от повторного захода
	bool started = false;
	/**
	 * Признак ОКОНЧАНИЯ работы отклика
	 *
	 * @warning Выходить из опроса надо по окончанию, а не по началу. Отклик заводит
	 *          устройство чужим потоком и на этом ЗАСЫПАЕТ; пробуждение приходит
	 *          завершением на порт, то есть следующим оборотом цикла. Выйди опрос
	 *          по началу работы - оборотов больше не будет, будить волокно станет
	 *          нечем, и отказ выглядел бы дефектом движка. Промах этот здесь уже
	 *          был допущен однажды
	 *
	 */
	bool handled = false;
	// Признаки согласия движка на каждом шаге настройки
	bool options = false, address = false, target = false;
	// Опознаватель узла туннеля, заведённого откликом
	awh::event::id_t tid = 0;
	// Добавляем событие интервала
	const awh::event::id_t interval = this->_io->event(awh::event::node_t::INTERVAL, awh::event::family_t::TIMER);
	// Событие обязано завестись
	ASSERT_GT(interval, 0u);
	// Устанавливаем задержку интервала
	this->_io->setTimeout(interval, awh::event::action_t::NONE, 50);
	// Выполняем инициализацию движка
	ASSERT_TRUE(this->_io->initialize());
	// Выполняем фиксацию настроек события интервала
	ASSERT_TRUE(this->_io->commit(interval));
	/**
	 * Отклик заводит туннель ИЗ ЦИКЛА - то есть ровно так, как это делает
	 * настоящий потребитель, а не тело проверки
	 */
	this->_io->on(interval, [this, &inside, &raised, &handled, &started, &tid, &options, &address, &target]([[maybe_unused]] const awh::event::id_t eid, const awh::event::status_t status) noexcept -> void {
		// Если статус события не успешен либо отклик уже отработал
		if((status != awh::event::status_t::SUCCESS) || started)
			// Выходим из отклика
			return;
		// Отмечаем отклик начавшим работу
		started = true;
		// Запоминаем, идёт ли отклик внутри волокна
		inside = (awh::fiber::current() != nullptr);
		// Заводим узел туннеля
		tid = this->_io->event(awh::event::node_t::TUNNEL, awh::event::family_t::IPV4);
		// Если узел туннеля не заведён
		if(tid == 0){
			// Отмечаем отклик отработавшим
			handled = true;
			// Выходим из отклика
			return;
		}
		// Устанавливаем опции события туннеля
		options = this->_io->setOptions(tid, awh::event::options::NO_IO_BLOCK | awh::event::options::CLOSE_ON_EXEC);
		// Устанавливаем адреса концов туннеля
		address = this->_io->setAddress(tid, awh::event::address_t::IPV4, TUNNEL_LOCAL);
		target = this->_io->setTarget(tid, TUNNEL_PEER);
		// Выполняем фиксацию настроек туннеля прямо из отклика
		raised = this->_io->commit(tid);
		// Отмечаем отклик отработавшим: до сюда управление доходит уже ПОСЛЕ сна волокна
		handled = true;
	});
	// Выполняем запуск события интервала
	ASSERT_TRUE(this->_io->launch(interval));
	// Определяем срок ожидания отработки отклика
	const auto deadline = (std::chrono::steady_clock::now() + std::chrono::seconds(30));
	// Выполняем опрос событий до отработки отклика
	while(!handled && (std::chrono::steady_clock::now() < deadline) && this->_io->poll(__AWH_TEST_POLL_SLICE__));
	// Отклик обязан был отработать, иначе проверка ничего не проверяет
	ASSERT_TRUE(handled) << "отклик не сработал - проверка стала холостой";
	// Отклик обязан идти внутри волокна
	ASSERT_TRUE(inside) << "отклик идёт ВНЕ волокна: вынос заведения устройства в чужой поток не отпирается";
	// Узел туннеля обязан был завестись
	ASSERT_GT(tid, 0u) << "движок не записал узел туннеля из отклика";
	/**
	 * Опыт сличения: тот же узел, но фиксация ИЗ ТЕЛА проверки
	 *
	 * @note Сличение это отделяет одну разницу от другой. Пройди фиксация здесь и
	 *       откажи в отклике - виновата обстановка вызова, а не узел и не окружение
	 */
	const bool outside = (raised ? false : this->_io->commit(tid));
	/**
	 * Устройство обязано было завестись
	 *
	 * @note Это и есть суть проверки: заведение прошло ЧУЖИМ потоком, волокно на
	 *       нём уснуло, пробуждение пришло завершением на порт, и кадр вызова
	 *       пережил сон вместе со всеми своими переменными
	 */
	ASSERT_TRUE(raised) << "устройство не заведено из отклика: путь выноса работы в чужой поток неисправен"
		<< " (опции=" << options << ", адрес=" << address << ", цель=" << target << ")"
		<< ", из тела проверки=" << outside
		<< "\nЗаписи журнала (" << journal->size() << "):\n" << [&journal]() noexcept -> std::string {
			// Место под свод записей журнала
			std::string result;
			// Выполняем перебор всех записей журнала
			for(const auto & item : * journal)
				// Дополняем свод очередной записью
				result.append("  ").append(item).append("\n");
			// Выводим собранный свод
			return result;
		}();
	// Сносим заведённые узлы
	this->_io->destroy(tid);
	this->_io->destroy(interval);
}
#endif

/**
 * Возвращаем системные макросы потребителю библиотеки:
 * имена, подавленные в начале файла, снова принадлежат ему
 */
#include <sys/macro/restore.hpp>
