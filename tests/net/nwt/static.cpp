/**
 * @file static.cpp
 * @date 2025-12-14
 *
 * @license{LicenseRef-AWH-1.0}
 *
 * @author Yuriy Lobarev
 *
 * @telegram{forman}
 * @phone{+7 (910) 983-95-90}
 *
 * @email forman@anyks.com
 * @site https://anyks.com
 *
 * @brief Статические тесты модуля определения типов сетевых адресов — проверка создания и сброса объекта модуля,
 *        а также корректности распознавания URL, доменов, IP- и MAC-адресов, e-mail и путей файловой системы
 *
 * @copyright Copyright © 2025
 *
 */

/**
 * Подключаем заголовочный файлы проекта
 */
#include "nwt.hpp"

/**
 * @brief Объект модуля на уровне файла, закрепляющий порядок построения статических объектов
 *
 * @details Объект этот заведён НЕ для удобства, а ради самого своего построения. Списки
 *          доменных зон модуля прежде были объектами пространства имён, и объект модуля,
 *          объявленный потребителем на уровне файла, строился РАНЬШЕ них: конструктор
 *          трогал недостроенные наборы, поля их несли мусор, коэффициент загрузки выходил
 *          `inf`, и приложение падало ДО входа в `main` с необработанным
 *          `std::overflow_error: __next_prime overflow`. Списки переведены на
 *          функционально-статические — построение первым же обращением, в каком бы
 *          порядке оно ни пришло
 *
 * @warning Возврат прежнего устройства валит ВЕСЬ набор проверок до входа в `main`, ещё
 *          до вывода списка проверок. Это и есть закрепление: молча дефект не вернётся
 *
 * @note Санитайзеры этот класс дефектов НЕ ловят: под `-fsanitize=address` подмена выдачи
 *       памяти идёт мимо нашего распределителя, и падения не случается вовсе. Ловится
 *       только обычной сборкой
 *
 */
static awh::nwt_t __awh_static_storage_nwt__(nullptr);

/**
 * @brief Тест построения объекта модуля на уровне файла
 *
 * @details Закрепляет порядок построения статических объектов: объект выше построен ДО
 *          входа в `main`, и здесь проверяется, что он не просто пережил построение, а
 *          работоспособен — списки доменных зон заполнены и разбор ими пользуется
 *
 */
TEST_F(NwtFixture, StaticStorageNwtTest){
	// Проверяем, что объект, построенный на уровне файла, разбирает доменное имя
	const awh::nwt_t::url_t url = __awh_static_storage_nwt__.parse("https://example.com/path");
	// Проверяем, что разбор опознал запись адресом
	ASSERT_EQ(url.type, awh::nwt_t::types_t::URL);
	// Проверяем, что доменная зона выделена, то есть списки зон заполнены
	ASSERT_STREQ(url.domain.c_str(), "com");
}

/**
 * @brief Тест создания объекта работы со списком параметров URL
 *
 */
TEST_F(NwtFixture, CreateNwtTest){
	// Проверяем, что объект работы со списком параметров URL создан
	ASSERT_TRUE(this->_nwt != nullptr);
	// Сбрасываем объект работы со списком параметров URL
	this->_nwt.reset();
	// Проверяем, что объект работы со списком параметров URL сброшен
	ASSERT_TRUE(this->_nwt == nullptr);
}

/**
 * @brief Тест сброса и повторного создания объекта работы со списком параметров URL
 *
 */
TEST_F(NwtFixture, ResetAndCreateNwtTest){
	// Проверяем, что объект работы со списком параметров URL создан
	ASSERT_TRUE(this->_nwt != nullptr);
	// Сбрасываем объект работы со списком параметров URL
	this->_nwt.reset();
	// Проверяем, что объект работы со списком параметров URL сброшен
	ASSERT_TRUE(this->_nwt == nullptr);
	// Создаём объект работы со списком параметров URL заново
	this->_nwt = std::make_unique <awh::nwt_t> (this->_log.get());
	// Проверяем, что объект работы со списком параметров URL создан
	ASSERT_TRUE(this->_nwt != nullptr);
}

/**
 * @brief Тест повторного создания объекта работы со списком параметров URL
 *
 */
TEST_F(NwtFixture, ReCreateNwtTest){
	// Проверяем, что объект работы со списком параметров URL создан
	ASSERT_TRUE(this->_nwt != nullptr);
	// Создаём объект работы со списком параметров URL заново
	this->_nwt = std::make_unique <awh::nwt_t> (this->_log.get());
	// Проверяем, что объект работы со списком параметров URL создан
	ASSERT_TRUE(this->_nwt != nullptr);
}
