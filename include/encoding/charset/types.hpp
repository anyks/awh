/**
 * @file: types.hpp
 * @date: 2026-08-03
 * @license: LicenseRef-AWH-1.0
 *
 * @telegram: @forman
 * @author: Yuriy Lobarev
 * @phone: +7 (910) 983-95-90
 * @email: forman@anyks.com
 * @site: https://anyks.com
 *
 * @brief Заголовочный файл общих определений модуля перекодировки — обозначения
 *        кодировок, порядок обращения с непредставимыми символами и виды записей
 *        таблиц соответствия кодировок Юникоду
 *
 * @copyright: Copyright © 2026
 *
 */

/**
 * Экранируем повторную инициализацию модуля
 */
#ifndef __AWH_CHARSET_TYPES__
#define __AWH_CHARSET_TYPES__

/**
 * Стандартные заголовочные файлы
 */
#include <cstdint>

/**
 * Подключаем заголовочные файлы проекта
 */
#include "../../sys/global.hpp"

/**
 * Снимаем на время объявлений макросы, чьи имена заняты
 * членами перечислений ниже (возвращает их macro_pop.hpp в конце файла)
 */
#include "../../sys/macro_push.hpp"

/**
 * @brief Основное пространство имён
 *
 */
namespace awh {
	/**
	 * Используем стандартное пространство имён
	 */
	using namespace std;

	/**
	 * @brief Пространство имён модуля перекодировки
	 *
	 */
	namespace charset {
		/**
		 * @brief Признак отсутствия соответствия символа кодировке
		 *
		 */
		constexpr uint16_t UNMAPPED = 0xFFFF;

		/**
		 * @brief Обозначение кодировки текста
		 *
		 * @details Набор кодировок задан стандартом кодировок консорциума WHATWG,
		 *          перечисляющим кодировки, обязательные к поддержке в сети, и дополнен
		 *          кодировками ASCII и ISO-8859-1, задаваемыми отдельными стандартами.
		 *          Многобайтовые кодировки восточных письменностей модулем не задаются.
		 *
		 */
		enum class encoding_t : uint8_t {
			NONE         = 0x00, // Кодировка не установлена
			ASCII        = 0x01, // Кодировка ASCII
			UTF8         = 0x02, // Кодировка UTF-8
			ISO8859_1    = 0x03, // Кодировка ISO-8859-1, латиница №1
			ISO8859_2    = 0x04, // Кодировка ISO-8859-2, латиница №2
			ISO8859_3    = 0x05, // Кодировка ISO-8859-3, латиница №3
			ISO8859_4    = 0x06, // Кодировка ISO-8859-4, латиница №4
			ISO8859_5    = 0x07, // Кодировка ISO-8859-5, кириллица
			ISO8859_6    = 0x08, // Кодировка ISO-8859-6, арабское письмо
			ISO8859_7    = 0x09, // Кодировка ISO-8859-7, греческое письмо
			ISO8859_8    = 0x0A, // Кодировка ISO-8859-8, еврейское письмо
			ISO8859_9    = 0x0B, // Кодировка ISO-8859-9, латиница №5
			ISO8859_10   = 0x0C, // Кодировка ISO-8859-10, латиница №6
			ISO8859_11   = 0x0D, // Кодировка ISO-8859-11, тайское письмо
			ISO8859_13   = 0x0E, // Кодировка ISO-8859-13, латиница №7
			ISO8859_14   = 0x0F, // Кодировка ISO-8859-14, латиница №8
			ISO8859_15   = 0x10, // Кодировка ISO-8859-15, латиница №9
			ISO8859_16   = 0x11, // Кодировка ISO-8859-16, латиница №10
			CP866        = 0x12, // Кодировка CP866, кириллица DOS
			CP874        = 0x13, // Кодировка CP874, тайское письмо
			CP1250       = 0x14, // Кодировка CP1250, языки Средней Европы
			CP1251       = 0x15, // Кодировка CP1251, кириллица
			CP1252       = 0x16, // Кодировка CP1252, языки Западной Европы
			CP1253       = 0x17, // Кодировка CP1253, греческое письмо
			CP1254       = 0x18, // Кодировка CP1254, турецкое письмо
			CP1255       = 0x19, // Кодировка CP1255, еврейское письмо
			CP1256       = 0x1A, // Кодировка CP1256, арабское письмо
			CP1257       = 0x1B, // Кодировка CP1257, языки Прибалтики
			CP1258       = 0x1C, // Кодировка CP1258, вьетнамское письмо
			KOI8_R       = 0x1D, // Кодировка KOI8-R, кириллица
			KOI8_U       = 0x1E, // Кодировка KOI8-U, украинское письмо
			MAC_ROMAN    = 0x1F, // Кодировка MacRoman, латиница Macintosh
			MAC_CYRILLIC = 0x20  // Кодировка MacCyrillic, кириллица Macintosh
		};

		/**
		 * @brief Порядок обращения с символами, кодировке не представимыми
		 *
		 */
		enum class replace_t : uint8_t {
			STRICT  = 0x00, // Перекодировка отклоняется целиком
			SKIP    = 0x01, // Непредставимые символы опускаются
			REPLACE = 0x02  // Непредставимые символы заменяются знаком вопроса
		};

		/**
		 * @brief Соответствие кодового значения Юникода байту кодировки
		 *
		 */
		typedef struct __AWH_SHARED_EXPORT__ Mapping {
			// Кодовое значение символа Юникода
			uint16_t code;
			// Байт кодировки, которому соответствует кодовое значение
			uint8_t byte;
		} mapping_t;

		/**
		 * @brief Таблица соответствия однобайтовой кодировки Юникоду
		 *
		 */
		typedef struct __AWH_SHARED_EXPORT__ Table {
			// Обозначение кодировки текста
			encoding_t encoding;
			// Каноническое имя кодировки
			const char * name;
			// Соответствие байтов кодировки кодовым значениям Юникода
			const uint16_t * unicode;
			// Соответствие кодовых значений Юникода байтам кодировки
			const mapping_t * mappings;
			// Количество записей обратного соответствия
			size_t count;
		} table_t;

		/**
		 * @brief Соответствие имени кодировки её обозначению
		 *
		 */
		typedef struct __AWH_SHARED_EXPORT__ Labeling {
			// Имя кодировки, приведённое к нормальному виду
			const char * name;
			// Обозначение кодировки текста
			encoding_t encoding;
		} labeling_t;
	};
};

/**
 * Возвращаем макросы, снятые в начале файла
 */
#include "../../sys/macro_pop.hpp"

#endif // __AWH_CHARSET_TYPES__
