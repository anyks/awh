/**
 * @file xml.cpp
 * @date 2026-08-11
 *
 * @license{LicenseRef-AWH-1.0}
 *
 * @author Yuriy Lobarev
 *
 * @telegram{forman}
 * @phone{+7 (910) 983-95-90}
 *
 * @email forman@anyks.com
 * @site https://anyks.com
 *
 * @brief Инструмент фаззинга кодека разметки XML — построение полуструктурированного
 *        текста разметки с точечной порчей, подача его чтению целиком и кусками
 *        произвольного размера, сборка дерева и его перезапись для поиска аварийных
 *        завершений, выходов за границы буфера и расхождений разбора
 *
 * @copyright Copyright © 2026
 *
 */

/**
 * Стандартные заголовочные файлы
 */
#include <string>
#include <vector>
#include <algorithm>
#include <random>
#include <functional>
#include <cstdio>
#include <cstdlib>
#include <cstdint>

/**
 * Подключаем заголовочный файл проекта
 */
#include <sys/log.hpp>
#include <codec/xml/xml.hpp>

/**
 * @brief Пространство имён проверок этого файла
 *
 * @note Держится оно безымянным намеренно: проверки кодеков собираются одной
 *       программою, и одноимённые построения разных файлов иначе сходятся в
 *       одно, порождая порчу вдали от места её причины
 *
 */
namespace {
	/**
	 * @brief Объект журнала проверок с отключённым выводом
	 *
	 * @details Вывод отключается назначением пустого перечня приёмников: отказы
	 *          разбора проверки наводят намеренно, и журнал их засорял бы выдачу
	 *
	 */
	struct Silent {
		/**
		 * @brief Функция получения объекта фреймворка проверок
		 *
		 * @details Объект заводится статикою местною, а не общею файла: заведение его
		 *          порядком построения статики оканчивается падением ещё до входа в
		 *          проверки, ибо фреймворк сам опирается на статику из библиотеки
		 *
		 * @return объект фреймворка проверок
		 *
		 */
		static const awh::fmk_t & framework() noexcept {
			// Объект фреймворка проверок
			static awh::fmk_t fmk;
			// Выводим объект фреймворка проверок
			return fmk;
		}
		// Объект журнала проверок
		awh::log_t log;
		/**
		 * @brief Конструктор
		 *
		 */
		Silent() noexcept : log(&Silent::framework()) {
			// Выполняем отключение вывода логов
			this->log.mode({});
		}
	};
	/**
	 * @brief Функция получения объекта журнала проверок
	 *
	 * @return объект журнала проверок
	 *
	 */
	const awh::log_t * logger() noexcept {
		// Объект журнала проверок
		static Silent silent;
		// Выводим объект журнала проверок
		return &silent.log;
	}
}

/**
 * Используем стандартное пространство имён
 */
using namespace std;

/**
 * Подписываемся на пространство имён контейнера разметки
 */
using namespace awh::codec;

/**
 * @brief Внутренние вспомогательные средства генератора (внутренняя компоновка)
 *
 */
namespace {
	/**
	 * @brief Учёт проделанной работы
	 *
	 */
	struct Statistic {
		// Количество построенных текстов разметки
		uint64_t texts;
		// Количество испорченных текстов разметки
		uint64_t corrupted;
		// Количество текстов, разобранных чтением до конца
		uint64_t survived;
		// Количество выданных чтением событий
		uint64_t events;
		// Количество собранных деревьев разметки
		uint64_t trees;
		// Количество перезаписей дерева разметки
		uint64_t rewrites;
		// Количество построенных заведомо правильных текстов разметки
		uint64_t intact;
		// Количество заведомо правильных текстов, разобранных чтением до конца
		uint64_t accepted;
		// Количество снятых владеющих значений
		uint64_t values;
		// Количество значений, пересобранных потоковым сборщиком
		uint64_t builds;
		// Количество значений, привитых обратно в дерево разметки
		uint64_t grafts;
		// Количество узлов, снесённых из дерева разметки по пути
		uint64_t removals;
		// Количество узлов, сброшенных в дереве разметки по пути
		uint64_t resets;
		/**
		 * @brief Конструктор
		 *
		 */
		Statistic() noexcept :
		 texts(0), corrupted(0), survived(0),
		 events(0), trees(0), rewrites(0), intact(0), accepted(0),
		 values(0), builds(0), grafts(0), removals(0), resets(0) {}
	/**
	 * Учёт проделанной работы
	 *
	 * @note Имя «stat» тут не годится: у MinGW заголовки объявляют «struct stat»
	 *       в области видимости, и обращение к учёту становится двусмысленным
	 */
	} totals;

	/**
	 * @brief Атрибут узла, запомненный для сличения
	 *
	 */
	struct Property {
		// Префикс имени атрибута и его местное имя
		string prefix, local;
		// Обозначение пространства имён атрибута
		string uri;
		// Значение атрибута, приведённое к окончательному виду
		string value;
		// Признак того, что значение взято из объявления по умолчанию
		bool defaulted;
		// Место атрибута в исходном тексте
		uint32_t line, column;
		/**
		 * @brief Конструктор
		 *
		 */
		Property() noexcept : defaulted(false), line(0), column(0) {}
		/**
		 * @brief Оператор сравнения
		 *
		 * @param other атрибут для сравнения
		 * @return      результат сравнения
		 *
		 */
		bool operator != (const Property & other) const noexcept {
			// Выполняем сличение всех признаков атрибута
			return (
				(this->prefix != other.prefix) ||
				(this->local != other.local) ||
				(this->uri != other.uri) ||
				(this->value != other.value) ||
				(this->defaulted != other.defaulted) ||
				(this->line != other.line) ||
				(this->column != other.column)
			);
		}
	};

	/**
	 * @brief Событие разбора, запомненное для сличения
	 *
	 */
	struct Event {
		// Разновидность события
		uint8_t event;
		// Префикс имени узла и его местное имя
		string prefix, local;
		// Обозначение пространства имён узла
		string uri;
		// Содержимое события
		string text;
		// Глубина вложенности узла события
		uint32_t depth;
		// Место начала события в исходном тексте
		uint32_t line, column;
		// Смещение начала события в исходном тексте
		uint64_t position;
		// Обращение с пробельным содержимым в узле события
		uint8_t space;
		// Признак того, что узел записан самозакрывающейся меткой
		bool empty;
		// Перечень атрибутов узла события
		vector <Property> attributes;
		// Перечень объявлений пространств имён узла события
		vector <Property> bindings;
		/**
		 * @brief Конструктор
		 *
		 */
		Event() noexcept : event(0), depth(0), line(0), column(0), position(0), space(0), empty(false) {}
		/**
		 * @brief Оператор сравнения
		 *
		 * @param other событие для сравнения
		 * @return      результат сравнения
		 *
		 */
		bool operator != (const Event & other) const noexcept {
			/**
			 * Если разошёлся хоть один признак самого события
			 */
			if((this->event != other.event) || (this->prefix != other.prefix) ||
			   (this->local != other.local) || (this->uri != other.uri) ||
			   (this->text != other.text) || (this->depth != other.depth) ||
			   (this->line != other.line) || (this->column != other.column) ||
			   (this->position != other.position) ||
			   (this->space != other.space) || (this->empty != other.empty) ||
			   (this->attributes.size() != other.attributes.size()) ||
			   (this->bindings.size() != other.bindings.size()))
				// Выводим результат сличения событий
				return true;
			/**
			 * Выполняем перебор всех атрибутов узла события
			 */
			for(size_t i = 0; i < this->attributes.size(); i++){
				// Если очередной атрибут разошёлся
				if(this->attributes.at(i) != other.attributes.at(i))
					// Выводим результат сличения событий
					return true;
			}
			/**
			 * Выполняем перебор всех объявлений пространств имён узла события
			 */
			for(size_t i = 0; i < this->bindings.size(); i++){
				// Если очередное объявление разошлось
				if(this->bindings.at(i) != other.bindings.at(i))
					// Выводим результат сличения событий
					return true;
			}
			// Выводим результат сличения событий
			return false;
		}
	};

	/**
	 * @brief Метод выборки настроек разбора текста разметки
	 *
	 * @param engine источник псевдослучайных чисел
	 * @return       настройки разбора текста разметки
	 *
	 */
	xml::reader_t::settings_t settings(mt19937 & engine) noexcept {
		// Собираемые настройки разбора текста разметки
		xml::reader_t::settings_t result;
		// Разряды, задающие сочетание признаков разбора
		const uint32_t flags = static_cast <uint32_t> (engine());
		// Устанавливаем флаг разрешения префиксов по договору о пространствах имён
		result.namespaces = ((flags & 0x01) != 0);
		// Устанавливаем флаг подстановки ссылок на объявленные сущности
		result.entities = ((flags & 0x02) != 0);
		// Устанавливаем флаг выдачи примечаний отдельным событием
		result.emitComments = ((flags & 0x04) != 0);
		// Устанавливаем флаг выдачи указаний обработчику отдельным событием
		result.emitProcessing = ((flags & 0x08) != 0);
		// Устанавливаем флаг отделения незначимого пробельного содержимого
		result.separateSpaces = ((flags & 0x10) != 0);
		// Устанавливаем флаг подстановки значений атрибутов, объявленных по умолчанию
		result.defaults = ((flags & 0x20) != 0);
		/**
		 * Устанавливаем флаг отказа при ссылке на внешнюю сущность в содержимом
		 *
		 * @note Флаг этот прежде не перебирался вовсе, а вместе с ним недостижимы были
		 *       обе ветви обхождения со ссылкой: и отказ при строгости, и распознание
		 *       с пропуском при снятой строгости
		 */
		result.externals = ((flags & 0x40) != 0);
		/**
		 * Устанавливаем флаг склеивания подряд идущих кусков текстового содержимого
		 *
		 * @details Склейка выключена у кодека по умолчанию, и путь частичной выдачи
		 * содержимого - тот самый, каким пользуются без указания настроек. Прежде
		 * ворошитель гонял одну лишь склейку включённой: сличать выдачу иначе было
		 * нечем, ведь без склейки событий выходит тем больше, чем мельче куски. Сличать
		 * следует само содержимое, а не его нарезку на события: подряд идущее содержимое
		 * одного вида сводится в одно событие перед сличением
		 *
		 * @note Выключается склейка у трети проходов: включённая проверяет равенство
		 *       выдачи знак в знак, выключенная - равенство содержимого
		 */
		result.mergeText = ((flags & 0x03) != 0x03);
		/**
		 * Если требуется поджать пределы разбора
		 */
		if((flags & 0xC0) == 0xC0){
			// Устанавливаем наибольшую допустимую глубину вложенности узлов
			result.maxDepth = (1 + (engine() % 10));
			// Устанавливаем наибольшую допустимую длину имени в знаках
			result.maxName = (1 + (engine() % 48));
			// Устанавливаем наибольшее допустимое количество атрибутов у одного узла
			result.maxAttributes = (engine() % 6);
			// Устанавливаем наибольшее допустимое количество объявленных сущностей
			result.maxEntities = (engine() % 4);
			// Устанавливаем наибольший допустимый общий объём подстановки сущностей
			result.maxExpansion = (engine() % 1024);
			/**
			 * Устанавливаем наибольший допустимый объём одного события
			 *
			 * @details Поджимается предел лишь при включённой склейке. Предел задан на
			 * одно событие, а с выключенной склейкой число событий задаётся нарезкой:
			 * содержимое выдаётся частями, каждая из которых предела не достигает, тогда
			 * как то же содержимое, пришедшее целиком, выходит одним событием и предел
			 * перебирает. Сочетание это чуждо всякой нарезке по самому своему устройству,
			 * и сличать по нему подачу целиком с подачей кусками нечем
			 */
			result.maxEvent = (result.mergeText ? (engine() % 512) : 0);
		}
		// Выводим собранные настройки разбора текста разметки
		return result;
	}

	/**
	 * @brief Метод построения имени узла разметки
	 *
	 * @param engine источник псевдослучайных чисел
	 * @param valid  признак построения заведомо правильной разметки
	 * @return       построенное имя узла разметки
	 *
	 */
	string title(mt19937 & engine, const bool valid) noexcept {
		// Результат работы функции - построенное имя узла разметки
		string result;
		/**
		 * Выполняем выборку записи имени узла разметки
		 */
		switch(engine() % 16){
			// Дописываем имя с префиксом объявленного пространства имён
			case 0:
			case 1:
			case 2:
			case 3: result.append("ns:"); break;
			// Дописываем имя с префиксом необъявленного пространства имён
			case 4: if(!valid) result.append("nope:"); break;
			// Дописываем имя с отведённым договором префиксом
			case 5: if(!valid) result.append("xml:"); break;
			// Дописываем имя с двумя разделителями префикса
			case 6: if(!valid) result.append("a:b:"); break;
		}
		// Дописываем местное имя узла разметки
		result.append(1, static_cast <char> ('a' + (engine() % 4)));
		/**
		 * Если требуется дописать имя длиной сверх обычного
		 */
		if((engine() % 12) == 0)
			// Дописываем продолжение имени узла разметки
			result.append(40, 'z');
		// Выводим построенное имя узла разметки
		return result;
	}

	/**
	 * @brief Метод построения содержимого узла разметки
	 *
	 * @param engine   источник псевдослучайных чисел
	 * @param valid    признак построения заведомо правильной разметки
	 * @param declared признак объявленных описанием типа сущностей
	 * @param external признак объявленной описанием типа ВНЕШНЕЙ сущности
	 * @return         построенное содержимое узла разметки
	 */
	string content(mt19937 & engine, const bool valid, const bool declared, const bool external) noexcept {
		// Результат работы функции - построенное содержимое узла разметки
		string result;
		/**
		 * Если описание типа объявило внешнюю сущность
		 *
		 * @note Ссылка на неё выбирается ПРЕЖДЕ прочего содержимого и с высокой долей:
		 *       вложенная в общий перебор, она выпадала раз на шестьдесят тысяч проходов,
		 *       и обе ветви обхождения со ссылкою оставались ворошителю недостижимы
		 */
		if(external && ((engine() % 2) == 0)){
			// Дописываем содержимое со ссылкой на внешнюю сущность
			result.append(valid ? "до&ext;после" : "до&ext");
			// Выводим построенное содержимое узла разметки
			return result;
		}
		/**
		 * Выполняем выборку записи содержимого узла разметки
		 */
		switch(engine() % 16){
			// Дописываем простое содержимое
			case 0: result.append("text"); break;
			// Дописываем пробельное содержимое
			case 1: result.append(((engine() % 4) == 0) ? string(80, ' ') : string(" \t\r\n ")); break;
			// Дописываем содержимое со ссылками на отведённые договором сущности
			case 2: result.append("&lt;&amp;&gt;&quot;&apos;"); break;
			// Дописываем содержимое с числовыми ссылками
			case 3: result.append("&#65;&#x416;&#x10FFFF;&#0;"); break;
			// Дописываем содержимое со ссылкой на объявленную сущность
			case 4: {
				// Если сущности объявлены описанием типа документа
				if(declared)
					// Дописываем содержимое со ссылками на объявленные сущности
					result.append(valid ? "&plain;&plain;&plain;&plain;&plain;&plain;&plain;&plain;" : "&plain; &markup; &loop;");
				/**
				 * Иначе, если описание типа объявило ВНЕШНЮЮ сущность
				 *
				 * @note Ссылка эта прежде не порождалась вовсе: описание типа сущность
				 *       объявляло, а ссылки на неё в содержимом не было ни одной, и ветвь
				 *       обхождения с нею ворошителю была недостижима
				 */
				else if(external)
					// Дописываем содержимое со ссылкой на внешнюю сущность
					result.append("до&ext;после");
				// Иначе дописываем содержимое со ссылками на отведённые договором сущности
				else result.append("&amp;&amp;&amp;");
			} break;
			// Дописываем содержимое со ссылкой на необъявленную сущность
			case 5: if(!valid) result.append("&missing;"); break;
			// Дописываем содержимое с незавершённой ссылкой
			case 6: if(!valid) result.append("&nope tail"); break;
			// Дописываем содержимое со знаками не из набора ASCII
			case 7: result.append("содержимое"); break;
			// Дописываем содержимое с недопустимой последовательностью UTF-8
			case 8: if(!valid) result.append("bad\xC3\x28 tail"); break;
			// Дописываем содержимое с недопустимой для разметки последовательностью
			case 9: if(!valid) result.append("a]]>b"); break;
			// Дописываем длинное содержимое
			case 10: result.append(200, 'x'); break;
			// Дописываем содержимое числом
			case 11: result.append(" -1234 "); break;
			// Дописываем содержимое, набранное произвольными знаками
			case 12: {
				// Если требуется построить заведомо правильную разметку
				if(valid)
					// Выходим из выборки записи содержимого узла разметки
					break;
				// Количество знаков произвольного набора
				const uint32_t length = (engine() % 8);
				/**
				 * Выполняем дозапись знаков произвольного набора
				 */
				for(uint32_t i = 0; i < length; i++)
					// Дописываем произвольный знак
					result.push_back(static_cast <char> (engine() % 256));
			} break;
		}
		// Выводим построенное содержимое узла разметки
		return result;
	}

	/**
	 * @brief Метод построения узла разметки со всем его содержимым
	 *
	 * @param result текст разметки, к которому дописывается построенный узел
	 * @param engine источник псевдослучайных чисел
	 * @param depth    текущая глубина вложенности узла разметки
	 * @param valid    признак построения заведомо правильной разметки
	 * @param declared признак объявленных описанием типа сущностей
	 *
	 * @param external признак объявленной описанием типа ВНЕШНЕЙ сущности
	 */
	void element(string & result, mt19937 & engine, const uint32_t depth, const bool valid, const bool declared, const bool external) noexcept {
		// Имя построенного узла разметки
		const string name(::title(engine, valid));
		// Дописываем знак начала открывающей метки узла
		result.push_back('<');
		// Дописываем имя узла разметки
		result.append(name);
		// Количество атрибутов построенного узла разметки
		const uint32_t count = (engine() % 4);
		// Разряды уже записанных узлу разновидностей атрибутов
		uint32_t written = 0;
		/**
		 * Если узел является корневым в заведомо правильной разметке
		 *
		 * @note Префикс пространства имён связывается корневым узлом: без связывания
		 *       всякое имя с префиксом разбор отвергает, и правильной такая разметка
		 *       не была бы вовсе
		 */
		if(valid && (depth == 0)){
			// Дописываем объявление пространства имён для префикса
			result.append(" xmlns:ns=\"urn:named\"");
			// Запоминаем записанную узлу разновидность атрибута
			written |= (1u << 1);
		}
		/**
		 * Выполняем построение атрибутов узла разметки
		 */
		for(uint32_t i = 0; i < count; i++){
			// Разновидность записываемого атрибута узла разметки
			const uint32_t kind = (engine() % 8);
			/**
			 * Если разновидность атрибута узлу уже записана
			 *
			 * @note Повтор имени атрибута договором запрещён, и заведомо правильная
			 *       разметка повторов не несёт: разбор отверг бы её на второй же метке,
			 *       не дойдя ни до разрешения пространств имён, ни до сборки дерева
			 */
			if(valid && ((written & (1u << kind)) != 0))
				// Выполняем переход к следующему атрибуту узла разметки
				continue;
			// Запоминаем записанную узлу разновидность атрибута
			written |= (1u << kind);
			// Дописываем отступ перед именем атрибута
			result.push_back(' ');
			/**
			 * Выполняем выборку записи атрибута узла разметки
			 */
			switch(kind){
				// Дописываем объявление пространства имён по умолчанию
				case 0: result.append("xmlns=\"urn:default\""); break;
				// Дописываем объявление пространства имён для префикса
				case 1: result.append("xmlns:ns=\"urn:named\""); break;
				// Дописываем отмену объявления пространства имён
				case 2: result.append(valid ? "xmlns:other=\"urn:other\"" : "xmlns:ns=\"\""); break;
				// Дописываем атрибут обращения с пробельным содержимым
				case 3: result.append((engine() % 2) == 0 ? "xml:space=\"preserve\"" : "xml:space=\"default\""); break;
				// Дописываем атрибут со значением, требующим приведения
				case 4: result.append(valid ? "attr=\"a\tb\r\nc\rd &amp; e\"" : (declared ? "attr=\"a\tb\nc &amp; &plain;\"" : "attr=\"a\tb\nc &amp; d\"")); break;
				// Дописываем атрибут со значением в одиночных кавычках
				case 5: result.append("other='value'"); break;
				// Дописываем атрибут без кавычек вокруг значения
				case 6: if(!valid) result.append("attr=value"); break;
				/**
				 * Дописываем обычный атрибут
				 *
				 * @note Имена берутся из тех же шести, какими описание типа документа
				 *       объявляет виды атрибутов: разойдись они - объявленный вид не пришёлся
				 *       бы ни одному записанному атрибуту, и приведение значения не
				 *       выполнялось бы ни разу
				 * @note Значение изредка несёт вереницы пробельных знаков по краям и внутри:
				 *       без них приведение хотя и вызывается, но ничего не меняет, и ход его
				 *       остаётся непройденным
				 */
				case 7: {
					// Дописываем имя атрибута
					result.append(1, "pqrtuv"[engine() % 6]);
					// Дописываем начало значения атрибута
					result.append("=\"");
					/**
					 * Выполняем выборку записи значения атрибута
					 */
					switch(engine() % 4){
						// Дописываем значение с вереницами пробелов по краям и внутри
						case 0: result.append("  один   два  "); break;
						// Дописываем значение из одних пробельных знаков
						case 1: result.append("   "); break;
						// Дописываем значение с пробельными знаками, приводимыми к пробелу
						case 2: result.append(" a\tb\r\nc "); break;
						// Дописываем значение без пробельных знаков вовсе
						default: result.append("value");
					}
					// Дописываем завершение значения атрибута
					result.append("\"");
				} break;
			}
		}
		/**
		 * Если узел записывается самозакрывающейся меткой
		 */
		if((engine() % 4) == 0){
			// Дописываем завершение самозакрывающейся метки узла
			result.append("/>");
			// Выходим из функции
			return;
		}
		// Дописываем знак завершения открывающей метки узла
		result.push_back('>');
		// Количество вложенных записей узла разметки
		const uint32_t inner = (engine() % 4);
		/**
		 * Выполняем построение вложенных записей узла разметки
		 */
		for(uint32_t i = 0; i < inner; i++){
			/**
			 * Выполняем выборку разновидности вложенной записи
			 */
			switch(engine() % 8){
				// Дописываем текстовое содержимое узла
				case 0:
				case 1:
				case 2: result.append(::content(engine, valid, declared, external)); break;
				// Дописываем раздел дословного текста
				case 3: result.append(((engine() % 2) == 0) ? "<![CDATA[ a < b & c ]]>" : "<![CDATA[ a\r\n b\r c ]]>"); break;
				// Дописываем примечание
				case 4: result.append(valid ? "<!-- примечание -->" : "<!-- примечание -- хвост -->"); break;
				/**
				 * Дописываем указание обработчику
				 *
				 * @note Цель с разделителем префикса строится наравне с обычной: договор о
				 *       пространствах имён на цели указаний не распространяется, и разбор
				 *       с выключенным их разрешением такую цель принимает целиком. Записать
				 *       разобранное обратно при этом обязано удаваться
				 */
				case 5: result.append(((engine() % 4) == 0) ? "<?a:b данные?>" : "<?target данные?>"); break;
				// Дописываем вложенный узел разметки
				case 6:
				case 7: {
					// Если предел глубины вложенности не исчерпан
					if(depth < 6)
						// Выполняем построение вложенного узла разметки
						::element(result, engine, (depth + 1), valid, declared, external);
					// Иначе дописываем текстовое содержимое узла
					else result.append("deep");
				} break;
			}
		}
		/**
		 * Если требуется оставить узел незакрытым
		 */
		if(!valid && ((engine() % 24) == 0))
			// Выходим из функции, не закрывая узел разметки
			return;
		// Дописываем знак начала закрывающей метки узла
		result.append("</");
		/**
		 * Дописываем имя закрываемого узла разметки
		 *
		 * @note Изредка закрывается имя, отличное от открытого: разбору положено
		 *       отвечать на такое отказом, и путь этот проверять тоже требуется
		 */
		result.append((!valid && ((engine() % 24) == 0)) ? string("other") : name);
		// Дописываем знак завершения закрывающей метки узла
		result.push_back('>');
	}

	/**
	 * @brief Метод построения полуструктурированного текста разметки
	 *
	 * @param engine источник псевдослучайных чисел
	 * @param valid  признак построения заведомо правильной разметки
	 * @return       построенный текст разметки
	 *
	 */
	string generate(mt19937 & engine, const bool valid) noexcept {
		// Результат работы функции - построенный текст разметки
		string result;
		/**
		 * Признак объявленных описанием типа документа сущностей
		 *
		 * @note Ссылка на необъявленную сущность разбор отвергает, и в заведомо
		 *       правильной разметке её быть не вправе: ссылки ставятся лишь туда,
		 *       где описание типа объявило, на что они указывают
		 */
		bool declared = false;
		// Признак объявленной описанием типа ВНЕШНЕЙ сущности
		bool external = false;
		/**
		 * Если требуется дописать метку порядка байтов
		 */
		if((engine() % 12) == 0)
			// Дописываем метку порядка байтов в начало текста разметки
			result.append("\xEF\xBB\xBF");
		/**
		 * Если требуется дописать объявление разметки
		 */
		if((engine() % 3) != 0){
			/**
			 * Выполняем выборку записи объявления разметки
			 */
			switch(engine() % 4){
				// Дописываем объявление разметки с одним изданием
				case 0: result.append("<?xml version=\"1.0\"?>"); break;
				// Дописываем объявление разметки с кодировкой
				case 1: result.append("<?xml version=\"1.0\" encoding=\"UTF-8\"?>"); break;
				// Дописываем объявление разметки с признаком самодостаточности
				case 2: result.append("<?xml version=\"1.1\" encoding=\"utf-8\" standalone=\"yes\"?>"); break;
				// Дописываем объявление разметки с неизвестной кодировкой
				case 3: result.append(valid ? "<?xml version=\"1.0\"?>" : "<?xml version=\"1.0\" encoding=\"КОИ-8\"?>"); break;
			}
			// Дописываем знак завершения строки
			result.push_back('\n');
		}
		/**
		 * Если требуется дописать описание типа документа
		 */
		if((engine() % 2) == 0){
			// Дописываем начало описания типа документа
			result.append("<!DOCTYPE a");
			/**
			 * Если требуется дописать обозначение внешнего источника
			 */
			if((engine() % 4) == 0)
				// Дописываем обозначение внешнего источника
				result.append(" SYSTEM \"a.dtd\"");
			/**
			 * Если требуется дописать внутреннее подмножество
			 */
			if((engine() % 4) != 0){
				// Дописываем начало внутреннего подмножества
				result.append(" [\n");
				// Запоминаем, что сущности объявлены описанием типа документа
				declared = true;
				/**
				 * Выполняем построение объявлений внутреннего подмножества
				 */
				switch(engine() % 9){
					// Дописываем объявление простой сущности
					case 0: result.append(valid ? "<!ENTITY plain \"значение\">\n" : "<!ENTITY plain \"зна\r\nче\rние\">\n"); break;
					// Дописываем объявление сущности, содержащей разметку
					case 1: result.append("<!ENTITY plain \"v\">\n<!ENTITY markup \"<b>текст</b>\">\n"); break;
					// Дописываем объявление сущности, ссылающейся на себя
					case 2: result.append(valid ? "<!ENTITY plain \"\">\n" : "<!ENTITY loop \"&loop;\">\n<!ENTITY plain \"\">\n"); break;
					// Дописываем объявление сущности с многократной подстановкой
					case 3: result.append("<!ENTITY a0 \"xxxxxxxx\">\n<!ENTITY plain \"&a0;&a0;&a0;&a0;\">\n"); break;
					// Дописываем объявление умолчаний атрибутов
					case 4: {
						// Дописываем объявление строения узла и умолчаний его атрибутов
						result.append("<!ELEMENT a ANY>\n<!ATTLIST a q CDATA \"умолчание\" r ID #IMPLIED>\n");
						// Запоминаем, что сущностей описание типа документа не объявило
						declared = false;
					} break;
					// Дописываем объявление внешней сущности и указание обработчику
					case 5: {
						// Дописываем объявление внешней сущности с прочими записями подмножества
						result.append("<!ENTITY ext SYSTEM \"e.xml\">\n<?target данные?>\n<!-- примечание -->\n");
						// Запоминаем, что описание типа объявило внешнюю сущность
						external = true;
						// Запоминаем, что сущности «plain» описание типа документа не объявило
						declared = false;
					} break;
					/**
					 * Дописываем объявление атрибутов видами, приведению подлежащими
					 *
					 * @note Значение атрибута, объявленного видом, отличным от «CDATA»,
					 *       приводится: пробельные знаки по краям снимаются, а вереница их
					 *       сводится к одному пробелу. Ход этот идёт по хранилищу значений
					 *       ПРАВКОЮ НА МЕСТЕ, и ворошить его обязательно
					 */
					case 6: {
						// Дописываем объявление строения узла и видов его атрибутов
						result.append("<!ELEMENT a ANY>\n<!ATTLIST a t NMTOKENS #IMPLIED u NMTOKEN \"  об  ъ  \" v CDATA #IMPLIED>\n");
						// Запоминаем, что сущностей описание типа документа не объявило
						declared = false;
					} break;
					/**
					 * Дописываем повторное объявление одного и того же атрибута
					 *
					 * @note Связывающим договор велит считать ПЕРВОЕ объявление: вид из
					 *       повторного отменять первый не вправе
					 */
					case 7: {
						// Дописываем два объявления одного атрибута разными видами
						result.append("<!ELEMENT a ANY>\n<!ATTLIST a t CDATA #IMPLIED>\n<!ATTLIST a t NMTOKENS #IMPLIED>\n");
						// Запоминаем, что сущностей описание типа документа не объявило
						declared = false;
					} break;
					/**
					 * Дописываем ссылку на непрочитанную параметрическую сущность
					 *
					 * @note Объявления ЗА нею договором обрабатывать запрещено, а объявления
					 *       до неё действительны: ворошению подлежат обе стороны рубежа
					 */
					case 8: {
						// Дописываем объявления по обе стороны ссылки на параметрическую сущность
						result.append("<!ELEMENT a ANY>\n<!ATTLIST a t NMTOKEN \" до \">\n");
						// Дописываем ссылку на непрочитанную параметрическую сущность
						result.append("<!ENTITY % pe SYSTEM \"p.dtd\">\n%pe;\n");
						// Дописываем объявление, стоящее за ссылкой
						result.append("<!ATTLIST a u NMTOKEN \" за \">\n");
						// Запоминаем, что сущностей описание типа документа не объявило
						declared = false;
					} break;
				}
				// Дописываем завершение внутреннего подмножества
				result.push_back(']');
			}
			// Дописываем завершение описания типа документа
			result.append(">\n");
		}
		// Количество записей перед корневым узлом разметки
		const uint32_t before = (engine() % 3);
		/**
		 * Выполняем построение записей перед корневым узлом разметки
		 */
		for(uint32_t i = 0; i < before; i++){
			/**
			 * Выполняем выборку разновидности записи
			 */
			switch(engine() % 3){
				// Дописываем примечание
				case 0: result.append("<!-- голова -->\n"); break;
				// Дописываем указание обработчику
				case 1: result.append("<?target голова?>\n"); break;
				// Дописываем пробельное содержимое
				case 2: result.append((engine() % 4) == 0 ? string(80, ' ') : string("  \n")); break;
			}
		}
		// Выполняем построение корневого узла разметки
		::element(result, engine, 0, valid, declared, external);
		/**
		 * Если требуется дописать второй узел верхнего уровня
		 *
		 * @note Договор допускает лишь один корневой узел, и разбору положено
		 *       отвечать на второй отказом
		 */
		if(!valid && ((engine() % 8) == 0))
			// Выполняем построение второго узла верхнего уровня
			::element(result, engine, 0, valid, declared, external);
		// Дописываем знак завершения строки
		result.push_back('\n');
		// Выводим построенный текст разметки
		return result;
	}

	/**
	 * @brief Метод порчи построенного текста разметки
	 *
	 * @param text   текст разметки для порчи
	 * @param engine источник псевдослучайных чисел
	 *
	 */
	void corrupt(string & text, mt19937 & engine) noexcept {
		// Если текст разметки пустой, то порчу не выполняем
		if(text.empty())
			// Выходим из функции
			return;
		// Количество вносимых искажений
		const uint32_t count = (1 + (engine() % 4));
		/**
		 * Выполняем внесение искажений в текст разметки
		 */
		for(uint32_t i = 0; i < count; i++){
			// Разновидность вносимого искажения
			const uint32_t kind = (engine() % 4);
			/**
			 * Положение вносимого искажения
			 *
			 * @note Обращения к источнику разнесены по отдельным предложениям
			 *       намеренно: порядок вычисления доводов одного выражения языком
			 *       не определён, и два обращения в одном выражении давали бы разную
			 *       последовательность у разных построителей. Прогон обязан
			 *       воспроизводиться не только на своей машине
			 */
			const size_t place = (engine() % text.length());
			// Знак, вносимый искажением
			const char letter = static_cast <char> (engine() % 256);
			/**
			 * Выполняем выборку разновидности искажения
			 */
			switch(kind){
				// Выполняем замену произвольного знака
				case 0: text.at(place) = letter; break;
				// Выполняем обрезание текста разметки
				case 1: text.resize(place); break;
				// Выполняем вставку произвольного знака
				case 2: text.insert(place, 1, letter); break;
				// Выполняем удаление произвольного знака
				case 3: text.erase(place, 1); break;
			}
			// Если текст разметки опустел, то порчу прекращаем
			if(text.empty())
				// Выходим из цикла внесения искажений
				break;
		}
	}

	/**
	 * @brief Метод разбора текста разметки с запоминанием выданных событий
	 *
	 * @param text     разбираемый текст разметки
	 * @param options  настройки разбора текста разметки
	 * @param chunk    размер куска подачи, ноль - подача текста целиком
	 * @param events   собираемый перечень выданных событий
	 * @return         состояние чтения по завершении разбора
	 *
	 */
	xml::state_t consume(const string & text, const xml::reader_t::settings_t & options, const size_t chunk, vector <Event> & events, Event * failure = nullptr) noexcept {
		// Создаём объект чтения текста разметки
		xml::reader_t reader(::logger(), options);
		// Размер куска подачи текста разметки
		const size_t size = (chunk > 0 ? chunk : text.length());
		// Смещение начала очередного куска подачи
		size_t offset = 0;
		/**
		 * Выполняем подачу текста разметки кусками
		 */
		do {
			// Размер подаваемого куска текста разметки
			const size_t length = ((text.length() - offset) < size ? (text.length() - offset) : size);
			// Признак того, что подаётся последний кусок текста разметки
			const bool end = ((offset + length) >= text.length());
			// Если подача куска текста разметки не удалась
			if(!reader.feed(text.data() + offset, length, end))
				// Выходим из цикла подачи текста разметки
				break;
			// Выполняем смещение начала очередного куска подачи
			offset += length;
			/**
			 * Выполняем чтение выданных разбором событий
			 */
			while(reader.next()){
				// Создаём запоминаемое событие разбора
				Event event;
				// Запоминаем разновидность события
				event.event = static_cast <uint8_t> (reader.event());
				// Запоминаем префикс имени узла
				event.prefix.assign(reader.name().prefix);
				// Запоминаем местное имя узла
				event.local.assign(reader.name().local);
				// Запоминаем обозначение пространства имён узла
				event.uri.assign(reader.name().uri);
				// Запоминаем содержимое события
				event.text.assign(reader.text());
				// Запоминаем глубину вложенности узла события
				event.depth = reader.depth();
				// Запоминаем строку начала события в исходном тексте
				event.line = reader.location().line;
				// Запоминаем положение начала события в строке исходного текста
				event.column = reader.location().column;
				// Запоминаем смещение начала события от начала исходного текста
				event.position = reader.location().offset;
				// Запоминаем обращение с пробельным содержимым в узле события
				event.space = static_cast <uint8_t> (reader.space());
				// Запоминаем признак записи узла самозакрывающейся меткой
				event.empty = reader.empty();
				/**
				 * Выполняем перебор всех атрибутов узла события
				 */
				for(const xml::attribute_t & attribute : reader.attributes()){
					// Создаём запоминаемый атрибут узла
					Property property;
					// Запоминаем префикс имени атрибута
					property.prefix.assign(attribute.name.prefix);
					// Запоминаем местное имя атрибута
					property.local.assign(attribute.name.local);
					// Запоминаем обозначение пространства имён атрибута
					property.uri.assign(attribute.name.uri);
					// Запоминаем значение атрибута
					property.value.assign(attribute.value);
					// Запоминаем признак получения значения из объявления по умолчанию
					property.defaulted = attribute.defaulted;
					// Запоминаем строку атрибута в исходном тексте
					property.line = attribute.location.line;
					// Запоминаем положение атрибута в строке исходного текста
					property.column = attribute.location.column;
					// Выполняем сохранение атрибута в перечне
					event.attributes.push_back(::move(property));
				}
				/**
				 * Выполняем перебор всех объявлений пространств имён узла события
				 */
				for(const xml::binding_t & binding : reader.bindings()){
					// Создаём запоминаемое объявление пространства имён
					Property property;
					// Запоминаем префикс объявленного пространства имён
					property.prefix.assign(binding.prefix);
					// Запоминаем обозначение объявленного пространства имён
					property.uri.assign(binding.uri);
					// Выполняем сохранение объявления в перечне
					event.bindings.push_back(::move(property));
				}
				// Выполняем сохранение события в перечне
				events.push_back(::move(event));
				// Выполняем учёт выданного разбором события
				totals.events++;
			}
			// Если разбор прекращён ошибкой
			if(reader.state() == xml::state_t::FAILED)
				// Выходим из цикла подачи текста разметки
				break;
		// Выполняем подачу до исчерпания текста разметки
		} while(offset < text.length());
		/**
		 * Если место отказа требуется запомнить
		 */
		if(failure != nullptr){
			// Запоминаем код ошибки разбора
			failure->event = static_cast <uint8_t> (reader.error());
			// Запоминаем строку обнаружения ошибки разбора
			failure->line = reader.errorLocation().line;
			// Запоминаем положение ошибки в строке исходного текста
			failure->column = reader.errorLocation().column;
			// Запоминаем смещение ошибки от начала исходного текста
			failure->position = reader.errorLocation().offset;
		}
		// Выводим состояние чтения по завершении разбора
		return reader.state();
	}

	/**
	 * @brief Метод вывода разбираемого текста разметки в шестнадцатеричном виде
	 *
	 * @param text выводимый текст разметки
	 *
	 */
	void dump(const string & text) noexcept {
		// Выводим начало записи разбираемого текста разметки
		::fprintf(stderr, "xml fuzz: text=\"");
		/**
		 * Выполняем перебор всех знаков разбираемого текста разметки
		 */
		for(size_t i = 0; i < text.length(); i++)
			// Выводим очередной знак разбираемого текста разметки
			::fprintf(stderr, "\\x%02X", static_cast <uint8_t> (text.at(i)));
		// Выводим завершение записи разбираемого текста разметки
		::fprintf(stderr, "\"\n");
	}

	/**
	 * @brief Метод сличения перечней выданных разбором событий
	 *
	 * @param first  перечень событий подачи текста целиком
	 * @param second перечень событий подачи текста кусками
	 * @param chunk  размер куска подачи текста разметки
	 * @param text   разбираемый текст разметки
	 * @return       результат сличения перечней
	 *
	 */
	bool compare(const vector <Event> & first, const vector <Event> & second, const size_t chunk, const string & text, const xml::reader_t::settings_t & options) noexcept {
		/**
		 * Если количество выданных событий разошлось
		 */
		if(first.size() != second.size()){
			// Выводим сообщение о расхождении количества выданных событий
			::fprintf(stderr, "xml fuzz: chunk=%zu events %zu != %zu\n", chunk, first.size(), second.size());
			/**
			 * Выполняем перебор всех событий обоих перечней
			 *
			 * @note Печатаются оба перечня целиком: расхождение количества событий
			 *       разбирается сличением их по порядку, а не пересчётом
			 */
			for(size_t k = 0; k < ((first.size() > second.size()) ? first.size() : second.size()); k++){
				// Выводим очередную пару событий обоих перечней
				::fprintf(stderr, "xml fuzz:   %2zu целиком: %s кусками: %s\n", k,
					(k < first.size() ? (to_string(first.at(k).event) + "[" + first.at(k).text + "]").c_str() : "—"),
					(k < second.size() ? (to_string(second.at(k).event) + "[" + second.at(k).text + "]").c_str() : "—"));
			}
			// Выводим настройки разбора, при которых обнаружено расхождение
			::fprintf(stderr, "xml fuzz: ns=%d ent=%d com=%d pi=%d sp=%d def=%d depth=%u name=%u attrs=%u ents=%u exp=%llu event=%llu\n",
				(int) options.namespaces, (int) options.entities, (int) options.emitComments, (int) options.emitProcessing,
				(int) options.separateSpaces, (int) options.defaults, options.maxDepth, options.maxName,
				options.maxAttributes, options.maxEntities,
				(unsigned long long) options.maxExpansion, (unsigned long long) options.maxEvent);
			// Выводим разбираемый текст разметки
			dump(text);
			// Выводим результат сличения перечней
			return false;
		}
		/**
		 * Выполняем перебор всех выданных разбором событий
		 */
		for(size_t i = 0; i < first.size(); i++){
			// Если очередное событие разошлось
			if(first.at(i) != second.at(i)){
				// Получаем расходящееся событие подачи целиком
				const Event & a = first.at(i);
				// Получаем расходящееся событие подачи кусками
				const Event & b = second.at(i);
				// Выводим сообщение о расхождении выданного события
				::fprintf(stderr, "xml fuzz: chunk=%zu event %zu differs\n", chunk, i);
				/**
				 * Выводим оба расходящихся события по их признакам
				 *
				 * @note Печатаются оба целиком, а не одно лишь разошедшееся поле: разбор
				 *       расхождения начинается с того, чем события отличаются, а искать
				 *       это перечитыванием шестнадцатеричной записи текста дорого
				 */
				::fprintf(stderr, "xml fuzz:   целиком: вид=%u имя=[%s|%s|%s] место=%u:%u глубина=%u пробелы=%u пустой=%d атрибутов=%zu объявлений=%zu текст=[%s]\n",
					a.event, a.prefix.c_str(), a.local.c_str(), a.uri.c_str(), a.line, a.column, a.depth,
					a.space, (int) a.empty, a.attributes.size(), a.bindings.size(), a.text.c_str());
				// Выводим расходящееся событие подачи кусками
				::fprintf(stderr, "xml fuzz:   кусками: вид=%u имя=[%s|%s|%s] место=%u:%u глубина=%u пробелы=%u пустой=%d атрибутов=%zu объявлений=%zu текст=[%s]\n",
					b.event, b.prefix.c_str(), b.local.c_str(), b.uri.c_str(), b.line, b.column, b.depth,
					b.space, (int) b.empty, b.attributes.size(), b.bindings.size(), b.text.c_str());
				// Выводим настройки разбора, при которых обнаружено расхождение
				::fprintf(stderr, "xml fuzz: ns=%d ent=%d com=%d pi=%d sp=%d def=%d depth=%u name=%u attrs=%u ents=%u exp=%llu event=%llu\n",
					(int) options.namespaces, (int) options.entities, (int) options.emitComments, (int) options.emitProcessing,
					(int) options.separateSpaces, (int) options.defaults, options.maxDepth, options.maxName,
					options.maxAttributes, options.maxEntities,
					(unsigned long long) options.maxExpansion, (unsigned long long) options.maxEvent);
				// Выводим разбираемый текст разметки
				dump(text);
				// Выводим результат сличения перечней
				return false;
			}
		}
		// Выводим результат сличения перечней
		return true;
	}

	/**
	 * @brief Метод проверки связности узла дерева разметки
	 *
	 * @details Дерево обходится и вперёд, и назад, и вверх: узел, полученный движением
	 * в одну сторону, обязан вернуть исходный движением в обратную. Порванная связь
	 * означает обход, уходящий не туда, а то и вовсе не кончающийся
	 *
	 * @param node проверяемый узел дерева разметки
	 * @return     результат проверки связности узла
	 *
	 */
	bool linked(const xml::node_t & node) noexcept {
		// Количество вложенных узлов, обойдённых вперёд
		size_t forward = 0;
		// Последний обойдённый вложенный узел
		xml::node_t last;
		/**
		 * Выполняем перебор всех вложенных узлов вперёд
		 */
		for(xml::node_t child = node.first(); child.valid(); child = child.next()){
			/**
			 * Если родитель вложенного узла не совпадает с обходимым узлом
			 */
			if((child.parent().valid() != node.valid()) || (node.valid() && (child.parent() != node))){
				// Выводим сообщение о порванной связи с родителем
				::fprintf(stderr, "xml fuzz: parent link broken\n");
				// Выводим результат проверки связности узла
				return false;
			}
			/**
			 * Если сосед позади вложенного узла не совпадает с предыдущим
			 *
			 * @note Непригодные узлы сличаются признаком пригодности, а не оператором:
			 *       оператор сличает и дерево, которому узел принадлежит, отчего
			 *       непригодный узел дерева не равен непригодному узлу ниоткуда
			 */
			if((child.prev().valid() != last.valid()) || (last.valid() && (child.prev() != last))){
				// Выводим сообщение о порванной связи с соседом
				::fprintf(stderr, "xml fuzz: sibling link broken\n");
				// Выводим результат проверки связности узла
				return false;
			}
			// Запоминаем обойдённый вложенный узел
			last = child;
			// Выполняем учёт обойдённого вложенного узла
			forward++;
		}
		/**
		 * Если последний обойдённый узел не совпадает с последним вложенным
		 */
		if((last.valid() != node.last().valid()) || (last.valid() && (last != node.last()))){
			// Выводим сообщение о расхождении последнего вложенного узла
			::fprintf(stderr, "xml fuzz: last child differs\n");
			// Выводим результат проверки связности узла
			return false;
		}
		// Количество вложенных узлов, обойдённых назад
		size_t backward = 0;
		/**
		 * Выполняем перебор всех вложенных узлов назад
		 */
		for(xml::node_t child = node.last(); child.valid(); child = child.prev())
			// Выполняем учёт обойдённого вложенного узла
			backward++;
		/**
		 * Если обходы вперёд и назад разошлись количеством узлов
		 */
		if(forward != backward){
			// Выводим сообщение о расхождении обходов дерева
			::fprintf(stderr, "xml fuzz: %zu children forward, %zu backward\n", forward, backward);
			// Выводим результат проверки связности узла
			return false;
		}
		// Выводим результат проверки связности узла
		return true;
	}

	/**
	 * @brief Метод проверки связности узла дерева со всем его содержимым
	 *
	 * @param node проверяемый узел дерева разметки
	 * @return     результат проверки связности узла
	 *
	 */
	bool broken(const xml::node_t & node) noexcept {
		// Если связность самого узла нарушена, проверку прекращаем
		if(!linked(node))
			// Выводим результат проверки связности узла
			return false;
		/**
		 * Выполняем перебор всех вложенных узлов дерева разметки
		 */
		for(xml::node_t child = node.first(); child.valid(); child = child.next()){
			// Если связность вложенного узла нарушена, проверку прекращаем
			if(!broken(child))
				// Выводим результат проверки связности узла
				return false;
		}
		// Выводим результат проверки связности узла
		return true;
	}

	/**
	 * @brief Метод обхода узла дерева разметки со всем его содержимым
	 *
	 * @param node обходимый узел дерева разметки
	 *
	 */
	void traverse(const xml::node_t & node) noexcept {
		// Выполняем обращение к имени узла дерева разметки
		const xml::name_t name = node.name();
		// Выполняем обращение к содержимому узла дерева разметки
		const string text = node.text();
		// Выполняем обращение к месту узла в исходном тексте разметки
		node.location();
		/**
		 * Выполняем перебор всех атрибутов узла дерева разметки
		 */
		for(const xml::attribute_t & attribute : node.attributes()){
			// Выполняем обращение к значению атрибута по его имени
			node.attribute(attribute.name.local, attribute.name.uri);
			// Выполняем обращение к признаку наличия атрибута
			node.has(attribute.name.local, attribute.name.uri);
			// Число, полученное разбором значения атрибута
			int64_t number = 0;
			// Выполняем разбор значения атрибута числом
			node.value(number, attribute.name.local, attribute.name.uri);
		}
		// Выполняем обращение к перечню объявлений пространств имён узла
		node.bindings();
		// Выполняем обращение к вложенному узлу по его имени
		node.child(name.local, name.uri);
		// Выполняем поиск вложенного узла по всему поддереву
		node.find(name.local, name.uri);
		/**
		 * Выполняем перебор всех вложенных узлов дерева разметки
		 */
		for(xml::node_t child = node.first(); child.valid(); child = child.next())
			// Выполняем обход вложенного узла дерева разметки
			traverse(child);
	}

	/**
	 * @brief Метод переноса накопленного содержимого в слепок дерева
	 *
	 * @param result собираемый слепок дерева разметки
	 * @param text   накопленное содержимое, ещё не перенесённое в слепок
	 * @param strip  признак пропуска содержимого из одних пробельных знаков
	 * @param exact  признак содержимого, взятого дословно и пропуску не подлежащего
	 *
	 */
	void flush(string & result, string & text, const bool strip, bool & exact) noexcept {
		/**
		 * Если накопленного содержимого нет, переносить нечего
		 */
		if(text.empty()){
			// Выполняем сброс признака дословно взятого содержимого
			exact = false;
			// Выходим из переноса накопленного содержимого
			return;
		}
		// Признак того, что накопленное содержимое состоит из одних пробельных знаков
		bool blank = true;
		/**
		 * Выполняем перебор всех знаков накопленного содержимого
		 */
		for(const char letter : text){
			/**
			 * Если обнаружен знак, не являющийся пробельным
			 */
			if(!xml::isSpace(static_cast <uint32_t> (static_cast <uint8_t> (letter)))){
				// Запоминаем, что содержимое пробельным не является
				blank = false;
				// Выходим из перебора знаков содержимого
				break;
			}
		}
		/**
		 * Если содержимое переносить в слепок требуется
		 *
		 * @note Нарядная запись ставит отступы сама, и содержимое из одних пробельных
		 *       знаков она вправе как убрать, так и добавить: слепок его не считает
		 */
		if(!strip || !blank || exact)
			// Выполняем перенос накопленного содержимого в слепок
			result.append("|").append(text);
		// Выполняем очистку накопленного содержимого
		text.clear();
		// Выполняем сброс признака дословно взятого содержимого
		exact = false;
	}

	/**
	 * @brief Метод сборки слепка узла дерева разметки
	 *
	 * @details Слепок описывает дерево по смыслу, а не по написанию: имена берутся
	 * обозначением пространства имён и местным именем без префикса - запись назначает
	 * префиксы заново, - атрибуты складываются упорядоченно, а описание типа документа
	 * пропускается: записывать его модуль не умеет вовсе
	 *
	 * @note Подряд идущее содержимое сливается в одно, а вид его - текстовое,
	 * пробельное либо дословное - не различается. Различия эти обратного перехода не
	 * переживают, и не по вине перехода: примечание и указание обработчику содержимое
	 * разрывают, а в дерево при отключённой их выдаче не попадают, - записанному
	 * разрывать содержимое уже нечем. Так же и пробельное содержимое рядом с
	 * непробельным: записанные подряд, они читаются обратно одним узлом
	 *
	 * @param node   узел дерева разметки, слепок которого собирается
	 * @param result собираемый слепок узла дерева разметки
	 * @param text   накопленное содержимое, ещё не перенесённое в слепок
	 * @param exact  признак содержимого, взятого дословно и пропуску не подлежащего
	 * @param base   признак пропуска пробельного содержимого, заданный изначально
	 *
	 */
	void snapshot(const xml::node_t & node, string & result, string & text, const bool strip, bool & exact, const bool base) noexcept {
		/**
		 * Определяем вид узла дерева разметки
		 */
		switch(static_cast <uint8_t> (node.kind())){
			// Если узел является описанием типа документа, слепок его пропускаем
			case static_cast <uint8_t> (xml::kind_t::DOCTYPE): return;
			/**
			 * Если узел является разделом дословного текста
			 *
			 * @note Раздел дословного текста значим сам по себе: пропускать его наравне
			 *       с отступом нельзя даже тогда, когда он собран одними пробелами
			 */
			case static_cast <uint8_t> (xml::kind_t::CDATA):
				// Запоминаем, что накопленное содержимое взято дословно
				exact = true;
			// Если узел является текстовым содержимым либо пробельным
			case static_cast <uint8_t> (xml::kind_t::TEXT):
			case static_cast <uint8_t> (xml::kind_t::SPACE):
				// Выполняем накопление содержимого узла
				text.append(node.text());
			// Выходим из сборки слепка узла дерева разметки
			return;
		}
		// Выполняем перенос накопленного содержимого в слепок
		flush(result, text, strip, exact);
		/**
		 * Определяем вид узла дерева разметки
		 */
		switch(static_cast <uint8_t> (node.kind())){
			// Если узел является узлом разметки
			case static_cast <uint8_t> (xml::kind_t::ELEMENT): {
				// Дописываем к слепку имя узла разметки
				result.append("<{").append(node.name().uri).append("}").append(node.name().local);
				// Собираемый перечень атрибутов узла разметки
				vector <string> items;
				/**
				 * Выполняем перебор всех атрибутов узла разметки
				 */
				for(const xml::attribute_t & attribute : node.attributes()){
					// Собираемая запись очередного атрибута узла
					string item;
					// Дописываем к записи имя атрибута с обозначением пространства имён
					item.append("{").append(attribute.name.uri).append("}").append(attribute.name.local);
					// Дописываем к записи значение атрибута
					item.append("=").append(attribute.value);
					// Выполняем сохранение записи атрибута в перечне
					items.push_back(::move(item));
				}
				/**
				 * Выполняем упорядочение перечня атрибутов узла
				 *
				 * @note Порядок атрибутов договором не задан, и запись вправе его менять:
				 *       объявления пространств имён она ставит первыми
				 */
				::sort(items.begin(), items.end());
				/**
				 * Выполняем перебор всех записей атрибутов узла
				 */
				for(const string & item : items)
					// Дописываем к слепку очередной атрибут узла
					result.append(" ").append(item);
				// Дописываем к слепку конец записи узла разметки
				result.append(">");
			} break;
			// Если узел является примечанием
			case static_cast <uint8_t> (xml::kind_t::COMMENT):
				// Дописываем к слепку содержимое примечания
				result.append("!").append(node.text());
			break;
			// Если узел является указанием обработчику
			case static_cast <uint8_t> (xml::kind_t::PROCESSING):
				// Дописываем к слепку цель указания обработчику и его данные
				result.append("?").append(node.name().local).append(" ").append(node.text());
			break;
		}
		// Признак пропуска пробельного содержимого внутри узла
		bool inner = strip;
		/**
		 * Если узел является узлом разметки
		 */
		if(node.kind() == xml::kind_t::ELEMENT){
			/**
			 * Получаем заданное узлом обращение с пробельным содержимым
			 *
			 * @note Атрибут отыскивается обоими написаниями: с выключенным разрешением
			 *       префиксов имя его в дереве не разделено и пространства имён не имеет
			 */
			const string_view spacing = (!node.attribute("space", xml::XML_NAMESPACE).empty() ?
				node.attribute("space", xml::XML_NAMESPACE) : node.attribute("xml:space"));
			/**
			 * Если узел требует сохранения пробельного содержимого
			 *
			 * @note Послабление на пробельное содержимое даётся отступам нарядной записи,
			 *       а внутри такого узла отступов быть не должно вовсе: пробельные знаки
			 *       там значимы, и запись обязана сохранить их знак в знак
			 */
			if(!spacing.empty())
				/**
				 * Запоминаем обращение с пробельным содержимым, заданное узлом
				 *
				 * @note Значение, договором не отведённое, толкуется отменой наравне с
				 *       `default` - тем же самым образом, каким его толкуют чтение и запись
				 */
				inner = ((spacing.compare("preserve") == 0) ? false : base);
		}
		/**
		 * Выполняем перебор всех вложенных узлов дерева разметки
		 */
		for(xml::node_t child = node.first(); child.valid(); child = child.next())
			// Выполняем сборку слепка вложенного узла дерева разметки
			snapshot(child, result, text, inner, exact, base);
		/**
		 * Если узел является узлом разметки
		 */
		if(node.kind() == xml::kind_t::ELEMENT){
			// Выполняем перенос накопленного содержимого в слепок
			flush(result, text, inner, exact);
			// Дописываем к слепку завершение записи узла разметки
			result.append("</>");
		}
	}

	/**
	 * @brief Метод сборки слепка дерева разметки целиком
	 *
	 * @param document дерево разметки, слепок которого собирается
	 * @return         собранный слепок дерева разметки
	 *
	 */
	string snapshot(const xml::document_t & document, const bool strip, const bool base) noexcept {
		// Собираемый слепок дерева разметки
		string result;
		// Накопленное содержимое, ещё не перенесённое в слепок
		string text;
		// Признак содержимого, взятого дословно и пропуску не подлежащего
		bool exact = false;
		// Выполняем сборку слепка дерева разметки
		snapshot(document.root(), result, text, strip, exact, base);
		// Выполняем перенос накопленного содержимого в слепок
		flush(result, text, strip, exact);
		// Выводим собранный слепок дерева разметки
		return result;
	}

	/**
	 * @brief Метод перезаписи дерева разметки
	 *
	 * @param document перезаписываемое дерево разметки
	 * @param options  настройки записи текста разметки
	 * @param result   собираемый текст перезаписи дерева разметки
	 * @return         результат выполнения операции
	 *
	 */
	bool rewrite(const xml::document_t & document, const xml::writer_t::settings_t & options, string & result) noexcept {
		// Создаём объект записи текста разметки
		xml::writer_t writer(::logger(), options);
		// Выполняем учёт перезаписи дерева разметки
		totals.rewrites++;
		/**
		 * Если запись дерева разметки не удалась
		 *
		 * @note Отказ этот прежде считался поводом прекратить проверку молча, а
		 *       молчание неотличимо от согласия: дерево, которое разбор собрал, а
		 *       запись принять отказалась, и есть дефект. Отказ потому докладывается
		 */
		if(!writer.element(document.root()) || !writer.complete()){
			// Выводим сообщение об отказе записи собранного дерева разметки
			::fprintf(stderr, "xml fuzz: rewrite refused, error=%u (%s)\n", static_cast <uint32_t> (writer.error()), xml::message(writer.error()));
			// Выводим отрицательный результат выполнения операции
			return false;
		}
		// Выполняем сохранение собранного текста перезаписи
		result.assign(writer.text());
		// Выводим положительный результат выполнения операции
		return true;
	}

	/**
	 * @brief Метод поиска соседствующего текстового содержимого
	 *
	 * @details Два соседних узла текстового либо пробельного содержимого запись сливает
	 *          воедино: разделявший их узел, чтением опущенный, - примечание либо
	 *          указание обработчику, - в тексте не появляется вовсе, и разбор записи
	 *          даёт один узел вместо двух. Различие это - свойство самого написания
	 *          разметки, а не изъян кодека
	 *
	 * @param value осматриваемое владеющее значение
	 * @return      признак наличия соседствующего текстового содержимого
	 *
	 */
	bool adjacent(const xml::value_t & value) noexcept {
		// Признак того, что предыдущий узел был текстовым либо пробельным
		bool previous = false;
		/**
		 * Выполняем перебор всего вложенного содержимого значения
		 */
		for(size_t i = 0; i < value.size(); i++){
			// Получаем признак того, что узел является текстовым либо пробельным
			const bool current = ((value[i].kind() == xml::kind_t::TEXT) || (value[i].kind() == xml::kind_t::SPACE));
			// Если текстовое содержимое соседствует с текстовым, выводим признак находки
			if(current && previous) return true;
			// Запоминаем признак текстового содержимого для следующего узла
			previous = current;
			/**
			 * Если вложенное содержимое несёт соседствующее текстовое содержимое
			 */
			if(::adjacent(value[i]))
				// Выводим признак наличия соседствующего текстового содержимого
				return true;
		}
		// Выводим признак отсутствия соседствующего текстового содержимого
		return false;
	}
	/**
	 * @brief Метод пересборки владеющего значения потоковым сборщиком
	 *
	 * @details Значение обходится узел за узлом, и всякий узел подаётся сборщику тем же
	 *          самым порядком, каким он был бы записан в текст: собранное сборщиком
	 *          обязано совпасть с обходимым
	 *
	 * @param value   пересобираемое владеющее значение
	 * @param builder потоковый сборщик владеющего значения
	 * @return        признак успешности пересборки
	 *
	 */
	bool rebuild(const xml::value_t & value, xml::builder_t & builder) noexcept {
		/**
		 * Определяем вид пересобираемого значения
		 */
		switch(static_cast <uint8_t> (value.kind())){
			// Если значение является узлом разметки
			case static_cast <uint8_t> (xml::kind_t::ELEMENT): {
				/**
				 * Если открытие узла разметки не удалось
				 */
				if(!builder.open(value.local(), value.uri(), value.prefix()))
					// Выводим признак неудачной пересборки
					return false;
				/**
				 * Выполняем перебор всех привязок пространств имён узла разметки
				 */
				for(auto & item : value.bindings()){
					/**
					 * Если заведение привязки пространства имён не удалось
					 */
					if(!builder.binding(item.prefix, item.uri))
						// Выводим признак неудачной пересборки
						return false;
				}
				/**
				 * Выполняем перебор всех атрибутов узла разметки
				 */
				for(auto & item : value.attributes()){
					/**
					 * Если заведение атрибута узла разметки не удалось
					 */
					if(!builder.attribute(item.local, item.value, item.uri, item.prefix))
						// Выводим признак неудачной пересборки
						return false;
				}
				/**
				 * Выполняем перебор всего вложенного содержимого узла разметки
				 */
				for(size_t i = 0; i < value.size(); i++){
					/**
					 * Если пересборка вложенного содержимого не удалась
					 */
					if(!::rebuild(value[i], builder))
						// Выводим признак неудачной пересборки
						return false;
				}
				// Выводим итог закрытия узла разметки
				return builder.close();
			}
			// Если значение является текстовым содержимым
			case static_cast <uint8_t> (xml::kind_t::TEXT):
				// Выводим итог подачи текстового содержимого
				return builder.text(value.text());
			// Если значение является разделом дословного текста
			case static_cast <uint8_t> (xml::kind_t::CDATA):
				// Выводим итог подачи раздела дословного текста
				return builder.cdata(value.text());
			// Если значение является примечанием
			case static_cast <uint8_t> (xml::kind_t::COMMENT):
				// Выводим итог подачи примечания
				return builder.comment(value.text());
			// Если значение является указанием обработчику
			case static_cast <uint8_t> (xml::kind_t::PROCESSING):
				// Выводим итог подачи указания обработчику
				return builder.processing(value.local(), value.text());
		}
		/**
		 * Выводим итог подачи значения целиком
		 *
		 * @note Вид, потоковой подаче не подлежащий, - описание типа документа либо
		 *       пробельное содержимое, - подаётся значением целиком: сборщик своего
		 *       вызова для них не держит вовсе
		 */
		return builder.value(value);
	}
	/**
	 * @brief Метод проверки дерева разметки на построенном тексте
	 *
	 * @param text    разбираемый текст разметки
	 * @param options настройки разбора текста разметки
	 * @param engine  источник псевдослучайных чисел
	 * @return        результат проверки дерева разметки
	 *
	 */
	bool tree(const string & text, const xml::reader_t::settings_t & options, mt19937 & engine) noexcept {
		// Создаём дерево разметки
		xml::document_t document(::logger());
		// Выполняем учёт собранного дерева разметки
		totals.trees++;
		// Если разбор текста разметки не удался, то проверку прекращаем
		if(!document.parse(text, options))
			// Выводим результат проверки дерева разметки
			return true;
		// Выполняем обход всего собранного дерева разметки
		traverse(document.root());
		/**
		 * Если связность собранного дерева разметки нарушена
		 */
		if(!broken(document.root())){
			// Выводим разбираемый текст разметки
			dump(text);
			// Выводим результат проверки дерева разметки
			return false;
		}
		// Собираемые настройки записи текста разметки
		xml::writer_t::settings_t settings;
		// Устанавливаем вид записи собираемого текста разметки
		settings.format = (((engine() % 2) == 0) ? xml::format_t::PRETTY : xml::format_t::COMPACT);
		/**
		 * Выполняем выборку знака отступа нарядной записи
		 */
		switch(engine() % 3){
			// Устанавливаем отступ знаками горизонтальной табуляции
			case 0: settings.separator = xml::separator_t::TABS; break;
			// Устанавливаем отступ пробелами
			case 1: settings.separator = xml::separator_t::SPACES; break;
			// Отменяем отступ вовсе
			case 2: settings.separator = xml::separator_t::NONE; break;
		}
		// Устанавливаем количество знаков отступа на один уровень вложенности
		settings.indent = (1 + (engine() % 3));
		// Устанавливаем флаг записи узлов без содержимого самозакрывающейся меткой
		settings.collapse = ((engine() % 2) == 0);
		// Устанавливаем флаг экранирования знаков, выходящих за пределы US-ASCII
		settings.escapeNonAscii = ((engine() % 2) == 0);
		/**
		 * Настройки повторного разбора перезаписанного текста разметки
		 *
		 * @note Пределы разбора снимаются намеренно: запись вправе быть длиннее
		 *       исходного текста, оставаясь равнозначной ему по смыслу. Знаки за
		 *       пределами US-ASCII записываются числовыми ссылками по десятку байтов
		 *       на знак, а префиксы пространств имён назначаются заново. Держать на
		 *       перезаписи пределы исходного текста значило бы отвергать её за то,
		 *       чего сама запись и добавила
		 */
		xml::reader_t::settings_t relaxed = options;
		// Настройки разбора с пределами по умолчанию
		const xml::reader_t::settings_t limits;
		// Снимаем предел глубины вложенности узлов
		relaxed.maxDepth = limits.maxDepth;
		// Снимаем предел длины имени
		relaxed.maxName = limits.maxName;
		// Снимаем предел количества атрибутов узла
		relaxed.maxAttributes = limits.maxAttributes;
		// Снимаем предел количества объявленных сущностей
		relaxed.maxEntities = limits.maxEntities;
		// Снимаем предел объёма подстановки сущностей
		relaxed.maxExpansion = limits.maxExpansion;
		// Снимаем предел объёма одного события
		relaxed.maxEvent = limits.maxEvent;
		// Текст первой перезаписи дерева разметки
		string first;
		/**
		 * Если перезапись дерева разметки не удалась
		 *
		 * @note Разобранное обязано записываться обратно: отказ записи по дереву,
		 *       собранному разбором, докладывается расхождением, а не пропускается
		 */
		if(!rewrite(document, settings, first)){
			// Выводим разбираемый текст разметки
			dump(text);
			// Выводим результат проверки дерева разметки
			return false;
		}
		// Если перезапись дерева разметки оказалась пустой, то проверку прекращаем
		if(first.empty())
			// Выводим результат проверки дерева разметки
			return true;
		// Создаём дерево разметки для повторного разбора
		xml::document_t repeat(::logger());
		/**
		 * Если повторный разбор перезаписанного текста не удался
		 */
		if(!repeat.parse(first, relaxed)){
			// Выводим сообщение об отказе повторного разбора перезаписанного текста
			::fprintf(stderr, "xml fuzz: reparse failed, error=%u\n", static_cast <uint32_t> (repeat.error()));
			// Выводим исходный текст разметки
			dump(text);
			// Выводим перезапись дерева разметки
			dump(first);
			// Выводим результат проверки дерева разметки
			return false;
		}
		// Признак пропуска пробельного содержимого при сличении слепков
		const bool strip = (settings.format == xml::format_t::PRETTY);
		// Слепок исходного дерева разметки
		const string before = ::snapshot(document, strip, strip);
		// Слепок дерева, собранного разбором перезаписи
		const string after = ::snapshot(repeat, strip, strip);
		/**
		 * Если слепки деревьев разошлись
		 *
		 * @note Сличаются слепки лишь плотной записи. Нарядная ставит отступы и внутри
		 *       узлов со смешанным содержимым, а там пробельные знаки содержимым и
		 *       являются: дерево принимает их наравне с прочим, и слепок разрастается
		 *       пробельными узлами. Устройство это намеренное - отступы ставятся ради
		 *       удобства чтения, - и равнозначности написания от нарядной записи не ждут
		 */
		if(before != after){
			// Выводим сообщение о расхождении слепков деревьев
			::fprintf(stderr, "xml fuzz: tree differs after rewrite\n  до    [%s]\n  после [%s]\n", before.c_str(), after.c_str());
			// Выводим исходный текст разметки
			dump(text);
			// Выводим перезапись дерева разметки
			dump(first);
			// Выводим результат проверки дерева разметки
			return false;
		}
		// Текст повторной перезаписи дерева разметки
		string second;
		/**
		 * Если повторная перезапись дерева разметки не удалась
		 */
		if(!rewrite(repeat, settings, second)){
			// Выводим сообщение об отказе повторной перезаписи дерева разметки
			::fprintf(stderr, "xml fuzz: second rewrite failed\n");
			// Выводим перезапись дерева разметки
			dump(first);
			// Выводим результат проверки дерева разметки
			return false;
		}
		/**
		 * Если повторная перезапись разошлась с первой
		 */
		if(second != first){
			// Выводим сообщение о расхождении повторной перезаписи
			::fprintf(stderr, "xml fuzz: rewrite unstable, collapse=%d escape=%d ns=%d ent=%d com=%d pi=%d sp=%d def=%d depth=%u name=%u attrs=%u ents=%u exp=%llu event=%llu\n",
				(int) settings.collapse, (int) settings.escapeNonAscii,
				(int) options.namespaces, (int) options.entities, (int) options.emitComments, (int) options.emitProcessing,
				(int) options.separateSpaces, (int) options.defaults, options.maxDepth, options.maxName,
				options.maxAttributes, options.maxEntities,
				(unsigned long long) options.maxExpansion, (unsigned long long) options.maxEvent);
			// Выводим первую перезапись дерева разметки
			dump(first);
			// Выводим повторную перезапись дерева разметки
			dump(second);
			// Выводим результат проверки дерева разметки
			return false;
		}
		// Перечень узлов разметки, годных к записи поддеревом
		vector <xml::node_t> nodes;
		/**
		 * @brief Метод сбора узлов разметки собранного дерева
		 *
		 * @param node узел дерева разметки, с которого ведётся сбор
		 *
		 */
		const function <void (const xml::node_t &)> collect = [&nodes, &collect](const xml::node_t & node) noexcept -> void {
			// Если узел является узлом разметки, берём его в перечень
			if(node.kind() == xml::kind_t::ELEMENT) nodes.push_back(node);
			/**
			 * Выполняем перебор всех вложенных узлов дерева разметки
			 */
			for(xml::node_t child = node.first(); child.valid(); child = child.next())
				// Выполняем сбор узлов разметки вложенного узла
				collect(child);
		};
		// Выполняем сбор узлов разметки собранного дерева
		collect(document.root());
		/**
		 * Если собранное дерево несёт узлы разметки
		 *
		 * @details Записывать можно всякий узел дерева, а не только корень, и записанное
		 * поддерево обязано остаться равнозначным исходному: пространства имён предков
		 * запись объявляет заново, а обращение с пробельным содержимым, заданное выше по
		 * дереву, - наследует. Проверялась прежде одна лишь запись корня, и потеря
		 * значимых пробелов у поддерева под `xml:space="preserve"` тем и пряталась
		 */
		if(!nodes.empty()){
			// Выбираем узел разметки, записываемый поддеревом
			const xml::node_t node = nodes.at(engine() % nodes.size());
			// Создаём объект записи текста разметки поддерева
			xml::writer_t writer(::logger(), settings);
			// Выполняем учёт перезаписи поддерева разметки
			totals.rewrites++;
			/**
			 * Если запись поддерева разметки не удалась
			 *
			 * @note Всякий узел собранного дерева обязан записываться: отказ прежде
			 *       уводил проверку мимо всего сличения молча, а молчание неотличимо
			 *       от согласия
			 */
			if(!writer.element(node) || !writer.complete()){
				// Выводим сообщение об отказе записи поддерева разметки
				::fprintf(stderr, "xml fuzz: subtree write refused, error=%u (%s)\n", static_cast <uint32_t> (writer.error()), xml::message(writer.error()));
				// Выводим исходный текст разметки
				dump(text);
				// Выводим результат проверки дерева разметки
				return false;
			}
			/**
			 * Выполняем сличение записанного поддерева с исходным узлом
			 */
			{
				// Создаём дерево разметки для разбора записанного поддерева
				xml::document_t subtree(::logger());
				/**
				 * Если разбор записанного поддерева не удался
				 */
				if(!subtree.parse(writer.text(), relaxed)){
					// Выводим сообщение об отказе разбора записанного поддерева
					::fprintf(stderr, "xml fuzz: subtree reparse failed, error=%u\n", static_cast <uint32_t> (subtree.error()));
					// Выводим исходный текст разметки
					dump(text);
					// Выводим записанное поддерево разметки
					dump(writer.text());
					// Выводим результат проверки дерева разметки
					return false;
				}
				// Признак сохранения пробельного содержимого, унаследованный поддеревом
				bool preserve = false;
				/**
				 * Выполняем перебор всех объемлющих узлов записанного поддерева
				 */
				for(xml::node_t parent = node.parent(); parent.valid(); parent = parent.parent()){
					// Если объемлющий узел узлом разметки не является, обращения он не задаёт
					if(parent.kind() != xml::kind_t::ELEMENT) continue;
					// Получаем заданное объемлющим узлом обращение с пробельным содержимым
					const string_view spacing = (!parent.attribute("space", xml::XML_NAMESPACE).empty() ?
						parent.attribute("space", xml::XML_NAMESPACE) : parent.attribute("xml:space"));
					// Если обращения объемлющий узел не задаёт, поднимаемся выше по дереву
					if(spacing.empty()) continue;
					// Запоминаем обращение с пробельным содержимым, заданное объемлющим узлом
					preserve = (spacing.compare("preserve") == 0);
					// Выходим из перебора объемлющих узлов
					break;
				}
				/**
				 * Признак пропуска пробельного содержимого при сличении слепков поддерева
				 *
				 * @note Под сохранением пробелов послабление не даётся: отступов запись там
				 *       не ставит вовсе, и пробельные знаки обязаны совпасть знак в знак
				 */
				const bool loose = ((settings.format == xml::format_t::PRETTY) && !preserve);
				/**
				 * Признак пропуска пробельного содержимого вне сохранения пробелов
				 *
				 * @note Узел, сохранение отменяющий, возвращает послабление обратно: отступы
				 *       внутри него запись ставит по-прежнему, и сличать их знак в знак нельзя
				 */
				const bool base = (settings.format == xml::format_t::PRETTY);
				// Слепок исходного поддерева разметки
				string source, content;
				// Признак содержимого, взятого дословно и пропуску не подлежащего
				bool exact = false;
				// Выполняем сборку слепка исходного поддерева разметки
				snapshot(node, source, content, loose, exact, base);
				// Выполняем перенос накопленного содержимого в слепок
				flush(source, content, loose, exact);
				// Слепок дерева, собранного разбором записанного поддерева
				const string written = ::snapshot(subtree, loose, base);
				/**
				 * Если слепки поддеревьев разошлись
				 */
				if(source != written){
					// Выводим сообщение о расхождении слепков поддеревьев
					::fprintf(stderr, "xml fuzz: subtree differs after rewrite\n  до    [%s]\n  после [%s]\n", source.c_str(), written.c_str());
					// Выводим исходный текст разметки
					dump(text);
					// Выводим записанное поддерево разметки
					dump(writer.text());
					// Выводим результат проверки дерева разметки
					return false;
				}
			}
			/**
			 * Выполняем сличение владеющего значения с исходным узлом
			 *
			 * @details Значение снимается с того же узла и гоняется двумя ходами:
			 *          пересобирается потоковым сборщиком узел за узлом и записывается
			 *          в текст с последующим разбором. Всякий раз итог сличается со
			 *          снятым значением сравнением самих значений, а не записей их
			 */
			{
				// Выполняем снятие владеющего значения с узла дерева разметки
				const xml::value_t value(node);
				// Выполняем учёт снятого владеющего значения
				totals.values++;
				// Объект потокового сборщика владеющего значения
				xml::builder_t builder(::logger());
				/**
				 * Если пересборка значения потоковым сборщиком не удалась
				 */
				if(!::rebuild(value, builder)){
					// Выводим сообщение об отказе пересборки значения
					::fprintf(stderr, "xml fuzz: value rebuild refused\n");
					// Выводим исходный текст разметки
					dump(text);
					// Выводим результат проверки дерева разметки
					return false;
				}
				// Выполняем учёт пересобранного владеющего значения
				totals.builds++;
				/**
				 * Если пересобранное значение разошлось со снятым
				 */
				if(!(builder.finish() == value)){
					// Выводим сообщение о расхождении пересобранного значения со снятым
					::fprintf(stderr, "xml fuzz: value rebuild differs\n  запись [%s]\n", value.dump().c_str());
					// Выводим исходный текст разметки
					dump(text);
					// Выводим результат проверки дерева разметки
					return false;
				}
				/**
				 * Выполняем сличение прививки владеющего значения с исходным узлом
				 *
				 * @note Прививаемое место обязано существовать: прививка заменяет
				 *       поддерево, а не заводит новое, - оттого узел в принимающем
				 *       дереве и заведён
				 */
				{
					// Дерево разметки, принимающее прививку
					xml::document_t host(::logger());
					/**
					 * Если заведение принимающего прививку дерева удалось
					 */
					if(host.parse("<host><graft/></host>", relaxed)){
						/**
						 * Если прививка значения в дерево разметки не удалась
						 */
						if(!host.graft("/host/graft", value)){
							// Выводим сообщение об отказе прививки значения
							::fprintf(stderr, "xml fuzz: value graft refused\n  запись [%s]\n", value.dump().c_str());
							// Выводим исходный текст разметки
							dump(text);
							// Выводим результат проверки дерева разметки
							return false;
						}
						// Выполняем учёт привитого владеющего значения
						totals.grafts++;
						/**
						 * Если прививка корнем дерева с несколькими узлами отказом не завершилась
						 *
						 * @note Корень узлом разметки не является вовсе, и ребёнком ему не
						 *       бывать: одним узлом корню с несколькими узлами не стать, а
						 *       прививка заменяет узел узлом. Записи изъян этот не торчит,
						 *       ибо запись корень пропускает, - оттого проверяется он отказом,
						 *       а не сличением
						 */
						const xml::value_t root(document.root());
						/**
						 * Если корень несёт несколько узлов, а прививка им отказом не завершилась
						 */
						if((root.size() != 1) && host.graft("/host/0", root)){
							// Выводим сообщение о принятой прививке значением корня дерева
							::fprintf(stderr, "xml fuzz: document graft accepted\n  запись [%s]\n", value.dump().c_str());
							// Выводим исходный текст разметки
							dump(text);
							// Выводим результат проверки дерева разметки
							return false;
						}
						/**
						 * Если снятое с привитого места значение разошлось с привитым
						 */
						if(!(xml::value_t(host.element().first()) == value)){
							// Выводим сообщение о расхождении привитого значения с исходным
							::fprintf(stderr, "xml fuzz: value graft differs\n  запись [%s]\n", value.dump().c_str());
							// Выводим исходный текст разметки
							dump(text);
							// Выводим результат проверки дерева разметки
							return false;
						}
						// Получаем путь к привитому узлу дерева разметки
						const string path = (string("/host/") + string(host.element().first().name().local));
						/**
						 * Если сброс содержимого привитого узла завершился отказом
						 *
						 * @note Сброс обязан оставить сам узел на месте, лишив его одного лишь
						 *       содержимого: имя и свойства узла сброс не трогает вовсе
						 */
						if(!host.reset(path)){
							// Выводим сообщение об отказе сброса содержимого узла
							::fprintf(stderr, "xml fuzz: node reset refused\n  путь [%s]\n", path.c_str());
							// Выводим исходный текст разметки
							dump(text);
							// Выводим результат проверки дерева разметки
							return false;
						}
						// Увеличиваем счёт сброшенных узлов
						totals.resets++;
						/**
						 * Если сброшенный узел место своё утратил либо содержимое сохранил
						 */
						if(!host.has(path) || host.at(path).first().valid()){
							// Выводим сообщение о расхождении сброса содержимого узла
							::fprintf(stderr, "xml fuzz: node reset differs\n  запись [%s]\n", host.dump().c_str());
							// Выводим исходный текст разметки
							dump(text);
							// Выводим результат проверки дерева разметки
							return false;
						}
						/**
						 * Если снос сброшенного узла завершился отказом
						 *
						 * @note Снос обязан убрать сам узел: опрос наличия его отвечает
						 *       отсутствием - тем он и отличен от сброса
						 */
						if(!host.erase(path)){
							// Выводим сообщение об отказе сноса узла
							::fprintf(stderr, "xml fuzz: node removal refused\n  путь [%s]\n", path.c_str());
							// Выводим исходный текст разметки
							dump(text);
							// Выводим результат проверки дерева разметки
							return false;
						}
						// Увеличиваем счёт снесённых узлов
						totals.removals++;
						/**
						 * Если снесённый узел в дереве уцелел
						 */
						if(host.has(path) || host.element().first().valid()){
							// Выводим сообщение о расхождении сноса узла
							::fprintf(stderr, "xml fuzz: node removal differs\n  запись [%s]\n", host.dump().c_str());
							// Выводим исходный текст разметки
							dump(text);
							// Выводим результат проверки дерева разметки
							return false;
						}
					}
				}
				// Выполняем запись владеющего значения в текст
				const string record = value.dump();
				/**
				 * Если запись владеющего значения в текст удалась
				 *
				 * @note Пустой итог означает отказ записи, а не усечение: узел, записи
				 *       не подлежащий, отвергается потоком записи целиком
				 *
				 * @note Значение с соседствующим текстовым содержимым из сличения изъято:
				 *       запись сливает соседей воедино, и разбор её даёт один узел вместо
				 *       двух
				 */
				if(!record.empty() && !::adjacent(value)){
					// Создаём дерево разметки для разбора записанного значения
					xml::document_t reparsed(::logger());
					/**
					 * Если разбор записанного значения не удался
					 */
					if(!reparsed.parse(record, relaxed)){
						// Выводим сообщение об отказе разбора записанного значения
						::fprintf(stderr, "xml fuzz: value reparse failed, error=%u\n", static_cast <uint32_t> (reparsed.error()));
						// Выводим исходный текст разметки
						dump(text);
						// Выводим записанное значение
						dump(record);
						// Выводим результат проверки дерева разметки
						return false;
					}
					// Разыскиваемый узел разметки записанного значения
					xml::node_t found = reparsed.root().first();
					/**
					 * Выполняем поиск узла разметки среди содержимого корня
					 */
					while(found.valid() && (found.kind() != xml::kind_t::ELEMENT))
						// Выполняем переход к следующему узлу содержимого корня
						found = found.next();
					/**
					 * Если снятое с записи значение разошлось с исходным
					 */
					if(!found.valid() || !(xml::value_t(found) == value)){
						// Выводим сообщение о расхождении снятого с записи значения с исходным
						::fprintf(stderr, "xml fuzz: value differs after rewrite\n  запись [%s]\n", record.c_str());
						/**
						 * Выполняем перебор всего содержимого снятого значения
						 */
						for(size_t i = 0; i < value.size(); i++)
							// Выводим вид и содержимое очередного узла снятого значения
							::fprintf(stderr, "  снято  %zu: вид=%u имя=[%s] текст=[%s]\n", i, static_cast <uint32_t> (value[i].kind()), value[i].local().c_str(), value[i].text().c_str());
						/**
						 * Если узел разметки записанного значения разыскан
						 */
						if(found.valid()){
							// Снимаем владеющее значение с разысканного узла разметки
							const xml::value_t written(found);
							/**
							 * Выполняем перебор всего содержимого снятого с записи значения
							 */
							for(size_t i = 0; i < written.size(); i++)
								// Выводим вид и содержимое очередного узла снятого с записи значения
								::fprintf(stderr, "  прочтено %zu: вид=%u имя=[%s] текст=[%s]\n", i, static_cast <uint32_t> (written[i].kind()), written[i].local().c_str(), written[i].text().c_str());
						}
						// Выводим исходный текст разметки
						dump(text);
						// Выводим результат проверки дерева разметки
						return false;
					}
				}
			}
		}
		// Выводим результат проверки дерева разметки
		return true;
	}

	/**
	 * @brief Метод приведения текста разметки из UTF-8 в UTF-16
	 *
	 * @details Кодек обещает одинаковый разбор одного и того же текста, в какой бы из
	 * принимаемых кодировок он ни пришёл. Проверить обещание нечем, пока подача ведётся
	 * одной лишь UTF-8, и приведение здесь строит второй вид того же самого текста
	 *
	 * @param source приводимый текст разметки в кодировке UTF-8
	 * @param big    признак прямого порядка байтов в приведённом тексте
	 * @param result собираемый текст разметки в кодировке UTF-16
	 * @return       результат выполнения операции
	 *
	 */
	bool transcode(const string & source, const bool big, string & result) noexcept {
		// Выполняем очистку собираемого текста разметки
		result.clear();
		// Выполняем добавление метки порядка байтов
		result.append(big ? "\xFE\xFF" : "\xFF\xFE", 2);
		/**
		 * Определяем положение начала приводимого текста разметки
		 *
		 * @note Метка порядка байтов, стоящая в исходном тексте, приведению не подлежит:
		 *       приведённый текст получает свою собственную, а перенесённая наравне с
		 *       содержимым стала бы обычным знаком перед объявлением
		 */
		const size_t begin = ((source.compare(0, 3, "\xEF\xBB\xBF") == 0) ? 3 : 0);
		/**
		 * Выполняем перебор всех знаков приводимого текста разметки
		 */
		for(size_t i = begin; i < source.size();){
			// Получаем очередной байт приводимого текста разметки
			const uint8_t byte = static_cast <uint8_t> (source[i]);
			// Значение знака, собираемое из байтов
			uint32_t code = byte;
			// Количество байтов, занятых знаком
			size_t length = 1;
			/**
			 * Если байт началом знака быть не вправе, приведение невозможно
			 *
			 * @note Договор о кодировке отводит под знак не более четырёх байтов, и
			 *       байты от 0xF8 началом знака не являются вовсе. Принятые прежде за
			 *       начало четырёхбайтового знака, они давали приведению годное значение
			 *       там, где кодек текст верно отвергает, - и сличение кодировок ловило
			 *       собственный изъян приведения вместо расхождения кодека
			 */
			if(byte >= 0xF8) return false;
			// Если знак записан четырьмя байтами
			if(byte >= 0xF0){ code = (byte & 0x07); length = 4; }
			// Если знак записан тремя байтами
			else if(byte >= 0xE0){ code = (byte & 0x0F); length = 3; }
			// Если знак записан двумя байтами
			else if(byte >= 0xC0){ code = (byte & 0x1F); length = 2; }
			// Если знак записан продолжающим байтом, приведение невозможно
			else if(byte >= 0x80) return false;
			// Если знак выходит за пределы приводимого текста, приведение невозможно
			if((i + length) > source.size()) return false;
			/**
			 * Выполняем перебор всех продолжающих байтов знака
			 */
			for(size_t k = 1; k < length; k++){
				// Получаем очередной продолжающий байт знака
				const uint8_t next = static_cast <uint8_t> (source[i + k]);
				// Если байт продолжающим не является, приведение невозможно
				if((next & 0xC0) != 0x80) return false;
				// Выполняем добавление разрядов продолжающего байта к значению знака
				code = ((code << 6) | (next & 0x3F));
			}
			// Выполняем переход к следующему знаку приводимого текста
			i += length;
			/**
			 * Если знак записан длиннее необходимого, приведение невозможно
			 *
			 * @note Проверка обязательна: избыточная запись знака кодеком отвергается,
			 *       а приведение, её принявшее, построило бы из отвергнутого текста
			 *       текст пригодный - и сличение кодировок поймало бы собственный изъян
			 *       приведения вместо расхождения кодека
			 */
			if((length > 1) && (code < (((length == 2) ? 0x80u : ((length == 3) ? 0x800u : 0x10000u)))))
				// Выводим отрицательный результат выполнения операции
				return false;
			// Если знак недопустим договором, приведение невозможно
			if(!xml::isChar(code)) return false;
			// Пара шестнадцатибитных значений, записывающих знак
			uint16_t units[2] = {0, 0};
			// Количество шестнадцатибитных значений, занятых знаком
			size_t count = 1;
			/**
			 * Если знак лежит за пределами основного набора
			 */
			if(code >= 0x10000){
				// Получаем остаток значения знака сверх основного набора
				const uint32_t rest = (code - 0x10000);
				// Запоминаем старшее значение суррогатной пары
				units[0] = static_cast <uint16_t> (0xD800 + (rest >> 10));
				// Запоминаем младшее значение суррогатной пары
				units[1] = static_cast <uint16_t> (0xDC00 + (rest & 0x3FF));
				// Запоминаем количество значений, занятых знаком
				count = 2;
			// Запоминаем единственное значение, записывающее знак
			} else units[0] = static_cast <uint16_t> (code);
			/**
			 * Выполняем перебор всех значений, записывающих знак
			 */
			for(size_t k = 0; k < count; k++){
				// Получаем старший байт очередного значения
				const char high = static_cast <char> (units[k] >> 8);
				// Получаем младший байт очередного значения
				const char low = static_cast <char> (units[k] & 0xFF);
				// Выполняем добавление байтов значения в заданном порядке
				if(big){ result.push_back(high); result.push_back(low); }
				else { result.push_back(low); result.push_back(high); }
			}
		}
		// Выводим результат выполнения операции
		return true;
	}

	/**
	 * @brief Метод сброса смещений начала событий в перечне
	 *
	 * @note Смещение считается байтами исходного текста и кодировкой его разнится
	 *       по самому устройству: один и тот же знак занимает в UTF-16 вдвое больше
	 *       места. Сличению кодировок оно потому не подлежит, в отличие от места
	 *       события, считаемого строками и знаками
	 *
	 * @param events перечень событий, смещения которого сбрасываются
	 *
	 */
	void detach(vector <Event> & events) noexcept {
		/**
		 * Выполняем перебор всех событий перечня
		 */
		for(Event & event : events)
			// Выполняем сброс смещения начала события
			event.position = 0;
	}

	/**
	 * @brief Метод сведения подряд идущего содержимого в одно событие
	 *
	 * @details С выключенной склейкой разбор выдаёт содержимое частями по мере их
	 * прихода: событий выходит тем больше, чем мельче куски подачи, и сличать перечни
	 * знак в знак нельзя. Сличению подлежит содержимое, а не его нарезка на события:
	 * подряд идущие события одного вида сводятся в одно, и после сведения перечни
	 * обязаны совпасть при всякой нарезке
	 *
	 * @note Сводятся лишь события содержимого - текстового, пробельного и дословного
	 *       раздела: всё прочее выдаётся целиком по самому устройству разбора, и
	 *       сведение его скрыло бы расхождение
	 *
	 * @note Место сведённого события берётся от первого из них, а глубина у них общая:
	 *       части одного содержимого лежат в одном узле
	 *
	 * @param events сводимый перечень событий разбора
	 *
	 */
	void merge(vector <Event> & events) noexcept;

	/**
	 * @brief Метод усечения содержимого, выданного до обнаружения отказа
	 *
	 * @details Содержимое выдаётся частями по мере прихода кусков, и отказ, лежащий за
	 * уже выданной частью, её не отменяет: разбор кусками успевает выдать содержимое
	 * прежде, чем дойдёт до отказа, а разбор целиком видит отказ сразу и не выдаёт
	 * ничего. Свойство это принадлежит самой потоковости, а не кодеку - потребитель
	 * обязан отбросить выданное при всяком отказе
	 *
	 * @note Послабление ограничено хвостовым содержимым: снимается одно последнее
	 *       событие содержимого у обоих перечней разом. Всё, что стоит до него, сличается
	 *       без послаблений - расхождение вида события, его места либо имени остаётся
	 *       расхождением
	 *
	 * @param whole   перечень событий подачи целиком
	 * @param chunked перечень событий подачи кусками
	 *
	 */
	void clip(vector <Event> & whole, vector <Event> & chunked) noexcept {
		/**
		 * @brief Метод проверки события на принадлежность к содержимому
		 *
		 * @param event проверяемое событие разбора
		 * @return      результат проверки
		 *
		 */
		auto content = [](const Event & event) noexcept -> bool {
			/**
			 * Определяем разновидность проверяемого события
			 */
			switch(event.event){
				// Если событие является содержимым
				case static_cast <uint8_t> (xml::event_t::TEXT):
				case static_cast <uint8_t> (xml::event_t::SPACE):
				case static_cast <uint8_t> (xml::event_t::CDATA):
					// Выводим положительный результат проверки
					return true;
			}
			// Выводим отрицательный результат проверки
			return false;
		};
		/**
		 * Выполняем снятие лишнего хвостового содержимого с перечня подачи кусками
		 *
		 * @note Отказ обрывает содержимое, и подача кусками успевает выдать его частями
		 *       там, где подача целиком видит отказ прежде выдачи
		 */
		while((chunked.size() > whole.size()) && content(chunked.back()))
			// Выполняем снятие хвостового содержимого с перечня подачи кусками
			chunked.pop_back();
		/**
		 * Выполняем снятие лишнего хвостового содержимого с перечня подачи целиком
		 */
		while((whole.size() > chunked.size()) && content(whole.back()))
			// Выполняем снятие хвостового содержимого с перечня подачи целиком
			whole.pop_back();
		/**
		 * Если оба перечня оканчиваются содержимым
		 *
		 * @note Последнее содержимое оборвано отказом, и оборвано оно на разных местах:
		 *       выданное подачей целиком обязано быть началом выданного кусками
		 */
		if(!whole.empty() && !chunked.empty() && content(whole.back()) && content(chunked.back()) &&
		   (chunked.back().event == whole.back().event) &&
		   (chunked.back().text.compare(0, whole.back().text.size(), whole.back().text) == 0))
			// Выполняем усечение хвостового содержимого подачи кусками до общего начала
			chunked.back().text.assign(whole.back().text);
	}

	void merge(vector <Event> & events) noexcept {
		// Собираемый перечень сведённых событий
		vector <Event> result;
		// Выполняем отведение места под сведённый перечень
		result.reserve(events.size());
		/**
		 * Выполняем перебор всех событий перечня
		 */
		for(Event & event : events){
			/**
			 * Определяем разновидность очередного события
			 */
			switch(event.event){
				// Если событие является содержимым
				case static_cast <uint8_t> (xml::event_t::TEXT):
				case static_cast <uint8_t> (xml::event_t::SPACE):
				case static_cast <uint8_t> (xml::event_t::CDATA): {
					/**
					 * Если предыдущее событие является содержимым того же вида
					 */
					if(!result.empty() && (result.back().event == event.event) && (result.back().depth == event.depth)){
						// Выполняем присоединение содержимого к предыдущему событию
						result.back().text.append(event.text);
						// Выполняем переход к следующему событию перечня
						continue;
					}
				} break;
			}
			// Выполняем добавление события к сведённому перечню
			result.push_back(::move(event));
		}
		// Выполняем замену перечня событий сведённым
		events = ::move(result);
	}
}

/**
 * @brief Функция запуска приложения
 *
 * @param argc длина массива параметров
 * @param argv массив параметров
 * @return     код выхода из приложения
 *
 */
int32_t main(int32_t argc, char * argv[]) noexcept {
	// Количество выполняемых проходов генератора
	uint64_t count = 3000;
	// Если количество проходов задано параметром командной строки
	if(argc > 1)
		// Выполняем чтение количества проходов из параметра командной строки
		count = static_cast <uint64_t> (::strtoull(argv[1], nullptr, 10));
	// Зерно источника псевдослучайных чисел
	uint32_t seed = 0x5A1CE;
	/**
	 * Если зерно задано вторым параметром командной строки
	 *
	 * @note Зерно закреплено по умолчанию: прогон обязан воспроизводиться. Задавать
	 *       его иным стоит затем, что построения одного зерна складываются в один и
	 *       тот же набор, сколько проходов ни задай, - несколько зёрен обходят
	 *       разметку шире, чем один длинный прогон
	 */
	if(argc > 2)
		// Выполняем чтение зерна из параметра командной строки
		seed = static_cast <uint32_t> (::strtoul(argv[2], nullptr, 10));
	// Создаём источник псевдослучайных чисел с закреплённым зерном
	mt19937 engine(seed);
	/**
	 * Выполняем проходы генератора
	 */
	for(uint64_t i = 0; i < count; i++){
		// Выполняем выборку настроек разбора текста разметки
		const xml::reader_t::settings_t options = ::settings(engine);
		/**
		 * Признак построения заведомо правильной разметки
		 *
		 * @note Половина текстов строится правильными намеренно: разметка, отвергаемая
		 *       первой же меткой, до разрешения пространств имён, подстановки умолчаний
		 *       и сборки дерева не доходит вовсе, и пути эти остались бы непройденными
		 */
		const bool valid = ((engine() % 2) == 0);
		// Количество испорченных текстов разметки до построения очередного
		const uint64_t before = totals.corrupted;
		// Выполняем построение текста разметки
		string text = ::generate(engine, valid);
		// Выполняем учёт построенного текста разметки
		totals.texts++;
		/**
		 * Если требуется испортить построенный текст разметки
		 */
		if((engine() % 3) == 0){
			// Выполняем порчу построенного текста разметки
			::corrupt(text, engine);
			// Выполняем учёт испорченного текста разметки
			totals.corrupted++;
		}
		// Признак того, что построенный правильным текст остался неиспорченным
		const bool intact = (valid && (totals.corrupted == before));
		// Если построенный текст разметки остался заведомо правильным
		if(intact)
			// Выполняем учёт построенного заведомо правильного текста разметки
			totals.intact++;
		// Перечень событий подачи текста разметки целиком
		vector <Event> whole;
		// Место отказа разбора текста разметки, поданного целиком
		Event failure;
		// Выполняем подачу текста разметки целиком
		const xml::state_t state = ::consume(text, options, 0, whole, &failure);
		/**
		 * Если текст разметки разобран до конца
		 */
		if(state == xml::state_t::FINISHED){
			// Выполняем учёт разобранного до конца текста разметки
			totals.survived++;
			// Если разобранный текст построен заведомо правильным и не испорчен
			if(intact)
				// Выполняем учёт принятого разбором правильного текста разметки
				totals.accepted++;
		}
		/**
		 * Выполняем сличение подачи целиком с подачей кусками нескольких размеров
		 *
		 * @note Размеров берётся несколько, а не один: расхождения предела объёма
		 *       события проступают лишь при таких размерах куска, где накопление
		 *       успевает превысить предел прежде, чем построение будет дочитано,
		 *       и одиночный размер их обходит стороной
		 */
		for(uint32_t attempt = 0; attempt < 3; attempt++){
			// Размер куска подачи текста разметки
			const size_t chunk = ((attempt == 0) ? 1 : (1 + (engine() % 24)));
			// Перечень событий подачи текста разметки кусками
			vector <Event> chunked;
			// Место отказа разбора текста разметки, поданного кусками
			Event second;
			// Выполняем подачу текста разметки кусками
			const xml::state_t reached = ::consume(text, options, chunk, chunked, &second);
			/**
			 * Если склейка подряд идущего содержимого выключена
			 *
			 * @note Без склейки нарезка на события задаётся размером куска подачи, а
			 *       сличению подлежит само содержимое: подряд идущее сводится в одно
			 *       событие у обоих перечней разом
			 */
			if(!options.mergeText){
				// Выполняем сведение подряд идущего содержимого подачи целиком
				::merge(whole);
				// Выполняем сведение подряд идущего содержимого подачи кусками
				::merge(chunked);
			}
			/**
			 * Если разбор прекращён ошибкой при выключенной склейке
			 *
			 * @details Содержимое выдаётся частями по мере прихода кусков, и отказ, лежащий
			 * за уже выданной частью, её не отменяет: разбор кусками успевает выдать
			 * содержимое прежде, чем дойдёт до отказа, а разбор целиком видит отказ сразу и
			 * не выдаёт ничего. Свойство это принадлежит самой потоковости, а не кодеку -
			 * потребитель обязан отбросить выданное при всяком отказе
			 *
			 * @note Послабление ограничено: перечень подачи целиком обязан быть началом
			 *       перечня подачи кусками, а всё лишнее в нём - одним лишь содержимым.
			 *       Расхождение вида события, его места либо имени под послабление не
			 *       подпадает
			 */
			if(!options.mergeText && (reached == xml::state_t::FAILED))
				// Выполняем усечение содержимого, выданного до обнаружения отказа
				::clip(whole, chunked);
			// Если перечни выданных разбором событий разошлись
			if(!::compare(whole, chunked, chunk, text, options))
				// Выходим из приложения с кодом ошибки
				return EXIT_FAILURE;
			/**
			 * Если состояние разбора либо место отказа разошлись
			 */
			if((reached != state) || (failure.event != second.event) || (failure.line != second.line) ||
			   (failure.column != second.column) || (failure.position != second.position)){
				// Выводим сообщение о расхождении итога разбора
				::fprintf(stderr, "xml fuzz: chunk=%zu outcome differs: state %u/%u error %u/%u at %u:%u/%u:%u offset %llu/%llu\n",
					chunk, (unsigned) state, (unsigned) reached, failure.event, second.event,
					failure.line, failure.column, second.line, second.column,
					(unsigned long long) failure.position, (unsigned long long) second.position);
				// Выводим разбираемый текст разметки
				dump(text);
				// Выходим из приложения с кодом ошибки
				return EXIT_FAILURE;
			}
		}
		/**
		 * Выполняем сличение подачи того же текста разметки в кодировке UTF-16
		 *
		 * @details Кодек обещает разбирать один и тот же текст одинаково, в какой бы из
		 * принимаемых кодировок он ни пришёл: приведение к UTF-8 ведётся до разбора, и
		 * ниже него кодировка исходного текста не различима вовсе. Проверяется обещание
		 * обоими порядками байтов и подачей кусками - границы кусков ложатся при этом
		 * посреди шестнадцатибитных значений и посреди суррогатных пар
		 *
		 * @note Текст с объявленной кодировкой пропускается: объявление разошлось бы с
		 *       меткой порядка байтов, и разбор отверг бы его по этому расхождению.
		 *       Ищется одно лишь слово, без знака присваивания: договор допускает пробелы
		 *       вокруг него, и `encoding ="UTF-8"` иначе проскакивал бы мимо пропуска
		 *
		 * @note Предел объёма события снимается: он меряется байтами разметки, а их в
		 *       UTF-16 вдвое больше при том же самом тексте
		 */
		if(!text.empty() && (text.find("encoding") == string::npos)){
			// Настройки разбора со снятым пределом объёма события
			xml::reader_t::settings_t relaxed = options;
			// Выполняем снятие предела объёма одного события
			relaxed.maxEvent = 0;
			// Перечень событий подачи текста разметки в кодировке UTF-8
			vector <Event> narrow;
			// Место отказа разбора текста разметки в кодировке UTF-8
			Event first;
			// Выполняем подачу текста разметки в кодировке UTF-8 целиком
			const xml::state_t reached = ::consume(text, relaxed, 0, narrow, &first);
			// Выполняем сброс смещений начала событий, кодировкой разнящихся
			::detach(narrow);
			/**
			 * Если склейка подряд идущего содержимого выключена
			 *
			 * @note Подача целиком тоже сводится: с выключенной склейкой она выдаёт
			 *       содержимое частями там, где текст приходит не одним куском
			 */
			if(!relaxed.mergeText)
				// Выполняем сведение подряд идущего содержимого подачи целиком
				::merge(narrow);
			/**
			 * Выполняем перебор обоих порядков байтов кодировки UTF-16
			 */
			for(const bool big : {false, true}){
				// Текст разметки, приведённый к кодировке UTF-16
				string wide;
				// Если приведение текста разметки к кодировке UTF-16 не удалось
				if(!::transcode(text, big, wide))
					// Выполняем переход к следующему порядку байтов
					continue;
				/**
				 * Выполняем перебор размеров куска подачи текста разметки
				 *
				 * @note Нулевой размер означает подачу целиком, однобайтовый разрывает
				 *       всякое шестнадцатибитное значение, а трёхбайтовый - ещё и всякую
				 *       суррогатную пару, не совпадая с её длиной
				 */
				for(const size_t chunk : {static_cast <size_t> (0), static_cast <size_t> (1), static_cast <size_t> (3)}){
					// Перечень событий подачи текста разметки в кодировке UTF-16
					vector <Event> broad;
					// Место отказа разбора текста разметки в кодировке UTF-16
					Event second;
					// Выполняем подачу текста разметки в кодировке UTF-16
					const xml::state_t arrived = ::consume(wide, relaxed, chunk, broad, &second);
					// Выполняем сброс смещений начала событий, кодировкой разнящихся
					::detach(broad);
					/**
					 * Если склейка подряд идущего содержимого выключена
					 *
					 * @note Без склейки нарезка на события задаётся размером куска подачи,
					 *       а сличению подлежит само содержимое
					 */
					if(!relaxed.mergeText){
						// Выполняем сведение подряд идущего содержимого подачи кусками
						::merge(broad);
						/**
						 * Если разбор прекращён ошибкой
						 *
						 * @note Послабление то же, что и при сличении нарезки: содержимое,
						 *       выданное до обнаружения отказа, отказом не отменяется
						 */
						if(arrived == xml::state_t::FAILED)
							// Выполняем усечение содержимого, выданного до обнаружения отказа
							::clip(narrow, broad);
					}
					// Если перечни выданных разбором событий разошлись
					if(!::compare(narrow, broad, chunk, text, relaxed)){
						// Выводим сообщение о кодировке, при которой обнаружено расхождение
						::fprintf(stderr, "xml fuzz: UTF-16%s\n", (big ? "BE" : "LE"));
						// Выходим из приложения с кодом ошибки
						return EXIT_FAILURE;
					}
					/**
					 * Если состояние разбора либо место отказа разошлись
					 */
					if((arrived != reached) || (first.event != second.event) ||
					   (first.line != second.line) || (first.column != second.column)){
						// Выводим сообщение о расхождении итога разбора между кодировками
						::fprintf(stderr, "xml fuzz: UTF-16%s chunk=%zu outcome differs: state %u/%u error %u/%u at %u:%u/%u:%u\n",
							(big ? "BE" : "LE"), chunk, (unsigned) reached, (unsigned) arrived,
							first.event, second.event, first.line, first.column, second.line, second.column);
						// Выводим разбираемый текст разметки
						dump(text);
						// Выходим из приложения с кодом ошибки
						return EXIT_FAILURE;
					}
				}
			}
		}
		// Если проверка дерева разметки не удалась
		if(!::tree(text, options, engine))
			// Выходим из приложения с кодом ошибки
			return EXIT_FAILURE;
	}
	// Выводим статистику работы генератора
	::fprintf(
		stdout,
		"xml fuzz: seed=%u, %llu texts (%llu corrupted), %llu events, %llu parsed to the end, %llu trees, %llu rewrites, %llu of %llu intact texts accepted, %llu values, %llu builds, %llu grafts, %llu resets, %llu removals\n",
		seed,
		static_cast <unsigned long long> (totals.texts),
		static_cast <unsigned long long> (totals.corrupted),
		static_cast <unsigned long long> (totals.events),
		static_cast <unsigned long long> (totals.survived),
		static_cast <unsigned long long> (totals.trees),
		static_cast <unsigned long long> (totals.rewrites),
		static_cast <unsigned long long> (totals.accepted),
		static_cast <unsigned long long> (totals.intact),
		static_cast <unsigned long long> (totals.values),
		static_cast <unsigned long long> (totals.builds),
		static_cast <unsigned long long> (totals.grafts),
		static_cast <unsigned long long> (totals.resets), static_cast <unsigned long long> (totals.removals)
	);
	// Выходим из приложения
	return EXIT_SUCCESS;
}
